# 项目固定规则

以后修改这个仓库前先读本文件。

## 版本同步清单

每次更新插件版本时，必须同步修改这些位置：

- `main.py` 里的 `@register(..., "版本号")`
- `metadata.yaml` 里的 `version`
- `README.md` 项目信息表里的版本
- `CHANGELOG.md` 顶部新增对应版本更新记录

版本检查命令：

- `rg -n "1\.[0-9]+\.[0-9]+|v1\.[0-9]+\.[0-9]+" main.py metadata.yaml README.md CHANGELOG.md`

版本递增规则：

- 新增功能时中版本号递增，小版本号归零，例如 `v1.5.52` 后新增功能应为 `v1.6.0`。
- 修复 bug、兼容性调整、文案调整和内部重构时只递增小版本号。
- 大版本号只在出现破坏性变更、配置不兼容或功能边界明显重构时递增。

## 收尾流程

每次运行代码检查或修改代码后，必须执行：

- 删除仓库内生成的 `.pyc` 文件和空的 `__pycache__` 目录。
- 验证用的临时副本、差异文件、回滚脚本和 `_codex_artifacts/` 目录只允许放在仓库外的临时目录；验证结束后删除仓库内残留，不要提交或长期保留这些无用文件夹。
- 运行 `git status --short` 确认变更。
- 提交 Git，提交信息使用中文，格式为：`更新版本号至 vX.Y.Z，变更说明`。
- 提交成功后必须同步推送 GitHub `main`：先 `git fetch origin`，确认远端 `main` 可快进或已合并当前提交，再执行 `git push origin HEAD:main`；不得只推功能分支。
- 如果没有版本升级，提交信息使用：`更新功能，变更说明`。
- 删除明显无用的旧代码，避免保留历史撤回、重复文本清理等不再需要的逻辑。
- 本仓库默认不保留测试源码，保持仓库干净；用户明确要求删除测试时删除全部 `tests/` 测试文件和空目录。后续修改以语法检查、静态差异检查和必要的手工验证为准，不重新新增测试文件，除非用户再次明确要求保留测试。
- `main.py` 只用来调用：只保留插件注册、模块加载和功能分发，不写具体功能实现、消息解析或业务判断。
- 管理功能按分类放到 `功能文件/管理功能/基础功能/`、`功能文件/管理功能/小说功能/`、`功能文件/管理功能/网盘功能/` 和 `功能文件/管理功能/群聊功能/` 内；小说功能内的平台下载模块放入 `小说功能/小说/`，找书、开关和缓存清理等通用模块放入 `小说功能/功能/`；`功能文件/API功能/` 已整体删除，禁止重新引入 OIAPI、析API、崩溃API、聚合API、Y6api 或对应开关、切换指令。
- 群管功能放在 `功能文件/管理功能/群聊功能/群管功能.py`；数字撤回判断只能使用可见文本或 text 消息段，禁止把事件对象、非文本消息段或 ID 字段 `str()` 后参与数字匹配。
- 插件配置必须使用 AstrBot `_conf_schema.json`；新增配置时同步说明 README、CHANGELOG 和 AGENTS。运行时状态可以写入 `功能文件/下载缓存/`，但不能伪装成插件配置项；小说 TXT 写入后必须创建 `.txt.uploading` 占用标记，只有上传和后台备份结束后随源文件清除。插件启动或重载时必须清理该目录内上次运行遗留的小说 txt；运行期间每天按服务器本地时间零点删除文件日期早于当天的小说 txt，例如 22 日零点删除 21 日及更早的文件并保留 22 日文件。两种清理都必须跳过当前进程仍持有占用标记或等待续传的上传文件。

## 排错原则

- 所有面向用户的失败、异常和远端错误回复禁止直接拼接异常对象、请求 URL、响应原文或配置值；账号、IP、Cookie、Key、Token、数据库连接信息和本地路径只允许写入后端日志。QQ 用户侧的下载、文件发送和接口切换失败只使用固定短提示，不显示具体 API 名称或接口业务内容；API 名称只允许在帮助和手动切换菜单中展示。
- 不要只按猜测下结论。遇到 OneBot、AstrBot、适配器消息段等外部协议细节不确定时，必须先联网查官方文档或上游资料，再改代码。
- 如果不知道如何做，或者用户给的消息样本不足以确定消息段结构，必须先联网搜索官方文档、上游源码或适配器资料。
- 日志行号和版本号必须作为判断依据。如果用户日志里的行号与当前源码不一致，优先怀疑 AstrBot 实际运行的插件目录没有同步或仍在加载旧代码。
- 排查加载问题时，应临时让运行日志输出当前插件版本和源码路径，避免误以为 GitHub 最新代码已经被 AstrBot 加载。
- 排查未知消息类型时，应先加受限诊断日志查看真实参数，包括 `message_str`、`raw_message`、消息段 type/data、对象字符串和 message_id，再按日志改规则；卡片类撤回应保留可定位真实消息段的诊断日志。
- OneBot 11 普通 `@人` 是 `at` 消息段或 `[CQ:at,qq=...]`，不是群名片；普通 `@人` 必须优先放行，不能进入数字撤回或群名片撤回。

## AstrBot 重载排查

- AstrBot 官方文档说明：代码修改后，在 WebUI 插件管理里点击 `管理` -> `重载插件`。
- 重载插件只重新加载 AstrBot 运行目录里的当前磁盘文件，不等于自动 `git pull` GitHub 最新代码。
- 本插件使用 `功能文件...` 这类顶层导入；AstrBot 重载主插件时，Python 可能仍缓存这些子模块。`main.py` 必须显式 `importlib.invalidate_caches()` 并 `reload()` 子模块；存在直接导入关系时必须先重载依赖模块，例如先加载 `运行状态数据库` 再加载 `状态功能`。
- 如果 GitHub 已更新但日志仍显示旧版本或旧行号，必须先进入 AstrBot 实际插件目录执行 `git pull`，再删除 `.pyc` 和空 `__pycache__`，最后重载插件。
- 正确加载最新版时，日志必须出现 `Plugin 馒头bot (vX.Y.Z)`；如果仍有缓存疑问，再临时加入启动日志确认版本号和源码路径。
- 如果 WebUI 重载后仍是旧行号，直接重启 AstrBot 进程或容器；重启前后都要确认 `/AstrBot/data/plugins/馒头bot` 下的文件内容就是最新版本。

## AstrBot 限流排查

- 日志出现 `rate_limit_check.stage`、`会话 xxx 被限流`、`根据限流策略，此会话处理将被暂停 xxx 秒` 时，优先判断为 AstrBot 核心限流，不是本插件代码限流。
- AstrBot 官方配置在 `data/cmd_config.json` 的 `platform_settings.rate_limit`，默认示例为 `time: 60`、`count: 30`、`strategy: "stall"`；`stall` 会暂停会话等待，`discard` 会丢弃超限消息。
- 官方文档说明 `data/cmd_config.json` 是默认配置 `default`，WebUI 新建的其他配置文件在 `data/config/abconf_*.json`；排查时必须确认当前平台实际使用哪个配置文件。
- 想取消或降低触发概率，应在 AstrBot 配置里调大 `count`、调小 `time`，或把 `strategy` 改成 `discard`；插件内无法清除已经由 AstrBot 核心挂起的会话。
- 如果已经触发 `stall`，通常只能等待暂停时间结束；想立即恢复应先尝试在 WebUI 禁用/启用对应会话或重载配置，仍无效再重启 AstrBot。

## 帮助网页

- 帮助网页放在与 `管理功能` 同级的 `功能文件/页面功能/`：`帮助网页.py` 只负责 `aiohttp` 服务生命周期和路由注册，`帮助网页后端.py` 负责配置、鉴权和 API，`帮助网页页面.py` 负责 HTML 结构，`帮助网页样式.py`、`帮助网页脚本.py` 分别负责控制台 CSS/JavaScript，登录页资源放在 `登录页面.py`、`登录页面样式.py`、`登录页面脚本.py`；不再保留 `功能文件/管理功能/基础功能/帮助网页.py` 旧路径文件。网页使用管理员控制台界面展示小说开关、测试模式、主分享网盘、网盘安全摘要、数据库状态和服务器运行状态，并提供白名单、帮助网页、网盘目录、数据库连接等现有插件配置的真实编辑入口；不在网页返回 Cookie、账号、密码、会话 Cookie、数据库地址或原始响应。插件配置 `_conf_schema.json` 的 `help_web_settings` 包含 `help_web_domain`、`help_web_host`、`help_web_port`、`help_web_admin_username` 和 `help_web_admin_password`；网页默认监听 `0.0.0.0:8090`，自动获取服务器 IPv4 生成访问地址，无需填写域名，填写有效 HTTP/HTTPS 外网地址时可覆盖自动地址。网页打开后使用配置账号密码登录，后端以 HttpOnly、SameSite=Lax 会话 Cookie 保护所有 `/api/*` 控制台接口，访问地址不携带登录凭据；数据库未配置时写入控件禁用。账号密码仍不写入 `mantou_runtime_state`，敏感配置只接受写入、不返回原文，密码不得写入日志或 API 响应；监听地址、端口、登录账号和配置变更需要重载插件生效。插件生命周期启动 `aiohttp` 网页服务，帮助顶层菜单显示外链按钮，原有文字/回调帮助保持可用。官方机器人帮助按钮使用 `action.type=1` 回调；仅“打开网页版帮助”外链按钮使用 `action.type=0`。

## 功能边界

- 不保留用户激活、卡密、收费、付费或每日免费额度功能；所有小说下载均免费。运行状态只使用 MySQL 的 `mantou_runtime_state` 表，不再创建或读取激活、卡密和免费额度表。
- 数据库连接信息来自 `_conf_schema.json` 的 `database_settings`，运行状态仅用于三种网盘 Cookie、QQ阅读登录态、小说主网盘和小说功能开关。未配置数据库时读取运行状态必须直接返回默认值，不得尝试连接 MySQL 或刷告警；小说下载功能默认开启，相关运行状态不持久化。QQ阅读不提供手机号、验证码或滑块登录；插件管理员白名单 QQ 可直接发送含 `ywguid/ywkey` 的浏览器 Cookie，插件只保存这两个必要字段到 `mantou_runtime_state`（namespace=`qq_reader_auth`，state_key=`login_state`），新 Cookie 覆盖旧登录态，下载开始时读取最新值覆盖模块固定 `uid/usid`；固定 `fuid/qrsn` 不变，动态密钥池仅在当前进程内保存，不生成 `keypool.cache`。
- 群管功能保留数字撤回逻辑：命中自动撤回规则后，必须先判断发送者 QQ 群身份，QQ群主和管理员发送的消息不撤回、不撤回最近消息、不累计三次踢出计数；只要能拿到数字群号和 QQ，就必须通过 OneBot `get_group_member_info` 查询真实 QQ 群成员角色，并以查询结果为准，不能因为事件里是 `member` 就跳过查询，也不能把插件配置管理员当作 QQ 群管理员；只有查不到接口、缺少数字群号或缺少 QQ 时，才使用事件或 sender 里的 `role` 兜底；`owner` 和 `admin` 都视为保护身份。普通成员先撤回当前触发消息，撤回成功后通过 OneBot `get_group_msg_history` 拉取最近 100 条群历史，筛选该用户最近消息并逐条调用 `delete_msg` 撤回，默认最多撤回 8 条；不要写臆造批量撤回接口。广告类消息撤回并成功禁言后只发送一条三行提醒：@发送者、请勿发送此类消息、如果是小说请联系群主；不再发送管理员/发送者撤回详情通知。数字撤回模块只撤回当前消息和最近消息，不再自动踢人；数字、群名片/JSON 卡片、合并转发和 QQ 闪传等本模块处理的撤回类型都计数。
- 群名片/群分享/JSON 卡片消息需要撤回当前消息，包括 `[ComponentType.Json]`、组件对象字符串中的 `ComponentType.Json`，以及 QQ 官方机器人空间相册分享等卡片降级展示文本 `暂不能查看该消息内容`。
- 白名单域名 `changdunovel.com`、`fanqienovel.com`、`fqnovel.com`、`novelfm.com`、`qimao.com`、`app-share.wtzw.com`、`shuqi.com`、`shuqireader.com`、`zhuishushenqi.com`、`zhuishuvip.com`、`reader.qq.com`、`book.qq.com`、`novel.html5.qq.com`、`mr.baidu.com`、`boxnovel.baidu.com`、`novel.baidu.com`、`reader.browser.miui.com`、`dushu.xiaomi.com`、`jjwxc.net`、`jjwxc.com` 不撤回；白名单判断必须优先于 JSON 卡片、合并转发、闪传和数字撤回判断。
- 合并转发/聊天记录消息需要撤回当前消息，包括 OneBot `forward`/`node` 和 AstrBot `ComponentType.Forward`、`ComponentType.Node`、`ComponentType.Nodes`。
- QQ 闪传消息需要撤回当前消息，只识别展示文本 `QQ闪传` 和 aiocqhttp 显示文本 `该消息类型暂不支持查看`；普通文件、OneBot `file`、`[CQ:file,...]`、AstrBot `ComponentType.File` 不应撤回。
- OneBot 11 标准撤回接口是单条 `delete_msg`，没有标准批量撤回接口；除非确认适配器支持扩展接口，否则只循环单条撤回，不写臆造的批量接口。
- 状态功能放在 `功能文件/管理功能/基础功能/状态功能.py`，`main.py` 只调用 `处理状态指令`。群文件清理管理员白名单 QQ 可发送 `状态` 查看系统位数、CPU 占用、物理内存、磁盘空间、系统进程、操作系统、框架版本、数据库状态、系统运行时间和当前时间；每次输入仅检查一次数据库连通性，数据库只显示 `正常`、`未配置` 或 `异常`，不展示 Cookie、账号、IP、密钥、原始响应、数据库地址、功能开关、收费或网盘信息。
- 帮助功能放在 `功能文件/管理功能/基础功能/帮助功能.py`，`main.py` 只调用 `处理帮助指令`、`处理帮助指令MD带键盘` 和 `解析帮助回调命令`；QQ 官方互动桥放在 `功能文件/管理功能/基础功能/QQ官方交互桥.py`，`main.py` 只负责安装桥接。帮助菜单只展示现存的管理、小说、网盘和群聊功能；官方机器人帮助按钮使用 `action.type=1` 回调并由互动桥安全投递，不向聊天发送指令文本。
- 帮助、状态、小说功能开关、小说网盘切换、夸克扫码登录、UC/夸克/百度/QQ阅读 Cookie 保存、禁言以及群聊管理命令只能由插件配置 `group_file_cleanup_admin_qq` 里的 QQ 使用；这是插件管理员白名单，不等同于群管理员。白名单外命中这些管理指令时一律静默忽略，不发送权限提示。
- 小说功能开关放在 `功能文件/管理功能/小说功能/功能/小说功能开关.py`，`main.py` 只调用 `处理小说功能开关指令`、`当前事件可使用小说功能` 和 `获取小说功能关闭回复`。群文件清理管理员白名单 QQ 可发送 `小说` / `小说列表` 查看全部小说功能状态；`开小说` / `关小说` 是全部群聊共用的总开关，关闭时小说链接下载、找书搜索、翻页和选书下载都不可用，重新开启后不改动各平台单独开关；可发送 `开番茄` / `关闭番茄`、`开七猫` / `关闭七猫`、`开书旗` / `关闭书旗`、`开追书` / `关闭追书`、`开QQ阅读` / `关闭QQ阅读`、`开QQ浏览器` / `关闭QQ浏览器`、`开得间` / `关闭得间`、`开点众` / `关闭点众`、`开盐言` / `关闭盐言`、`开塔读` / `关闭塔读`、`开百度` / `关闭百度`、`开小米` / `关闭小米` 和 `开晋江` / `关闭晋江` 开关对应下载功能，`开启平台名` 与 `开平台名` 等效，QQ阅读大小写不敏感；管理员可发送 `开测试` / `开启测试`、`关测试` / `关闭测试` 控制测试模式，测试模式开启后管理员可继续测试已关闭的平台，普通用户仍受平台开关限制；状态写入 MySQL 内置表 `mantou_runtime_state`，不再写入 `功能文件/下载缓存/小说功能开关.json`；关闭后识别到对应小说链接时只回复功能已关闭，不进入下载流程。
- 小说主分享网盘路由放在 `功能文件/管理功能/网盘功能/小说网盘.py`，`main.py` 只调用 `处理网盘Cookie指令` 和 `处理网盘切换指令`。群文件清理管理员白名单 QQ 可发送 `网盘` / `网盘状态` / `当前网盘` 查看 UC、夸克和百度配置状态；发送 `换UC`、`换夸克`、`换百度` 切换后续所有小说共用的主分享网盘。状态写入 MySQL `mantou_runtime_state`（namespace=`novel_share_pan`，state_key=`active`），默认 UC；只调用当前选择的网盘，不自动回退其他网盘。所有小说平台只能调用 `小说网盘.上传小说并获取分享链接` 和 `小说网盘.发送小说下载完成链接`，不得重新直接绑定 UC。三种网盘 Cookie 的识别、解析和 MySQL 读写统一放在 `功能文件/管理功能/网盘功能/网盘Cookie.py`；支持普通 Cookie、`Cookie:` 请求头、curl、Cookie Editor JSON 和 Netscape cookies.txt，也支持 `UC Cookie:`、`夸克 Cookie:`、`百度 Cookie:` 前缀。状态写入 `mantou_runtime_state`（namespace=`novel_pan_auth`，state_key 分别为 `uc`、`quark`、`baidu`），每个平台保存账号列表，插件配置 Cookie 作为账号1，管理员继续保存同平台 Cookie 时追加账号；群选择写入 `novel_pan_account_selection`，键按平台和群标识隔离，默认账号1，UC/夸克/百度选择互不影响。群内账号选择指令为 `UC换N`、`夸换N`/`夸克换N`、`百换N`/`百度换N`。数据库保存值优先于插件配置，新值覆盖对应账号；任何日志不得输出 Cookie 原文。
- UC 网盘自动上传功能放在 `功能文件/管理功能/网盘功能/UC网盘.py`，`main.py` 只负责加载模块。插件配置使用 `_conf_schema.json` 的 `uc_pan_settings` 分类，包含 `uc_pan_cookie` 和 `uc_pan_upload_dir`，该分类必须放在 `database_settings` 上面，数据库配置继续保持最下面。`uc_pan_cookie` 可在插件配置手动填写为账号1；白名单管理员也可直接粘贴含 `UDRIVE_TRANSFER_SESS` 且同时含 `ctoken`、`__puus` 或 `__uid` 之一的浏览器 Cookie，数据库保存值优先，后续同平台 Cookie 追加账号。`UC换N` 只改变当前群的 UC 账号。`uc_pan_upload_dir` 默认 `/小说机器人`，上传时自动逐级创建目录。UC 被选为主网盘时，上传前会分页扫描并删除目标目录下与原小说文件名完全相同的远端普通文件，不删除同名文件夹；目录列表里的文件夹判断必须兼容 `dir=1`、`dir=true` 和布尔 `True`，创建上传目录 `/file` 返回同名冲突时应重新查找已有目录继续上传；先尝试用原小说文件名上传，遇到 `23008`、`同名冲突`、`file is doloading` 或 `file is downloading` 时重新扫描并删除同名旧文件，然后继续使用原小说文件名有限重试，不得改用备用远端文件名；如果上传完成接口返回这类处理中错误，应复用当前预上传文件 ID 继续创建分享，创建分享接口仍返回处理中时必须等待重试。UC OSS 单分片 PUT 响应没有返回 `ETag` 时，可以使用本地文件 MD5 作为单分片 ETag 继续完成上传，不要直接判定失败。数据库登录态使用期间响应刷新 Cookie 时，只在数据库仍是本次请求原值时回写，禁止覆盖管理员刚保存的新 Cookie。
- 夸克网盘功能放在 `功能文件/管理功能/网盘功能/夸克网盘.py`，插件配置使用 `_conf_schema.json` 的 `quark_pan_settings` 分类，包含 `quark_pan_cookie` 和 `quark_pan_upload_dir`，必须放在 `database_settings` 上面。Cookie 可在插件配置手动填写为账号1；白名单管理员也可直接粘贴含 `__puus` 且同时含 `b-user-id`、`__uid` 或 `__kps` 之一的浏览器 Cookie，或在私聊发送 `夸克登录` / `登录夸克` / `刷新夸克Cookie` 获取网页扫码二维码，扫码成功后自动新增一个账号；同一管理员重复登录时必须先取消并等待旧任务完成，Token 和状态轮询请求必须按夸克网页当前 UOP 请求器固定携带 `client_id=532`、`v=1.2` 和每次唯一 UUID `request_id`，不得添加网页端未使用的缓存查询参数，插件停止时必须取消全部扫码轮询。群内 `夸换N`/`夸克换N` 只改变当前群夸克账号。扫码二维码参数必须跟随当前夸克网页实现，轮询只在取得 `service_ticket` 时结束，其他业务状态继续等待；`/account/info` 固定携带 `lw=scan`、`fr=pc` 和 `platform=pc` 并校验业务成功，失败日志只记录 `token` / `poll` / `auth` / `cookie` 阶段、脱敏状态和异常类型。夸克网页 API 固定使用 `drive-pc.quark.cn/1/clouddrive`，默认查询参数为 `pr=ucpro`、`fr=pc`、空 `uc_param_str`；短时 `ctoken` 即使存在也不得加入查询参数，不再访问主页探测补全。上传目录默认 `/小说机器人`。夸克被选为主网盘时自动逐级创建目录、删除同名普通文件并轮询删除任务完成，再按网页端预上传字段、哈希校验、OSS 单分片 PUT、带 XML Content-MD5 的完成分片和 `/file/upload/finish` 上传，通过 `/share` 与 `/share/mypage/detail` 获取分享链接；数据库登录态使用期间响应刷新 `__puus` 时，只在数据库仍是本次请求原值时回写，禁止覆盖管理员刚保存的新 Cookie；配置来源的 Cookie 只在本次上传会话内更新。
- 百度网盘功能放在 `功能文件/管理功能/网盘功能/百度网盘.py`，`main.py` 只负责加载模块。插件配置使用 `_conf_schema.json` 的 `baidu_pan_settings` 分类，包含 `baidu_pan_cookie`、`baidu_pan_upload_dir` 和 `baidu_pan_upload_status`，该分类必须放在 `database_settings` 上面。Cookie 可在插件配置手动填写为账号1；白名单管理员也可直接粘贴同时含 `BDUSS` 和 `STOKEN` 的浏览器 Cookie，数据库保存值优先，后续同平台 Cookie 追加账号。群内 `百换N`/`百度换N` 只改变当前群百度账号。百度被选为主网盘时上传完整原小说 txt，并通过 `/share/set` 创建带四位提取码的永久分享链接，提取码写入链接查询参数；主分享不受 `baidu_pan_upload_status` 限制。选择 UC 或夸克时，百度继续按状态规则后台备份；完结小说在目标目录已有同名普通文件时跳过，连载删除同名普通文件后重传，不删除同名文件夹；百度作为当前主网盘时必须跳过重复后台备份。百度后台上传失败只记录受控 warning，不回复用户，不影响完成按钮发送。
- 任一主网盘上传成功后，QQ 官方机器人必须在同一条消息中发送“宝宝你的 / 书名 / 作者 / 已经下载完成”、Markdown“点击此文字打开”链接和 `action.type=0` 的“点击打开”按钮；不生成链接 txt，也不回退发送原小说 txt。当前主网盘未配置或上传失败时，QQ 用户侧仅回复 `文件发送失败，请稍后再试`，详细错误只记后端日志。非 QQ 官方适配器可降级为同一完成文案加文本链接。
- QQ 官方 Markdown 提及群成员只能使用 `<@member_openid>`；`member_openid` 来自官方群消息事件的 `author/member_openid`，不得用 QQ 号替代。QQ 官方群聊中的管理回复由 `帮助功能.发送QQ官方提及Markdown` 统一发送，首行必须提及当前操作人；私聊不插入提及。QQ 官方文档未提供写入全量消息开关的 REST 接口；群管理员需要在机器人资料页手动调整群消息接收设置，插件不得使用未文档化跳转链接或 UID 转换接口尝试代开。
- QQ 官方机器人 `qq_official` 发送文件不能使用 OneBot 的 `upload_group_file`/`upload_private_file` 作为主路径；应优先使用 AstrBot `File` 组件，它会走 QQ 官方富媒体接口 `/v2/users/{openid}/files` 或 `/v2/groups/{group_openid}/files`，再发送 `msg_type=7` 的 `media` 消息。如果 QQ 官方群聊返回 `call inner proxy error`，这是平台富媒体上传限制或临时错误，不要用臆造 OneBot 接口修复。
- 通过 AstrBot `File` 组件发送本地缓存文件时，不能在 yield 后立即删除缓存；应延迟清理，避免 qq_official/其他适配器在响应阶段读取文件前缓存已不存在。OneBot 回退接口调用完成后可以立即删除缓存。
- 找书功能放在 `功能文件/管理功能/小说功能/功能/找书.py`，`main.py` 只加载模块并优先调用 `获取找书下载回复流` 和 `处理找书指令`。用户发送 `找关键词`、`找书 关键词`、`找书名 关键词` 或 `找作者 关键词` 聚合搜索番茄、七猫、QQ阅读、QQ浏览器、书旗、追书、得间、点众、塔读、百度、小米、宜搜、米读、猫眼、酷我、酷匠、连城、菠萝包和晋江；前两种命令自动判断书名/作者意图，后两种显式指定；每页 5 条；支持 `上一页` / `下一页`。结果只展示书名、作者和页码，不展示链接、来源、平台名、热度数值和序号；分隔线使用 `————————`。跨平台同名同作者去重只保留一本；排序必须先按精确、前缀、包含、反向包含和保守模糊匹配分档，再比较实际匹配字数、查询覆盖率、多平台共识和次级热度；无直接书名或作者关系的结果必须过滤，多词查询的每个词都必须在书名或作者中命中。QQ 阅读候选必须先完成详情与完整目录预检，免费书、VIP 书和有免费章节的单章付费书均可展示；完全没有免费章节、目录不完整或预检失败的候选才剔除。QQ 官方机器人搜索结果使用 Markdown 文字指令链：书名、作者和翻页文字使用 `qqbot-cmd-input`，点击后将 `选N` 或翻页命令填入输入框，用户发送后进入现有下载或翻页流程；兼容手动发送 `选1`～`选5`。找书会话按群/私聊 + 用户标识隔离，需兼容 `group_openid` / `user_openid` / `group_member_openid`；会话过期后提示重新找书。用户侧失败只回短提示，解析失败细节只写后端日志。
- 七猫小说功能只允许放在 `功能文件/管理功能/小说功能/小说/七猫小说.py` 一个文件内，不拆分多个 py 文件；`main.py` 只负责加载模块并调用 `获取七猫小说回复流`。
- 百度小说功能只允许放在 `功能文件/管理功能/小说功能/小说/百度小说.py`，小米小说功能只允许放在 `功能文件/管理功能/小说功能/小说/小米小说.py`；两个模块只保留异步搜索、链接识别、详情、目录、正文、完整性校验、TXT 和小说网盘出口，不迁移旧插件的 EPUB、封面、外部上传服务或本地账号状态。百度小说还需支持 `boxnovel.baidu.com/boxnovel/yuedu/wapdetail` 分享页，从服务端详情提取 `novel_book_id` 后再请求接口；百度正文使用 PyCryptodome AES-128-CBC 解密。小米搜索使用 `dushu.xiaomi.com/store/v0/lib/query/onebox`，详情、目录和正文使用对应异步接口并复用进程内设备参数，兼容旧 `reader.browser.miui.com` 分享链接；目录或正文缺章时停止合成，不生成部分 TXT。
- 七猫小说只识别直接发送的七猫链接和七猫 JSON 分享卡片，不保留 `七猫搜索`、`七猫小说`、`七猫下载`、`七猫小说下载` 等文本命令。支持 `https://www.qimao.com/shuku/数字/`、`app-share.wtzw.com/.../article-detail/数字`、`app-share.wtzw.com/.../short-story-detail/数字` 或七猫 JSON 分享卡片，识别后下载 txt 发送到 QQ。下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，不要发送封面图片。七猫小说文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 文件顶部必须写入免责声明，全部写入 `功能文件/下载缓存/` 后优先通过 AstrBot `File` 组件发送，无法使用时再回退 OneBot 本地路径上传。文件发送成功后不要再额外发送下载完成或文件已发送提示，只有发送失败时回复错误原因。
- 数字/卡片撤回白名单必须包含七猫域名 `qimao.com` 和 `app-share.wtzw.com`，七猫分享卡片不能被 JSON 卡片撤回逻辑撤回。
- 七猫小说下载必须优先使用 App 批量正文接口，默认每批 50 章、最多 200 个批次异步并发；短篇 `short-story-detail` 必须走短篇详情、`reader_type=4` 目录和带 `reader_agent=1` 的正文/批量正文接口。七猫小说下载必须记录受控日志：开始下载、章节进度、章节完成汇总、发送方式、缓存文件删除结果；章节进度日志不能逐章刷屏，应按约 10% 分段输出。
- 七猫小说生成的 txt 只可写入本地缓存供当前主网盘分享和百度后台备份，禁止通过 AstrBot `File` 组件、OneBot `upload_group_file` 或 `upload_private_file` 发送给用户。完成消息统一由小说网盘路由发送链接按钮；当前主网盘未配置或上传失败时仅回复固定文件发送失败提示，缓存必须清理。
- 书旗小说功能放在 `功能文件/管理功能/小说功能/小说/书旗小说.py`；书旗搜索、联想、链接识别和下载逻辑都保留在该模块，`找书.py` 只调用其公开搜索入口并负责聚合排序。支持 `https://www.shuqi.com/book/数字.html`、`https://t.shuqi.com/catalog/数字/`、`https://t.shuqi.com/cover/数字`、`https://t.shuqi.com/shortNovel/reader/数字` 和 `http(s)://d.shuqi.com/短码`。下载使用 `https://ocean.shuqireader.com/api/bcspub/iosapi/book/chapterlist`，固定 `user_id=8000000`，按 `bookId + timestamp + user_id + 37e81a9d8f02596e1b895d07c171d5c9` 生成 MD5 签名；正文按目录返回的 `freeContUrlPrefix` 和章节后缀逐章请求，使用书旗字符变换与 Base64 解码。目录不完整、正文缺章、响应异常或解密失败时停止，不生成部分 TXT，不写失败占位；不得保留自动 UID、书评 VIP、`downloadbatch/index`、`freedownurl`、M9 或 SQB/SQC 旧逻辑。文件名、免责声明、本地缓存、当前主网盘分享和百度后台备份流程与其他小说一致。
- 追书小说功能放在 `功能文件/管理功能/小说功能/小说/追书小说.py`；支持 `m.zhuishushenqi.com/books/书籍ID`、`zhuishushenqi.com` 和 `zhuishuvip.com` 分享链接及找书结果。下载按追书神器游客登录流程建立会话，复用异步 HTTP 连接并发请求逐章密钥和 `chapter2` 正文，使用 PyCryptodome AES-GCM/AES-CBC 解密；目录、密钥或正文缺失时停止，不生成部分 TXT，不写失败占位。所有请求只使用公开接口返回的会话密钥，文件名、免责声明、本地缓存、当前主网盘分享和百度后台备份流程与其他小说一致。
- 晋江小说功能放在 `功能文件/管理功能/小说功能/小说/晋江小说.py`；只保留晋江公开搜索、详情、目录和正文接口，使用 `okhttp/4.9.0` 请求头；只请求目录中 `isvip=0` 或明确免费条件且未锁定的章节，付费/锁定章节不请求，正文明文清理后按目录顺序合成；复用同一个异步 HTTP 会话并发下载，缺少任意公开章节时停止，不生成部分 TXT，统一交给小说网盘出口。
- 点众小说功能放在 `功能文件/管理功能/小说功能/小说/点众小说.py`，全程只使用点众 App 的初始化、详情、目录、单章正文和广告解锁接口。目录按固定窗口使用空 `currentChapterId` 并发获取，正文按章节数动态分配独立 App 身份与独立 HTTP Session，最高 60 路；每路串行执行正文请求、广告解锁和正文复取，禁止让多个正文任务共享同一身份并发解锁。首轮后只对缺失章节使用新身份重试，最多 3 轮；目录或正文缺少任意章节时停止，不生成部分 TXT。下载日志只在开始、约 10% 分段进度和完成时使用 INFO，单章与重试细节使用 DEBUG。
- 得间小说功能放在 `功能文件/管理功能/小说功能/小说/得间小说.py`，正文完全按 `D:\新建文件夹\source-code-master\source-code-master\得间\得间.py` 的单章流程执行：`batchDownloadChapteres` 只用于取得章节地址清单，正文逐章定位地址、申请单章授权、下载并解密；每章独立生成参考源码所需的会话参数，同章重试复用该组参数。下载仅复用同一个异步 HTTP 会话和连接池，每章按参考逻辑重新取得章节清单，按章节数动态调度最高 500 路并发，删除 7 章正文地址预取；得间自定义 CTR 协议使用仓库内 `PyCryptodome`/`gmpy2` 库加速，不能替换为标准 AES；缺少任意正文时停止合成，不生成部分 TXT。
- 盐言小说功能放在 `功能文件/管理功能/小说功能/小说/盐言小说.py`，只识别 `story.zhihu.com/manuscript/paid_column/专栏ID/章节ID` 分享链接和卡片；一条分享链接只处理当前章节，不请求整本专栏目录、相邻章节或其他章节。`https://api.zhihu.com/km-indep-home/manuscript/{专栏ID}/{章节ID}/header` 只用于补充当前章节标题和作者；正文提取服务只使用一次对应的 `q=专栏ID&a=章节ID` 请求。单章失败只重试同一章节，不保留旧的本地恢复客户端；正文标题必须与详情标题一致，成功后单章合成 TXT。文件缓存、当前主分享网盘和百度后台备份流程与其他小说一致。
- QQ阅读功能放在 `功能文件/管理功能/小说功能/小说/QQ阅读.py`；以 `D:\Tencent\Downloads\qqread\qqread` 的动态网关签名、正文包章节 ID/UUID 关联和 QTEB/EPUB 解析为参考，保留当前 CONFIG、csigs、动态密钥池、libfock 解密和异步 Fetcher。只允许增加 AstrBot 链接识别、Cookie 必要字段解析与数据库登录态、App 详情/目录、完整性校验、TXT、本地缓存、小说主网盘分享和百度后台备份适配。只识别 `reader.qq.com` 与 `book.qq.com` 的书籍链接，不识别 QQ 浏览器 `novel.html5.qq.com`；直接粘贴 Cookie 时支持普通 Cookie、`ywkey: 值` / `ywguid: 值` 两行文本、Cookie 头、curl、Cookie Editor JSON 和 cookies.txt，只保存 `ywguid/ywkey`，不保存 Cookie 原文中的其他字段。登录态只写 MySQL `mantou_runtime_state`，禁止新增本地账号池、HAR、Cookie 或密钥缓存文件；动态密钥池只留在当前进程。`/sk`、目录和正文请求按最终参数生成动态网关签名，出版书 `/auth` 保持已验证的 App 登录包格式；普通正文每批 200 章、最多 100 路并发，每个批量范围只请求一次；请求使用复用的异步 HTTP 会话，正文包解密使用 PyCryptodome 的 AES/DES/CTR 实现并通过受限 CPU 队列执行；空正文、解密失败或响应缺失章节按连续最多 31 章窗口、最多 100 路并发重试，最多 3 轮，某轮没有恢复章节时立即停止；普通目录返回 HTTP 400 且详情存在章节数时切换出版资源目录；`site=4` 出版书目录必须同时识别 `ctebchaptercosurl`、`epubPureUrl` 和 `epubResourceUrl`，一次获取授权密码后并发解包，EPUB 多正文文件按 OPF spine 顺序合并；下载日志只在开始、约 10% 分段输出 INFO，不逐章刷屏，重试和统计细节使用 DEBUG；下载前提示、文件名、免责声明、本地缓存和 TXT 头部格式与其他小说功能一致；缺少任意章节时停止合成，不写失败占位。
- `shing-yu/7mao-novel-downloader` 已归档且 README 说明 4.0+ 开源部分为 AGPL-3.0、核心模块部分代码私有；本仓库不要直接复制其私有下载逻辑，七猫功能应以本地自测接口结果维护。
- 番茄小说主功能放在 `功能文件/管理功能/小说功能/小说/番茄小说.py`，`main.py` 只调用 `获取番茄小说回复流`。该模块只接入本地番茄畅听详情、目录和 App 正文下载链路，不接入音频、媒体、探测、命令行调试、外部备用下载源或下载切换命令；支持普通书籍和 `short-story-share` 一章短篇。
- 番茄小说下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，不发封面图片；只接受接口返回的原始整数总字数字段并显示为精确整数，例如 `2,760,000字`。当前畅听副本缺少字段时，可用书名、作者完全一致且章节数相差不超过 1 的规范记录补查，补查记录必须再经详情接口确认；无法核验时显示“未知”，不得从正文或“万字”展示值估算。正文使用批量接口按章节数动态分批，单批最多 1500 章、最多 5 个动态并发，并复用 HTTP/2 异步会话；完成后按目录顺序合并。畅听正文源返回缺章时，使用本地阅读 App 正文源补拉；最终仍缺章则停止下载，不写失败占位；下载日志只在开始、约 10% 分段进度和完成时使用 INFO，不逐章刷屏。
- 所有小说下载、正文和文件发送失败只向 QQ 用户回复固定短提示，不展示下载源名称、响应原文、IP、URL、账号、Cookie、密钥或请求参数；完整原因仅写受控后端日志。
- 番茄小说文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 顶部写入免责声明并使用 CRLF 换行。生成的 txt 只写入 `功能文件/下载缓存/` 供当前主网盘分享和百度后台备份；上传成功后发送统一“点击打开”链接按钮，当前主网盘未配置或失败时只回复固定文件发送失败提示。
- 数字撤回规则：消息中出现独立连续 9 到 12 位数字就触发。
- `你好1078887813` 和 `你好A1078887813 你好` 应触发。
- `107888A7813`、`1234567890123`、`123 456 789` 不应触发。
- 包含明文链接、URL 编码链接或常见域名路径的链接消息不应触发数字撤回。
- 普通 `@人` 消息不应触发数字撤回或群名片撤回；当前不提供 `@用户 踢` / `@用户 踢了` 管理员踢人入口。
- 修改群管功能后必须至少运行语法检查：`python -m py_compile main.py '功能文件\管理功能\群聊功能\群管功能.py'`。
- 书旗正文性能约定：继续使用 iOS 单章正文链路，复用单个异步 HTTP 会话，最多 400 路动态请求并发；字符变换和标准库 Base64 解码通过受限解码执行器运行，不恢复旧批量包、M9 或 SQB/SQC 逻辑。
