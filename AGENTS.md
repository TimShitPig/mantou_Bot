# 项目固定规则

以后修改这个仓库前先读本文件。

## 版本同步清单

每次更新插件版本时，必须同步修改这些位置：

- `main.py` 里的 `@register(..., "版本号")`
- `metadata.yaml` 里的 `version`
- `README.md` 项目信息表里的版本
- `CHANGELOG.md` 顶部新增对应版本更新记录

版本检查命令：

- `rg -n "1\.[0-9]+\.[0-9]+|v1\.[0-9]+\.[0-9]+" main.py metadata.yaml README.md CHANGELOG.md`

版本递增规则：

- 新增功能时中版本号递增，小版本号归零，例如 `v1.5.52` 后新增功能应为 `v1.6.0`。
- 修复 bug、兼容性调整、文案调整和内部重构时只递增小版本号。
- 大版本号只在出现破坏性变更、配置不兼容或功能边界明显重构时递增。

## 收尾流程

每次运行代码检查或修改代码后，必须执行：

- 删除仓库内生成的 `.pyc` 文件和空的 `__pycache__` 目录。
- 运行 `git status --short` 确认变更。
- 提交 Git，提交信息使用中文，格式为：`更新版本号至 vX.Y.Z，变更说明`。
- 如果没有版本升级，提交信息使用：`更新功能，变更说明`。
- 删除明显无用的旧代码，避免保留历史撤回、重复文本清理等不再需要的逻辑。
- `main.py` 只用来调用：只保留插件注册、模块加载和功能分发，不写具体功能实现、消息解析或业务判断。
- 管理功能按分类放到 `功能文件/管理功能/基础功能/`、`功能文件/管理功能/小说功能/`、`功能文件/管理功能/网盘功能/` 和 `功能文件/管理功能/群聊功能/` 内，API 请求逻辑放到 `功能文件/API功能/` 内；OIAPI 接口放到 `功能文件/API功能/OIAPI/`，析API 备用接口放到 `功能文件/API功能/析API/`，崩溃API 接口放到 `功能文件/API功能/崩溃API/`。
- 群管功能放在 `功能文件/管理功能/群聊功能/群管功能.py`；数字撤回判断只能使用可见文本或 text 消息段，禁止把事件对象、非文本消息段或 ID 字段 `str()` 后参与数字匹配。
- 插件配置必须使用 AstrBot `_conf_schema.json`；新增配置时同步说明 README、CHANGELOG 和 AGENTS。运行时状态可以写入 `功能文件/下载缓存/`，但不能伪装成插件配置项。

## 排错原则

- 不要只按猜测下结论。遇到 OneBot、AstrBot、适配器消息段等外部协议细节不确定时，必须先联网查官方文档或上游资料，再改代码。
- 如果不知道如何做，或者用户给的消息样本不足以确定消息段结构，必须先联网搜索官方文档、上游源码或适配器资料。
- 日志行号和版本号必须作为判断依据。如果用户日志里的行号与当前源码不一致，优先怀疑 AstrBot 实际运行的插件目录没有同步或仍在加载旧代码。
- 排查加载问题时，应临时让运行日志输出当前插件版本和源码路径，避免误以为 GitHub 最新代码已经被 AstrBot 加载。
- 排查未知消息类型时，应先加受限诊断日志查看真实参数，包括 `message_str`、`raw_message`、消息段 type/data、对象字符串和 message_id，再按日志改规则；卡片类撤回应保留可定位真实消息段的诊断日志。
- OneBot 11 普通 `@人` 是 `at` 消息段或 `[CQ:at,qq=...]`，不是群名片；普通 `@人` 必须优先放行，不能进入数字撤回或群名片撤回。

## AstrBot 重载排查

- AstrBot 官方文档说明：代码修改后，在 WebUI 插件管理里点击 `管理` -> `重载插件`。
- 重载插件只重新加载 AstrBot 运行目录里的当前磁盘文件，不等于自动 `git pull` GitHub 最新代码。
- 本插件使用 `功能文件...` 这类顶层导入；AstrBot 重载主插件时，Python 可能仍缓存这些子模块。`main.py` 必须显式 `importlib.invalidate_caches()` 并 `reload()` 子模块。
- 如果 GitHub 已更新但日志仍显示旧版本或旧行号，必须先进入 AstrBot 实际插件目录执行 `git pull`，再删除 `.pyc` 和空 `__pycache__`，最后重载插件。
- 正确加载最新版时，日志必须出现 `Plugin 馒头bot (vX.Y.Z)`；如果仍有缓存疑问，再临时加入启动日志确认版本号和源码路径。
- 如果 WebUI 重载后仍是旧行号，直接重启 AstrBot 进程或容器；重启前后都要确认 `/AstrBot/data/plugins/馒头bot` 下的文件内容就是最新版本。

## AstrBot 限流排查

- 日志出现 `rate_limit_check.stage`、`会话 xxx 被限流`、`根据限流策略，此会话处理将被暂停 xxx 秒` 时，优先判断为 AstrBot 核心限流，不是本插件代码限流。
- AstrBot 官方配置在 `data/cmd_config.json` 的 `platform_settings.rate_limit`，默认示例为 `time: 60`、`count: 30`、`strategy: "stall"`；`stall` 会暂停会话等待，`discard` 会丢弃超限消息。
- 官方文档说明 `data/cmd_config.json` 是默认配置 `default`，WebUI 新建的其他配置文件在 `data/config/abconf_*.json`；排查时必须确认当前平台实际使用哪个配置文件。
- 想取消或降低触发概率，应在 AstrBot 配置里调大 `count`、调小 `time`，或把 `strategy` 改成 `discard`；插件内无法清除已经由 AstrBot 核心挂起的会话。
- 如果已经触发 `stall`，通常只能等待暂停时间结束；想立即恢复应先尝试在 WebUI 禁用/启用对应会话或重载配置，仍无效再重启 AstrBot。

## 功能边界

- 保留 OIAPI 功能：`随机英文单词`、`随机一言`、`疯狂星期四`、`古诗词名句`。
- 用户激活功能放在 `功能文件/管理功能/基础功能/用户激活.py`，`main.py` 只负责加载模块并调用 `处理用户激活`、`获取未激活拦截回复`。群文件清理管理员白名单 QQ 可用 `@用户 激活`、`@用户 激活 天数`、`激活 用户QQ 天数` 激活成员，未带天数默认 30 天；已激活用户再次普通激活时不重复写入，只提示已激活；可用 `@用户 重置`、`重置 @用户` 或 `重置 用户QQ` 取消该用户激活；可用 `@用户 增加 天数`、`增加 用户QQ 天数`、`@用户 减少 天数` 或 `减少 用户QQ 天数` 在原到期时间基础上增减激活时间。群文件清理管理员白名单 QQ 可发送 `生成卡密` 默认生成 1 个 12 位卡密，或发送 `生成卡密 数量` 批量生成，单次最多 200 个；用户直接发送卡密可激活本人 30 天，卡密前后带普通文字也可识别，但只能从可见文本或 text/plain 消息段识别，且必须先确认卡密存在于 MySQL 卡密表；不存在的卡密候选不回复，不能从群名片、JSON 卡片、raw_message 或非文本消息段隐藏字段里识别；中间断开或插入字符不能拼接识别，同一卡密只能使用一次；已激活用户发送未使用卡密时不消耗卡密，只提示已激活。群文件清理管理员白名单 QQ 发送 `查询卡密` 时必须先显示 `1. 已使用`、`2. 未使用`、`0. 返回上一步`，随后同一群同一发送者发送 `1` 查询已使用卡密、发送 `2` 查询未使用卡密、发送 `0` 返回；也可直接发送 `查询卡密 使用`、`查询卡密 没使用`、`查询卡密 QQ号`、`查询卡密 @用户` 和 `查询卡密 卡密` 筛选，每页 5 条，发送 `下一页` / `下` / `上一页` / `上` 翻页，卡密列表必须在使用状态后同一行显示 `----xx天`；已有卡密查询结果时，发送 `复制` 会按当前查询条件输出全部卡密，格式和生成卡密一样一行一个，发送 `复制 数字` / `复制数字` / `复制 数字天` 只输出对应天数卡密。插件配置里的卡密同步列表和 MySQL 双向同步：删除配置列表里的卡密会删除数据库对应记录，删除数据库里的卡密会在插件同步配置时删除配置列表里的对应项，数据库新增卡密会刷新新增到配置列表，在配置列表按格式新增卡密会写入数据库。普通用户可发送 `查询` 查询自己的 QQ 号、卡密号、激活天数、剩余时间、激活时间和结束时间；管理员可用 `@用户 查询`、`查询 @用户`、`查询 用户QQ` 或 `@机器人 查询 用户QQ` 查询其他用户；管理员可用 `查询用户` 查看当前群已激活用户，每页 10 条，发送 `下一页` / `下` / `上一页` / `上` 翻页，列表必须按剩余时间从少到多排序，并显示 QQ 号、卡密号和剩余时间。激活、重置、增减和查询支持一次 @ 多个用户或填写多个 QQ 号批量操作，并且必须跳过机器人自己的 @。激活、重置和查询命令必须优先操作消息里被 @ 的用户，不得因为 `is_wake`、`is_atbot` 或 `is_at_or_wake_command` 把 `@用户 激活` / `@用户 重置` / `@用户 查询` 改成操作发送者本人；如果需要唤醒机器人并手写目标，可使用 `@机器人 激活 用户QQ`、`@机器人 重置 用户QQ` 或 `@机器人 查询 用户QQ`。@ 解析需兼容 `message`、`components`、`content` 组件字段、`message_obj.raw_message` 中的 `[CQ:at]` / `[At:QQ]`，以及可见文本里的 `@昵称 激活`、`@昵称(QQ) 激活`、`@昵称 重置`、`@昵称(QQ) 重置`、`@昵称 查询`、`@昵称(QQ) 查询`；激活命令触发时保留受控 `用户激活诊断` 日志。激活记录按群和用户保存。群文件清理管理员白名单 QQ 可发送 `开启付费` / `关闭付费`，也可发送 `开启收费` / `关闭收费` 控制全局收费总开关，状态写入 MySQL 内置表 `mantou_runtime_state` 的 `paid_access/global`；`关闭付费` 或 `关闭收费` 后，未单独设置付费开关的群聊和私聊全部免费，已单独 `开启群聊收费` 的群仍按该群开关收费，已单独 `开启私聊收费` 的私聊仍按私聊开关收费；未激活用户在免费范围内可直接使用受门禁保护功能，小说下载无限制，不消耗每日免费额度，也不显示剩余免费本数；`开启付费` 或 `开启收费` 强制群聊和私聊全部进入收费判断，不管之前保存的群聊和私聊独立收费状态是否关闭。群文件清理管理员白名单 QQ 可发送 `开启群聊收费` / `关闭群聊收费`，也可发送 `开启群聊付费` / `关闭群聊付费` 控制当前群激活门禁，默认各群开启，状态按群号写入 MySQL 内置表 `mantou_runtime_state`。未激活用户使用随机英文单词、随机一言、疯狂星期四、古诗词名句、七猫小说下载和番茄小说下载时，如果 `user_activation_daily_free_quota` 为正整数，则同一用户 QQ 每天可免费使用对应次数，不按群区分，换群不会重置次数；小说下载开始提示必须在 `正在下载中请稍等.....` 下方显示剩余免费本数，最后一本显示 `这是你今天的最后一本了` 和 `可以购买VIP进行无限下载`，次数用完后回复 `您今日的x本免费次数已经下载完了哦`、`如果要再下载可以购买VIP请看群公告` 和 `或者等第二天继续免费下载`；配置为 `0` 或留空时不开放免费额度。管理员不受激活门禁和免费额度限制。
- 用户激活、卡密、每日免费额度、按群保存的付费开关状态、私聊付费开关状态、番茄小说 API 当前选择和小说功能开关状态只使用 MySQL 存储，不再使用本地 JSON；连接信息来自 `_conf_schema.json` 的 `database_settings` 分类里的 `user_activation_database_host`、`user_activation_database_port`、`user_activation_database_user` 和 `user_activation_database_password` 配置，数据库名默认等于 `user_activation_database_user`。数据库配置必须放在 `_conf_schema.json` 最下面，不再暴露单独的数据库名配置项；代码必须兼容读取旧配置里残留的平铺字段和 `user_activation_database_name`。表名在代码内置固定为 `mantou_user_activation`、`mantou_user_activation_cards`、`mantou_user_free_quota` 和 `mantou_runtime_state`，不要暴露成配置项。数据库未配置完整或依赖缺失时，未激活用户不能持久消耗免费额度，应提示未激活；番茄小说 API 当前选择、付费开关和小说功能开关读取失败时只使用默认值，写入失败时必须明确回复错误。插件配置里的 `card_key_sync_used_cards` 和 `card_key_sync_unused_cards` 是 MySQL 卡密双向同步列表，不是本地卡密存储；同步使用 `mantou_runtime_state` 保存上次同步快照，用于区分配置变更和数据库变更。
- 群文件清理管理员白名单 QQ 可发送 `开启私聊收费` / `关闭私聊收费`，也可发送 `开启私聊付费` / `关闭私聊付费` 控制私聊激活门禁；默认开启，状态写入 MySQL 内置表 `mantou_runtime_state` 的 `paid_access/private`，不会影响任何群聊。私聊和群聊的下载入口必须共用 `获取未激活拦截回复` 与 `获取下载免费额度提示`；全局收费关闭时，群聊和私聊独立开关已设置则按独立开关判断，未设置则免费；全局收费开启时强制群聊和私聊全部进入收费判断；未设置全局总开关时群聊读取当前群付费开关，私聊读取私聊付费开关。
- 群管功能保留数字撤回逻辑：命中自动撤回规则后，必须先判断发送者 QQ 群身份，QQ群主和管理员发送的消息不撤回、不撤回最近消息、不累计三次踢出计数；只要能拿到数字群号和 QQ，就必须通过 OneBot `get_group_member_info` 查询真实 QQ 群成员角色，并以查询结果为准，不能因为事件里是 `member` 就跳过查询，也不能把插件配置管理员当作 QQ 群管理员；只有查不到接口、缺少数字群号或缺少 QQ 时，才使用事件或 sender 里的 `role` 兜底；`owner` 和 `admin` 都视为保护身份。普通成员先撤回当前触发消息，撤回成功后通过 OneBot `get_group_msg_history` 拉取最近 100 条群历史，筛选该用户最近消息并逐条调用 `delete_msg` 撤回，默认最多撤回 8 条；不要写臆造批量撤回接口。同一成员在同一个群里被数字撤回模块成功撤回 3 次后，调用 OneBot 标准 `set_group_kick` 踢出该成员；当前群踢出成功后，通过 `get_group_list` 获取机器人所在群，再用 `get_group_member_info` 检查该用户是否在其它群，存在则一并踢出；数字、群名片/JSON 卡片、合并转发和 QQ 闪传等本模块处理的撤回类型都计数。
- 群名片/群分享/JSON 卡片消息需要撤回当前消息，包括 `[ComponentType.Json]`、组件对象字符串中的 `ComponentType.Json`，以及 QQ 官方机器人空间相册分享等卡片降级展示文本 `暂不能查看该消息内容`。
- 白名单域名 `changdunovel.com`、`fanqienovel.com`、`fqnovel.com`、`novelfm.com` 不撤回；白名单判断必须优先于 JSON 卡片、合并转发、闪传和数字撤回判断。
- 合并转发/聊天记录消息需要撤回当前消息，包括 OneBot `forward`/`node` 和 AstrBot `ComponentType.Forward`、`ComponentType.Node`、`ComponentType.Nodes`。
- QQ 闪传消息需要撤回当前消息，只识别展示文本 `QQ闪传` 和 aiocqhttp 显示文本 `该消息类型暂不支持查看`；普通文件、OneBot `file`、`[CQ:file,...]`、AstrBot `ComponentType.File` 不应撤回。
- OneBot 11 标准撤回接口是单条 `delete_msg`，没有标准批量撤回接口；除非确认适配器支持扩展接口，否则只循环单条撤回，不写臆造的批量接口。
- 状态功能放在 `功能文件/管理功能/基础功能/状态功能.py`，`main.py` 只调用 `处理状态指令`。群文件清理管理员白名单 QQ 可发送 `状态` 查看系统信息、运行内存、运行 CPU、系统内存、系统 CPU、系统运行时间、插件运行时间、插件版本、番茄/七猫开关、当前番茄 API、当前群收费、私聊收费、每日免费额度、数据库配置状态和 UC/百度网盘启用状态；状态命令不能展示 OIAPI key、网盘 Cookie 等敏感原文。
- 帮助功能放在 `功能文件/管理功能/基础功能/帮助功能.py`，`main.py` 只调用 `处理帮助指令`。群文件清理管理员白名单 QQ 可发送 `帮助` 查看 `主动触发` / `被动触发` 大类，随后 120 秒内发送数字进入该大类的小类列表（API功能/管理功能/激活功能 等），再发送数字进入小类的触发项列表，再发送数字进入触发项详情；发送 `0` 返回上一层，也可直接发送 `帮助 数字` 直接进入指定大类的小类列表。帮助等待状态必须按会话和发送者 QQ 隔离，普通成员不能触发帮助功能。QQ 官方机器人适配 Markdown + Keyboard 按钮消息：官方机器人发送帮助时自动使用 Markdown 格式渲染，并附带可点击的按钮（大类选择、小类选择、触发项选择、返回上一步，每行 2 个按钮）；有快捷命令的触发项（全员禁言、付费开关、小说开关、群文件清理、番茄小说API切换）按钮点击后发送触发项名称进入详情页，其余触发项按钮点击发送数字；返回按钮点击发送 `返回上一步`，通过 `api._http.request` 直接调用 QQ 官方 API 发送 `msg_type=2` 的 Markdown + Keyboard 组合消息；发送失败时自动降级为纯文本。普通机器人保持原有纯文本格式不变。帮助菜单触发项详情页可配置 `快捷命令` 字段，配置后会显示对应的快捷命令按钮，点击即发送对应命令（私聊自动发送，群聊填入输入框，这是 QQ 官方平台限制）；已配置快捷命令按钮的触发项包括全员禁言、群文件清理、小说开关、付费开关、番茄小说API切换。
- 群文件清理、全部群文件清理、授权链接、帮助、状态、用户激活、生成卡密、查询卡密、卡密查询结果里的 `复制`、`查看API`、`1` / `2` / `3` / `4` / `5` API 切换、`开启番茄` / `关闭番茄`、`开启七猫` / `关闭七猫`、`开启付费` / `关闭付费`、`开启收费` / `关闭收费`、`开启群聊收费` / `关闭群聊收费`、`开启私聊收费` / `关闭私聊收费`、`@用户 踢` / `@用户 踢了`、`开启禁言`、`关闭禁言`、`开启全部禁言`、`关闭全部禁言`、`添加群聊`、`删除群聊`、`查看群聊`、`更改备注` 只能由插件配置 `group_file_cleanup_admin_qq` 里的 QQ 使用；这是群文件清理管理员白名单，不等同于群管理员。`@用户 踢` / `@用户 踢了` 支持一次 @ 多个用户批量踢出，并且必须跳过机器人自己的 @；当前群踢出成功后，通过 `get_group_list` 获取机器人所在数字 QQ 群，再用 `get_group_member_info` 检查该用户是否在其它群，存在则一并踢出；发送 `@用户 踢` 或 `@用户 踢了` 时，还会通过 `get_group_msg_history` 拉取目标用户最近消息并逐条调用 `delete_msg` 撤回，最多撤回 50 条，多个 @ 用户逐个处理，50 条按每个目标用户单独计算，不是所有用户合计。`开启禁言` / `关闭禁言` 调用 OneBot `set_group_whole_ban` 开关当前群全员禁言；`开启全部禁言` / `关闭全部禁言` 先调用 `get_group_list` 获取机器人所在数字 QQ 群，再逐群开关全员禁言。
- 小说功能开关放在 `功能文件/管理功能/小说功能/小说功能开关.py`，`main.py` 只调用 `处理小说功能开关指令`、`小说功能是否开启` 和 `获取小说功能关闭回复`。群文件清理管理员白名单 QQ 可发送 `开启番茄` / `关闭番茄` 和 `开启七猫` / `关闭七猫` 开关对应下载功能；状态写入 MySQL 内置表 `mantou_runtime_state`，不再写入 `功能文件/下载缓存/小说功能开关.json`；关闭后识别到对应小说链接时只回复功能已关闭，不进入下载流程。
- UC 网盘自动上传功能放在 `功能文件/管理功能/网盘功能/UC网盘.py`，`main.py` 只负责加载模块。插件配置使用 `_conf_schema.json` 的 `uc_pan_settings` 分类，包含 `uc_pan_cookie` 和 `uc_pan_upload_dir`，该分类必须放在 `database_settings` 上面，数据库配置继续保持最下面。`uc_pan_cookie` 由用户在插件配置手动填写浏览器 Cookie，留空表示不启用；`uc_pan_upload_dir` 默认 `/小说机器人`，上传时自动逐级创建目录。番茄和七猫小说下载完成后仍先生成完整原小说 txt；如果 UC 网盘启用，上传前会分页扫描并删除目标目录下与原小说文件名完全相同的远端普通文件，不删除同名文件夹；目录列表里的文件夹判断必须兼容 `dir=1`、`dir=true` 和布尔 `True`，创建上传目录 `/file` 返回同名冲突时应重新查找已有目录继续上传；先尝试用原小说文件名上传，遇到 `23008`、`同名冲突`、`file is doloading` 或 `file is downloading` 时重新扫描并删除同名旧文件，然后继续使用原小说文件名有限重试，不得改用备用远端文件名；如果上传完成接口返回这类处理中错误，应复用当前预上传文件 ID 继续创建分享，对齐旧项目 UC 客户端行为，不要立即创建新的上传任务或回退源文件；创建分享接口仍返回处理中时必须等待重试。上传成功时，QQ 发送的文件名必须和原小说文件名完全一致，但文件内容只能是 `请复制链接打开UC网盘` 和 `链接：xxx`；不要把分享链接直接作为群消息发送。UC 上传失败时必须回退发送完整原小说 txt，并记录受控日志，HTTP 错误日志需保留接口路径用于排查真实失败点。UC OSS 单分片 PUT 响应没有返回 `ETag` 时，可以使用本地文件 MD5 作为单分片 ETag 继续完成上传，不要直接判定失败。UC 网盘只服务小说下载后的自动上传，不新增管理员手动上传、删除、创建文件夹或分享命令。
- 百度网盘后台上传功能放在 `功能文件/管理功能/网盘功能/百度网盘.py`，`main.py` 只负责加载模块。插件配置使用 `_conf_schema.json` 的 `baidu_pan_settings` 分类，包含 `baidu_pan_cookie`、`baidu_pan_upload_dir` 和 `baidu_pan_upload_status`，该分类必须放在 `database_settings` 上面。`baidu_pan_cookie` 由用户在插件配置手动填写浏览器 Cookie，留空表示不启用；`baidu_pan_upload_dir` 默认 `/小说机器人`，上传时自动逐级创建目录；`baidu_pan_upload_status` 必须在配置界面提供 `完结`、`连载`、`全部` 三个可选项，默认只上传完结。番茄和七猫小说下载完成后仍先生成完整原小说 txt；如果百度网盘启用，会按状态规则后台上传原小说 txt；完结小说在目标目录已有同名普通文件时必须跳过重复上传，连载小说仍删除目标目录下同名普通文件后重新上传，不删除同名文件夹。百度网盘只做后台备份，不创建分享链接、不生成链接 txt、不改变 QQ 原有发送逻辑；如果 UC 网盘也启用，必须先按 UC 逻辑决定 QQ 发送内容，再上传原小说到百度网盘。百度上传失败只记录受控 warning，不回复用户，不影响 QQ 文件发送。
- 群文件清理统一由网页接口版 `功能文件/管理功能/群聊功能/网页群文件.py` 承担，不再保留适配器扩展接口版 `群文件清理.py`；`main.py` 只加载 `网页群文件功能` 并调用 `处理网页群文件清理`，该入口统一处理清理指令、清理选择数字回复与群聊管理指令。指令（仅群文件清理管理员白名单 QQ 可用）：`登录群文件`、`群文件登录cookie <cookie>`、`清理群文件` / `群文件清理`、`清理全部群文件`、`添加群聊 群号 [群号...]`、`删除群聊 群号 [群号...]`、`查看群聊`、`更改备注`、`更改备注 群号`。Cookie 通过 `群文件登录cookie` 指令保存，按管理员 QQ 隔离，31 天有效，过期自动失效；Cookie 存 MySQL 内置表 `mantou_runtime_state`（namespace=`web_group_file_cookie`，state_key=管理员QQ，state_value 为 JSON `{cookie, expire}`），不再写本地 JSON 文件，读取时检查 expire 过期自动清空对应记录；不暴露到 `状态` 命令。
- 网页群文件清理请求 `pan.qun.qq.com/cgi-bin/group_file/get_file_list` 和 `delete_file`，从 Cookie 提取 `skey` 后用 hash 算法计算 `bkn` 鉴权；文件列表每页 50 个并发拉取，删除每批 20 个并发提交，失败自动重试 3 次。当前实现只删根目录文件，不递归子文件夹；遇到 `ec != 0` 或 HTTP 非 200 时按接口错误提示重新登录，不要用臆造接口绕过鉴权。核心清理逻辑抽到 `执行网页清理`，`清空指定群文件` 和 `清理全部已添加群` 复用，后者复用一个 aiohttp session 逐群清理并汇总成功/失败。
- `清理群文件` / `群文件清理` 列出「添加群聊」里的目标群列表后，同一会话同一发送者 120 秒内发送编号清理对应群，发送 `0` 取消；等待状态按会话和发送者 QQ 隔离（群聊用群号+发送者，私聊用发送者），过期自动失效；清理选择数字回复在 `处理网页群文件清理` 入口最前面判断，未命中等待状态时不拦截数字；列表为空时必须回复提示先发送 `添加群聊 群号` 添加目标群。`清理全部群文件` 先提示 `正在清理群文件（全部）`，再读取「添加群聊」添加的目标群列表逐群走网页接口清理。清理不再依赖机器人是群主/管理员，改用管理员 Cookie 鉴权；未登录 Cookie 时必须提示先发送 `登录群文件`。
- 群聊管理（`添加群聊` / `删除群聊` / `查看群聊` 及 MySQL 读写）合并在 `网页群文件.py` 内；待清理群列表写入 MySQL 内置表 `mantou_runtime_state`（namespace=`cleanup_groups`，state_key=`group_list`，state_value 为 JSON 数组），重载后继续生效；`清理全部已添加群` 通过 `读取待清理群列表` 读取该列表。`查看群聊` 在 QQ 官方机器人下用 Markdown + Keyboard 按钮交互：底部显示「删除群聊」按钮，点击发送 `删除群聊`（无参数）进入删除模式，显示每个群号按钮（点击发送 `删除群聊 群号` 直接删除并刷新列表保持删除模式）和底部「返回上一步」按钮（点击发送 `关闭删除群聊` 退出删除模式回到普通查看列表）；`清理群文件` / `群文件清理` 列出待清理群列表时也显示群号按钮（点击发送编号，命中清理选择等待状态后清理对应群）和取消按钮（点击发送 `0` 取消）；群号按钮每行 2 个，加返回/取消按钮超过 5 行自动降级为无按钮 markdown；所有按钮点击直接发送（`enter: True`），群聊和私聊一致；普通机器人仍为纯文本列表 + 提示，无按钮。`删除群聊`（无参数）和 `关闭删除群聊` 仅群文件清理管理员白名单 QQ 可用。`更改备注`（无参数）列出群号按钮选择要修改备注的群，点群号按钮进入 60 秒等待状态，下一条消息作为该群备注保存（namespace=`cleanup_groups`，state_key=`remark:{群号}`），超时自动取消；备注在 `查看群聊`、`清理群文件` 和 `删除群聊` 列表中显示；删除群聊时一并清空对应备注；`更改备注`、`更改备注 群号` 仅群文件清理管理员白名单 QQ 可用。所有指令类按钮（删除群聊、关闭删除群聊、更改备注、群号按钮、取消按钮、返回上一步）必须显式设置 `data为标签=False`，按钮 `data` 字段发送指令文本而非标签文本，否则点击「返回上一步」「取消」等按钮会发送标签文字导致命令无法识别。
- 授权链接功能放在 `功能文件/管理功能/群聊功能/授权链接.py`，`main.py` 只调用 `处理授权链接`。触发指令固定为 `授权`、`授权 数字群号` 或 `授权 数字群号 机器人QQ`，仅群文件清理管理员白名单 QQ 可用；后两者用于适配器取不到数字 `groupCode` 或数字 `botUin` 时手动指定。只生成 `https://club.vip.qq.com/transfer?open_kuikly_info=...`，JSON 参数必须包含 `page_name=ai_group_service_agreement_pop_page`、数字 `groupCode`、数字 `botUin`、字符串 `botUid`、`screen=1`，再整体 URL 编码。
- 授权链接的 `groupCode` 和 `botUin` 必须优先从当前事件、AstrBot `context.robot_id`、事件 JSON、消息段 JSON、URL 编码 JSON 和 bot API 动态获取；`botUid` 必须优先从事件 JSON 或通过 `getUidFromUin`、适配器等价扩展接口实时转换，转换返回可能是字符串、纯数字 UID、字典、列表或 QQ 到 UID 映射。取不到 `botUid` 时应明确提示失败，不要生成缺参链接或硬编码 UID。授权命令触发时应保留受控诊断日志 `授权链接事件诊断`，UID 转换接口有返回但无法识别时应保留 `授权链接UID转换响应未识别`，便于按真实响应补规则；授权链接只支持群主在安卓/鸿蒙 QQ 9.2.90 及以上打开，iOS 暂不支持。
- QQ 官方机器人 `qq_official` 发送文件不能使用 OneBot 的 `upload_group_file`/`upload_private_file` 作为主路径；应优先使用 AstrBot `File` 组件，它会走 QQ 官方富媒体接口 `/v2/users/{openid}/files` 或 `/v2/groups/{group_openid}/files`，再发送 `msg_type=7` 的 `media` 消息。如果 QQ 官方群聊返回 `call inner proxy error`，这是平台富媒体上传限制或临时错误，不要用臆造 OneBot 接口修复。
- 通过 AstrBot `File` 组件发送本地缓存文件时，不能在 yield 后立即删除缓存；应延迟清理，避免 qq_official/其他适配器在响应阶段读取文件前缓存已不存在。OneBot 回退接口调用完成后可以立即删除缓存。
- 七猫小说功能只允许放在 `功能文件/管理功能/小说功能/七猫小说.py` 一个文件内，不拆分多个 py 文件；`main.py` 只负责加载模块并调用 `获取七猫小说回复流`。
- 七猫小说只识别直接发送的七猫链接和七猫 JSON 分享卡片，不保留 `七猫搜索`、`七猫小说`、`七猫下载`、`七猫小说下载` 等文本命令。支持 `https://www.qimao.com/shuku/数字/`、`app-share.wtzw.com/.../article-detail/数字`、`app-share.wtzw.com/.../short-story-detail/数字` 或七猫 JSON 分享卡片，识别后下载 txt 发送到 QQ。下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，不要发送封面图片。七猫小说文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 文件顶部必须写入免责声明，全部写入 `功能文件/下载缓存/` 后优先通过 AstrBot `File` 组件发送，无法使用时再回退 OneBot 本地路径上传。文件发送成功后不要再额外发送下载完成或文件已发送提示，只有发送失败时回复错误原因。
- 数字/卡片撤回白名单必须包含七猫域名 `qimao.com` 和 `app-share.wtzw.com`，七猫分享卡片不能被 JSON 卡片撤回逻辑撤回。
- 七猫小说下载必须记录受控日志：开始下载、章节进度、章节完成汇总、发送方式、缓存文件删除结果；章节进度日志不能逐章刷屏，应按约 10% 分段输出。
- 七猫小说发送文件必须全部走本地缓存文件上传：运行时自动创建 `功能文件/下载缓存/`；QQ 官方群聊直接提示平台限制，其他场景生成 txt 后优先使用 AstrBot `File` 组件；无法使用时再按裸文件路径、`file://` URI 调用 OneBot 上传接口。每个候选失败要写日志，`File` 组件路径延迟清理缓存，OneBot 回退上传尝试结束后必须删除缓存 txt。
- `shing-yu/7mao-novel-downloader` 已归档且 README 说明 4.0+ 开源部分为 AGPL-3.0、核心模块部分代码私有；本仓库不要直接复制其私有下载逻辑，七猫功能应以本地自测接口结果维护。
- 番茄小说主功能放在 `功能文件/API功能/OIAPI/番茄小说.py`，析API功能放在 `功能文件/API功能/析API/番茄小说.py`，崩溃API功能放在 `功能文件/API功能/崩溃API/番茄小说.py`，聚合API功能放在 `功能文件/API功能/聚合API/番茄小说.py`，Y6api功能放在 `功能文件/API功能/Y6api/番茄小说.py`；只识别番茄明文链接和 JSON 卡片中的链接，不做 `番茄小说 书名` 搜索命令。插件配置项名必须是 `番茄小说key`，只支持单个 key。请求 `https://oiapi.net/api/FqRead` 时必须带 `key` 和 `type=json`。
- 番茄小说 API 通过指令手动切换：群文件清理管理员白名单 QQ 发送 `查看API` 列出站点，120 秒内发送 `1` 切换 OIAPI，发送 `2` 切换析API，发送 `3` 切换崩溃API，发送 `4` 切换聚合API，发送 `5` 切换 Y6api；也可直接发送 `oiapi`/`析api`/`崩溃api`/`聚合api`/`y6api` 快速切换；旧别名不再兼容；当前选择写入 MySQL 内置表 `mantou_runtime_state`，不再写入 `功能文件/下载缓存/番茄小说API.json`。禁止 OIAPI 失败后自动切换其他API，当前接口业务失败时必须只把接口 `message` 内容回复给用户，例如 `付费内容不解析` 或 `key额度不足`，不要显示 `code`、`message=` 或 `OIAPI返回失败` 这类调试信息；OIAPI 正文批次请求只要出现用户可见业务错误、HTTP/网络异常、JSON 解析异常、章节返回不完整等任何错误，必须立即停止当前下载，不再二分拆分，也不继续请求后续批次，避免继续消耗 key 额度；唯一例外是接口返回 `请检测章节选择是否正确` 时，必须额外请求一次 OIAPI `method=chapters` 目录，并使用 OIAPI 返回的章节数量单次重试正文请求，第二次仍失败就立即停止，不得继续拆分、循环或切换接口。
- 番茄小说识别 `book_id=数字`、`fanqienovel.com/数字`、`changdunovel.com/...book_id=数字`、`changdunovel.com/t/短码`、`m.novelfm.com/s/短码` 和 15-25 位书籍 ID；短链需要先请求一次并从最终地址或页面文本提取 `book_id`。书籍详情优先请求 `https://api.fqnovel.com/novel_ug/share/landing_page`，再用 `https://fanqienovel.com/page/{book_id}` 补齐，最后用 `FqRead method=chapters` 补章节数；选择析API时，书籍详情仍使用这套官方详情流程，析API只负责 `search`、`catalog`、`content`，仅在官方详情不可用时回退 `detail`；选择崩溃API时，使用 `http://111.170.14.45:2000` 的 `/info`、`/catalog`、`/download`、`/download/progress` 和 `/download/file`，通过下载任务进度接口记录受控进度日志，下载完整 TXT 后重新整理为当前番茄 TXT 文件格式；选择聚合API时，使用 `https://modtianmingyun-fanqie1.ms.show` 到 `https://modtianmingyun-fanqie10.ms.show` 以及 `https://fanqieapi.6666633.xyz:9981` 到 `:9983` 的同格式节点，节点测速必须包含小批量正文测试，详情请求 `/api/v1/books/{book_id}`，目录请求 `/api/v1/books/{book_id}/directory`，正文请求 `/api/v1/chapters/full`，正文每批 200 章、最多 40 个动态并发混合节点下载；批次失败必须换完可用节点，并在 200 章超时后自动降级为 100/50 章重试，最终失败必须停止当前下载，不得把 `【下载失败】` 占位写入 txt，通过独立模块解析详情、目录和批量正文；选择 Y6api 时，使用 `https://y68-napi.hf.space` 的 `/api/detail`、`/api/directory` 和 `/api/download` 后端打包下载接口，下载完整 TXT 后必须解析书名、作者、简介和章节正文，再交给当前统一 TXT 构建函数输出，不直接原样发送接口文件。不引入 App 签名、Cookie、Argus/Ladon/Gorgon、状态持久化和 key 轮换逻辑。
- 番茄小说下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，不发封面图片，外部提示不回复简介，但 txt 文件头部必须保留简介。正文优先按章节范围请求 `method=chapter`，超过约 500 万字时仍按初始分段请求；任一正文批次请求失败时立即停止当前下载并回复错误，不再拆成小范围或单章，不写失败章继续合并。
- 番茄小说文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 顶部必须写入与七猫一致的免责声明。析API、聚合API和 Y6api 正文必须保留段落换行；崩溃API 和 Y6api 下载回来的完整 TXT 必须解析书名、作者、简介和章节正文，再交给当前统一 TXT 构建函数输出，不直接原样发送接口文件；txt 输出统一使用 CRLF 换行。发送文件必须走本地缓存：QQ 官方群聊直接提示平台限制，其他场景写入 `功能文件/下载缓存/` 后优先使用 AstrBot `File` 组件发送，无法使用时再尝试裸本地路径和 `file://` URI 的 OneBot 上传接口。`File` 组件路径延迟清理缓存，OneBot 回退上传尝试结束后删除缓存 txt，成功时不额外发送完成提示。
- 数字撤回规则：消息中出现独立连续 9 到 12 位数字就触发。
- `你好1078887813` 和 `你好A1078887813 你好` 应触发。
- `107888A7813`、`1234567890123`、`123 456 789` 不应触发。
- 包含明文链接、URL 编码链接或常见域名路径的链接消息不应触发数字撤回。
- 普通 `@人` 消息不应触发数字撤回或群名片撤回；只有 `@用户 踢` / `@用户 踢了` 才会触发管理员踢人。
- 修改群管功能后必须至少运行语法检查：`python -m py_compile main.py '功能文件\管理功能\群聊功能\群管功能.py'`。
