# 项目固定规则

以后修改这个仓库前先读本文件。

## 版本同步清单

每次更新插件版本时，必须同步修改这些位置：

- `main.py` 里的 `@register(..., "版本号")`
- `metadata.yaml` 里的 `version`
- `README.md` 项目信息表里的版本
- `CHANGELOG.md` 顶部新增对应版本更新记录

版本检查命令：

- `rg -n "1\.[0-9]+\.[0-9]+|v1\.[0-9]+\.[0-9]+" main.py metadata.yaml README.md CHANGELOG.md`

版本递增规则：

- 新增功能时中版本号递增，小版本号归零，例如 `v1.5.52` 后新增功能应为 `v1.6.0`。
- 修复 bug、兼容性调整、文案调整和内部重构时只递增小版本号。
- 大版本号只在出现破坏性变更、配置不兼容或功能边界明显重构时递增。

## 收尾流程

每次运行代码检查或修改代码后，必须执行：

- 删除仓库内生成的 `.pyc` 文件和空的 `__pycache__` 目录。
- 运行 `git status --short` 确认变更。
- 提交 Git，提交信息使用中文，格式为：`更新版本号至 vX.Y.Z，变更说明`。
- 如果没有版本升级，提交信息使用：`更新功能，变更说明`。
- 删除明显无用的旧代码，避免保留历史撤回、重复文本清理等不再需要的逻辑。
- `main.py` 只用来调用：只保留插件注册、模块加载和功能分发，不写具体功能实现、消息解析或业务判断。
- 管理功能按分类放到 `功能文件/管理功能/基础功能/`、`功能文件/管理功能/小说功能/`、`功能文件/管理功能/网盘功能/` 和 `功能文件/管理功能/群聊功能/` 内；小说功能内的平台下载模块放入 `小说功能/小说/`，找书、开关和缓存清理等通用模块放入 `小说功能/功能/`；`功能文件/API功能/` 已整体删除，禁止重新引入 OIAPI、析API、崩溃API、聚合API、Y6api 或对应开关、切换指令。
- 群管功能放在 `功能文件/管理功能/群聊功能/群管功能.py`；数字撤回判断只能使用可见文本或 text 消息段，禁止把事件对象、非文本消息段或 ID 字段 `str()` 后参与数字匹配。
- 插件配置必须使用 AstrBot `_conf_schema.json`；新增配置时同步说明 README、CHANGELOG 和 AGENTS。运行时状态可以写入 `功能文件/下载缓存/`，但不能伪装成插件配置项；小说 TXT 写入后必须创建 `.txt.uploading` 占用标记，只有上传和后台备份结束后随源文件清除。插件启动或重载时必须清理该目录内上次运行遗留的小说 txt，但必须跳过当前进程仍持有占用标记的上传文件。

## 排错原则

- 所有面向用户的失败、异常和远端错误回复禁止直接拼接异常对象、请求 URL、响应原文或配置值；账号、IP、Cookie、Key、Token、数据库连接信息和本地路径只允许写入后端日志。QQ 用户侧的下载、文件发送和接口切换失败只使用固定短提示，不显示具体 API 名称或接口业务内容；API 名称只允许在帮助和手动切换菜单中展示。
- 不要只按猜测下结论。遇到 OneBot、AstrBot、适配器消息段等外部协议细节不确定时，必须先联网查官方文档或上游资料，再改代码。
- 如果不知道如何做，或者用户给的消息样本不足以确定消息段结构，必须先联网搜索官方文档、上游源码或适配器资料。
- 日志行号和版本号必须作为判断依据。如果用户日志里的行号与当前源码不一致，优先怀疑 AstrBot 实际运行的插件目录没有同步或仍在加载旧代码。
- 排查加载问题时，应临时让运行日志输出当前插件版本和源码路径，避免误以为 GitHub 最新代码已经被 AstrBot 加载。
- 排查未知消息类型时，应先加受限诊断日志查看真实参数，包括 `message_str`、`raw_message`、消息段 type/data、对象字符串和 message_id，再按日志改规则；卡片类撤回应保留可定位真实消息段的诊断日志。
- OneBot 11 普通 `@人` 是 `at` 消息段或 `[CQ:at,qq=...]`，不是群名片；普通 `@人` 必须优先放行，不能进入数字撤回或群名片撤回。

## AstrBot 重载排查

- AstrBot 官方文档说明：代码修改后，在 WebUI 插件管理里点击 `管理` -> `重载插件`。
- 重载插件只重新加载 AstrBot 运行目录里的当前磁盘文件，不等于自动 `git pull` GitHub 最新代码。
- 本插件使用 `功能文件...` 这类顶层导入；AstrBot 重载主插件时，Python 可能仍缓存这些子模块。`main.py` 必须显式 `importlib.invalidate_caches()` 并 `reload()` 子模块；存在直接导入关系时必须先重载依赖模块，例如先加载 `运行状态数据库` 再加载 `状态功能`。
- 如果 GitHub 已更新但日志仍显示旧版本或旧行号，必须先进入 AstrBot 实际插件目录执行 `git pull`，再删除 `.pyc` 和空 `__pycache__`，最后重载插件。
- 正确加载最新版时，日志必须出现 `Plugin 馒头bot (vX.Y.Z)`；如果仍有缓存疑问，再临时加入启动日志确认版本号和源码路径。
- 如果 WebUI 重载后仍是旧行号，直接重启 AstrBot 进程或容器；重启前后都要确认 `/AstrBot/data/plugins/馒头bot` 下的文件内容就是最新版本。

## AstrBot 限流排查

- 日志出现 `rate_limit_check.stage`、`会话 xxx 被限流`、`根据限流策略，此会话处理将被暂停 xxx 秒` 时，优先判断为 AstrBot 核心限流，不是本插件代码限流。
- AstrBot 官方配置在 `data/cmd_config.json` 的 `platform_settings.rate_limit`，默认示例为 `time: 60`、`count: 30`、`strategy: "stall"`；`stall` 会暂停会话等待，`discard` 会丢弃超限消息。
- 官方文档说明 `data/cmd_config.json` 是默认配置 `default`，WebUI 新建的其他配置文件在 `data/config/abconf_*.json`；排查时必须确认当前平台实际使用哪个配置文件。
- 想取消或降低触发概率，应在 AstrBot 配置里调大 `count`、调小 `time`，或把 `strategy` 改成 `discard`；插件内无法清除已经由 AstrBot 核心挂起的会话。
- 如果已经触发 `stall`，通常只能等待暂停时间结束；想立即恢复应先尝试在 WebUI 禁用/启用对应会话或重载配置，仍无效再重启 AstrBot。

## 功能边界

- 不保留用户激活、卡密、收费、付费或每日免费额度功能；所有小说下载均免费。运行状态只使用 MySQL 的 `mantou_runtime_state` 表，不再创建或读取激活、卡密和免费额度表。
- 数据库连接信息来自 `_conf_schema.json` 的 `database_settings`，运行状态仅用于群文件 Cookie、QQ阅读登录态、待清理群列表和小说功能开关。未配置数据库时读取运行状态必须直接返回默认值，不得尝试连接 MySQL 或刷告警；小说下载功能默认开启，相关运行状态不持久化。QQ阅读不提供手机号、验证码或滑块登录；群文件清理管理员白名单 QQ 可直接发送含 `ywguid/ywkey` 的浏览器 Cookie，插件只保存这两个必要字段到 `mantou_runtime_state`（namespace=`qq_reader_auth`，state_key=`login_state`），新 Cookie 覆盖旧登录态，下载开始时读取最新值覆盖模块固定 `uid/usid`；固定 `fuid/qrsn` 不变，动态密钥池仅在当前进程内保存，不生成 `keypool.cache`。
- 群管功能保留数字撤回逻辑：命中自动撤回规则后，必须先判断发送者 QQ 群身份，QQ群主和管理员发送的消息不撤回、不撤回最近消息、不累计三次踢出计数；只要能拿到数字群号和 QQ，就必须通过 OneBot `get_group_member_info` 查询真实 QQ 群成员角色，并以查询结果为准，不能因为事件里是 `member` 就跳过查询，也不能把插件配置管理员当作 QQ 群管理员；只有查不到接口、缺少数字群号或缺少 QQ 时，才使用事件或 sender 里的 `role` 兜底；`owner` 和 `admin` 都视为保护身份。普通成员先撤回当前触发消息，撤回成功后通过 OneBot `get_group_msg_history` 拉取最近 100 条群历史，筛选该用户最近消息并逐条调用 `delete_msg` 撤回，默认最多撤回 8 条；不要写臆造批量撤回接口。数字撤回模块只撤回当前消息和最近消息，不再自动踢人；数字、群名片/JSON 卡片、合并转发和 QQ 闪传等本模块处理的撤回类型都计数。
- 群名片/群分享/JSON 卡片消息需要撤回当前消息，包括 `[ComponentType.Json]`、组件对象字符串中的 `ComponentType.Json`，以及 QQ 官方机器人空间相册分享等卡片降级展示文本 `暂不能查看该消息内容`。
- 白名单域名 `changdunovel.com`、`fanqienovel.com`、`fqnovel.com`、`novelfm.com`、`qimao.com`、`app-share.wtzw.com`、`shuqi.com`、`shuqireader.com`、`reader.qq.com`、`book.qq.com`、`novel.html5.qq.com` 不撤回；白名单判断必须优先于 JSON 卡片、合并转发、闪传和数字撤回判断。
- 合并转发/聊天记录消息需要撤回当前消息，包括 OneBot `forward`/`node` 和 AstrBot `ComponentType.Forward`、`ComponentType.Node`、`ComponentType.Nodes`。
- QQ 闪传消息需要撤回当前消息，只识别展示文本 `QQ闪传` 和 aiocqhttp 显示文本 `该消息类型暂不支持查看`；普通文件、OneBot `file`、`[CQ:file,...]`、AstrBot `ComponentType.File` 不应撤回。
- OneBot 11 标准撤回接口是单条 `delete_msg`，没有标准批量撤回接口；除非确认适配器支持扩展接口，否则只循环单条撤回，不写臆造的批量接口。
- 状态功能放在 `功能文件/管理功能/基础功能/状态功能.py`，`main.py` 只调用 `处理状态指令`。群文件清理管理员白名单 QQ 可发送 `状态` 查看系统位数、CPU 占用、物理内存、磁盘空间、系统进程、操作系统、框架版本、数据库状态、系统运行时间和当前时间；每次输入仅检查一次数据库连通性，数据库只显示 `正常`、`未配置` 或 `异常`，不展示 Cookie、账号、IP、密钥、原始响应、数据库地址、功能开关、收费或网盘信息。
- 帮助功能放在 `功能文件/管理功能/基础功能/帮助功能.py`，`main.py` 只调用 `处理帮助指令`、`处理帮助指令MD带键盘` 和 `解析帮助回调命令`；QQ 官方互动桥放在 `功能文件/管理功能/基础功能/QQ官方交互桥.py`，`main.py` 只负责安装桥接。帮助菜单只展示现存的管理、小说、网盘和群聊功能；官方机器人帮助按钮使用 `action.type=1` 回调并由互动桥安全投递，不向聊天发送指令文本。
- 群文件清理、全部群文件清理、群文件状态、全量消息、帮助、状态、小说功能开关、QQ阅读 Cookie 保存、禁言以及群聊管理命令只能由插件配置 `group_file_cleanup_admin_qq` 里的 QQ 使用；这是群文件清理管理员白名单，不等同于群管理员。白名单外命中这些管理指令时一律静默忽略，不发送权限提示。
- 小说功能开关放在 `功能文件/管理功能/小说功能/功能/小说功能开关.py`，`main.py` 只调用 `处理小说功能开关指令`、`当前事件可使用小说功能` 和 `获取小说功能关闭回复`。群文件清理管理员白名单 QQ 可发送 `小说` / `小说列表` 查看全部小说功能状态；可发送 `开番茄` / `关闭番茄`、`开七猫` / `关闭七猫`、`开书旗` / `关闭书旗`、`开QQ阅读` / `关闭QQ阅读`、`开得间` / `关闭得间` 和 `开点众` / `关闭点众` 开关对应下载功能，`开启平台名` 与 `开平台名` 等效，QQ阅读大小写不敏感；管理员可发送 `开测试` / `开启测试`、`关测试` / `关闭测试` 控制测试模式，测试模式开启后管理员可继续测试已关闭的平台，普通用户仍受平台开关限制；状态写入 MySQL 内置表 `mantou_runtime_state`，不再写入 `功能文件/下载缓存/小说功能开关.json`；关闭后识别到对应小说链接时只回复功能已关闭，不进入下载流程。
- UC 网盘自动上传功能放在 `功能文件/管理功能/网盘功能/UC网盘.py`，`main.py` 只负责加载模块。插件配置使用 `_conf_schema.json` 的 `uc_pan_settings` 分类，包含 `uc_pan_cookie` 和 `uc_pan_upload_dir`，该分类必须放在 `database_settings` 上面，数据库配置继续保持最下面。`uc_pan_cookie` 由用户在插件配置手动填写浏览器 Cookie，`uc_pan_upload_dir` 默认 `/小说机器人`，上传时自动逐级创建目录。所有小说下载完成后先生成完整原小说 txt，并通过 UC 上传；上传前会分页扫描并删除目标目录下与原小说文件名完全相同的远端普通文件，不删除同名文件夹；目录列表里的文件夹判断必须兼容 `dir=1`、`dir=true` 和布尔 `True`，创建上传目录 `/file` 返回同名冲突时应重新查找已有目录继续上传；先尝试用原小说文件名上传，遇到 `23008`、`同名冲突`、`file is doloading` 或 `file is downloading` 时重新扫描并删除同名旧文件，然后继续使用原小说文件名有限重试，不得改用备用远端文件名；如果上传完成接口返回这类处理中错误，应复用当前预上传文件 ID 继续创建分享，创建分享接口仍返回处理中时必须等待重试。QQ 官方机器人上传成功后必须在同一条消息中发送“宝宝你的 / 书名 / 作者 / 已经下载完成”、Markdown“点击此文字打开”链接和 `action.type=0` 的“点击打开”按钮，供不支持链接按钮的客户端使用；不生成链接 txt，也不回退发送原小说 txt。UC 未配置或上传失败时，QQ 用户侧仅回复 `文件发送失败，请稍后再试`，详细错误只记后端日志。非 QQ 官方适配器可降级为同一完成文案加文本链接。UC OSS 单分片 PUT 响应没有返回 `ETag` 时，可以使用本地文件 MD5 作为单分片 ETag 继续完成上传，不要直接判定失败。UC 网盘只服务小说下载后的自动上传，不新增管理员手动上传、删除、创建文件夹或分享命令。
- 百度网盘后台上传功能放在 `功能文件/管理功能/网盘功能/百度网盘.py`，`main.py` 只负责加载模块。插件配置使用 `_conf_schema.json` 的 `baidu_pan_settings` 分类，包含 `baidu_pan_cookie`、`baidu_pan_upload_dir` 和 `baidu_pan_upload_status`，该分类必须放在 `database_settings` 上面。`baidu_pan_cookie` 由用户在插件配置手动填写浏览器 Cookie，留空表示不启用；`baidu_pan_upload_dir` 默认 `/小说机器人`，上传时自动逐级创建目录；`baidu_pan_upload_status` 必须在配置界面提供 `完结`、`连载`、`全部` 三个可选项，默认只上传完结。所有小说下载完成后仍先生成完整原小说 txt；如果百度网盘启用，会按状态规则后台上传原小说 txt；完结小说在目标目录已有同名普通文件时必须跳过重复上传，连载小说仍删除目标目录下同名普通文件后重新上传，不删除同名文件夹。百度网盘只做后台备份，不创建分享链接；完成按钮或降级链接消息发出后再后台上传原小说并清理源缓存，避免百度上传阻塞用户收到结果。百度上传失败只记录受控 warning，不回复用户，不影响完成按钮发送。
- 群文件清理统一由网页接口版 `功能文件/管理功能/群聊功能/网页群文件.py` 承担，不再保留适配器扩展接口版 `群文件清理.py`；`main.py` 只加载 `网页群文件功能` 并调用 `处理网页群文件清理`，该入口统一处理清理指令、清理选择数字回复与群聊管理指令。指令（仅群文件清理管理员白名单 QQ 可用）：`登录群文件`、`群文件登录cookie <cookie>`、`群文件状态` / `群文件cookie状态`、`清理群文件` / `群文件清理`、`清理全部群文件`、`添加群聊 群号 [群号...]`、`删除群聊 群号 [群号...]`、`查看群聊`、`更改备注`、`更改备注 群号`。Cookie 通过 `群文件登录cookie` 指令保存，支持普通 Cookie 头、curl、Cookie Editor JSON 和 cookies.txt，按管理员 QQ 隔离，31 天有效；Cookie 存 MySQL 内置表 `mantou_runtime_state`（namespace=`web_group_file_cookie`，state_key=管理员QQ，state_value 为 JSON），不再写本地 JSON 文件；保存后自动开启每 10 分钟保活，只使用 `skey` 调 qun.qq.com 账号级接口，只有 `p_skey` 时可保存但不做群文件接口回退保活；插件启动或重载时不得读取数据库启动保活；`群文件状态` / `群文件cookie状态` 和 `状态` 只显示 `群cookie：生效`、`群cookie：失效` 或 `群cookie：未保存`，不暴露 Cookie 原文、上次保活和失败次数。
- 网页群文件清理请求 `pan.qun.qq.com/cgi-bin/group_file/get_file_list` 和 `delete_file`，从 Cookie 提取 `skey` 或 `p_skey` 后用 hash 算法计算 `bkn` 鉴权；文件列表每页 50 个并发拉取，删除每批 20 个并发提交，失败自动重试 3 次。当前实现只删根目录文件，不递归子文件夹；遇到 `ec != 0` 或 HTTP 非 200 时按接口错误提示重新登录，不要用臆造接口绕过鉴权。核心清理逻辑抽到 `执行网页清理`，`清空指定群文件` 和 `清理全部已添加群` 复用，后者复用一个 aiohttp session 逐群清理并汇总成功/失败。
- `清理群文件` / `群文件清理` 列出「添加群聊」里的目标群列表后，同一会话同一发送者 120 秒内发送编号清理对应群，发送 `0` 取消；等待状态按会话和发送者 QQ 隔离（群聊用群号+发送者，私聊用发送者），过期自动失效；清理选择数字回复在 `处理网页群文件清理` 入口最前面判断，未命中等待状态时不拦截数字；列表为空时必须回复提示先发送 `添加群聊 群号` 添加目标群。`清理全部群文件` 先提示 `正在清理群文件（全部）`，再读取「添加群聊」添加的目标群列表逐群走网页接口清理。清理不再依赖机器人是群主/管理员，改用管理员 Cookie 鉴权；未登录 Cookie 时必须提示先发送 `登录群文件`。
- 群聊管理（`添加群聊` / `删除群聊` / `查看群聊` 及 MySQL 读写）合并在 `网页群文件.py` 内；待清理群列表写入 MySQL 内置表 `mantou_runtime_state`（namespace=`cleanup_groups`，state_key=`group_list`，state_value 为 JSON 数组），重载后继续生效。`查看群聊` 及删除、备注选择继续使用 Markdown + Keyboard 交互。`清理群文件` / `群文件清理` 的 QQ 官方选择键盘每页最多 9 个群号，每行 3 个群号，最后一行放「上一页 / 下一页 / 返回上一步」，总计固定最多 4 行；群数量超过 9 个时分页，不再因超过 QQ 官方 5 行限制而取消整块键盘。普通机器人仍为纯文本列表和手动编号选择。所有指令类按钮必须显式设置 `data为标签=False`，让按钮 `data` 发送实际指令文本。
- 全量消息功能放在 `功能文件/管理功能/群聊功能/全量消息.py`，`main.py` 只调用 `处理全量消息`。群文件清理管理员白名单 QQ 在目标群发送 `全量消息` 或 `全量消息状态` 时，必须按 QQ 官方 `GET /v2/groups/{group_openid}/bot_state` 查询状态；只使用官方群 OpenID，不获取、不要求、不显示数字 QQ 群号、机器人 QQ 或机器人 UID。响应 `recv_msg_setting=all` 才表示“群内全部消息已开启”，`allow_proactive_msg` 表示“机器人主动在群聊内发言”开关。该接口标注为内邀开放，调用失败时只显示状态暂不可查询并记录受控后端日志，禁止伪造状态。
- QQ 官方 Markdown 提及群成员只能使用 `<@member_openid>`；`member_openid` 来自官方群消息事件的 `author/member_openid`，不得用 QQ 号替代。QQ 官方群聊中的管理回复由 `帮助功能.发送QQ官方提及Markdown` 统一发送，首行必须提及当前操作人；私聊不插入提及。QQ 官方文档未提供写入全量消息开关的 REST 接口；群管理员需要在机器人资料页手动调整群消息接收设置，插件不得使用未文档化跳转链接或 UID 转换接口尝试代开。
- QQ 官方机器人 `qq_official` 发送文件不能使用 OneBot 的 `upload_group_file`/`upload_private_file` 作为主路径；应优先使用 AstrBot `File` 组件，它会走 QQ 官方富媒体接口 `/v2/users/{openid}/files` 或 `/v2/groups/{group_openid}/files`，再发送 `msg_type=7` 的 `media` 消息。如果 QQ 官方群聊返回 `call inner proxy error`，这是平台富媒体上传限制或临时错误，不要用臆造 OneBot 接口修复。
- 通过 AstrBot `File` 组件发送本地缓存文件时，不能在 yield 后立即删除缓存；应延迟清理，避免 qq_official/其他适配器在响应阶段读取文件前缓存已不存在。OneBot 回退接口调用完成后可以立即删除缓存。
- 找书功能放在 `功能文件/管理功能/小说功能/功能/找书.py`，`main.py` 只加载模块并优先调用 `获取找书下载回复流` 和 `处理找书指令`。用户发送 `找关键词` 或 `找书 关键词` 聚合搜索番茄、七猫、书旗、得间、点众；每页 5 条；支持 `上一页` / `下一页`。结果只展示书名、作者和页码，不展示链接、来源、平台名、热度数值和序号；分隔线使用 `————————`。跨平台同名同作者去重只保留一本，优先级为番茄 > 七猫 > 书旗 > 得间 > 点众；排序采用精确书名优先、多平台共识与相似度聚拢，热度只参与排序不展示。QQ 官方机器人搜索结果使用 Markdown 文字指令链：书名、作者和翻页文字使用 `qqbot-cmd-input`，点击后将 `选N` 或翻页命令填入输入框，用户发送后进入现有下载或翻页流程；兼容手动发送 `选1`～`选5`。找书会话按群/私聊 + 用户标识隔离，需兼容 `group_openid` / `user_openid` / `group_member_openid`；会话过期后提示重新找书。用户侧失败只回短提示，解析失败细节只写后端日志。
- 七猫小说功能只允许放在 `功能文件/管理功能/小说功能/小说/七猫小说.py` 一个文件内，不拆分多个 py 文件；`main.py` 只负责加载模块并调用 `获取七猫小说回复流`。
- 七猫小说只识别直接发送的七猫链接和七猫 JSON 分享卡片，不保留 `七猫搜索`、`七猫小说`、`七猫下载`、`七猫小说下载` 等文本命令。支持 `https://www.qimao.com/shuku/数字/`、`app-share.wtzw.com/.../article-detail/数字`、`app-share.wtzw.com/.../short-story-detail/数字` 或七猫 JSON 分享卡片，识别后下载 txt 发送到 QQ。下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，不要发送封面图片。七猫小说文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 文件顶部必须写入免责声明，全部写入 `功能文件/下载缓存/` 后优先通过 AstrBot `File` 组件发送，无法使用时再回退 OneBot 本地路径上传。文件发送成功后不要再额外发送下载完成或文件已发送提示，只有发送失败时回复错误原因。
- 数字/卡片撤回白名单必须包含七猫域名 `qimao.com` 和 `app-share.wtzw.com`，七猫分享卡片不能被 JSON 卡片撤回逻辑撤回。
- 七猫小说下载必须优先使用 App 批量正文接口，默认每批 500 章、最多 4 个批次并发；短篇 `short-story-detail` 必须走短篇详情、`reader_type=4` 目录和带 `reader_agent=1` 的正文/批量正文接口。七猫小说下载必须记录受控日志：开始下载、章节进度、章节完成汇总、发送方式、缓存文件删除结果；章节进度日志不能逐章刷屏，应按约 10% 分段输出。
- 七猫小说生成的 txt 只可写入本地缓存供 UC 和百度后台上传，禁止通过 AstrBot `File` 组件、OneBot `upload_group_file` 或 `upload_private_file` 发送给用户。完成消息统一由 UC 网盘模块发送链接按钮；UC 未配置或上传失败时仅回复固定文件发送失败提示，缓存必须清理。
- 书旗小说功能放在 `功能文件/管理功能/小说功能/小说/书旗小说.py`；只识别书旗链接和分享卡片，支持 `https://www.shuqi.com/book/数字.html`、`https://t.shuqi.com/catalog/数字/`、`https://t.shuqi.com/cover/数字`、`https://t.shuqi.com/shortNovel/reader/数字` 和 `http(s)://d.shuqi.com/短码`。下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`；下载时先请求 `downloadbatch/index` 和 `freedownurl` 批量接口获取约 30 章一组的批次/包 URL 状态，批量包已解锁时直接下载并解析 `.sqb/.sqc` 正文，未解锁或 URL 为空时再用最多 80 个动态并发拉取可读章节正文；单章网络断流、响应不完整等错误最多重试 3 次；短篇非免费章节可写入预览正文，不写失败占位。文件名、免责声明、本地缓存和发送逻辑与七猫一致。
- QQ阅读功能放在 `功能文件/管理功能/小说功能/小说/QQ阅读.py`；旧实现必须整文件删除，以 `D:\新建文件夹\source-code-master\source-code-master\QQ阅读\qq阅读.py` 的 CONFIG、csigs、动态密钥池、libfock 解密与 Fetcher 为唯一核心，只允许增加 AstrBot 链接识别、Cookie 必要字段解析与数据库登录态、App 详情/目录、完整性校验、TXT、本地缓存和 UC/百度发送适配。识别 `reader.qq.com`、`book.qq.com` 和 `novel.html5.qq.com` 的书籍链接；直接粘贴 Cookie 时支持普通 Cookie、`ywkey: 值` / `ywguid: 值` 两行文本、Cookie 头、curl、Cookie Editor JSON 和 cookies.txt，只保存 `ywguid/ywkey`，不保存 Cookie 原文中的其他字段；普通正文每批 500 章、最多 5 批动态并发，每个批量范围只请求一次，只有空正文、解密失败或响应缺失的章节才逐章重试 3 次；普通目录返回 HTTP 400 且详情存在章节数时切换出版资源目录；`site=4` 出版书目录必须同时识别 `ctebchaptercosurl`、`epubPureUrl` 和 `epubResourceUrl`，一次获取授权密码后并发解包；下载日志只在开始、约 10% 分段进度和完成时使用 INFO；下载前提示、文件名、免责声明、本地缓存和 TXT 头部格式与其他小说功能一致；缺少任意章节时停止合成，不写失败占位。
- `shing-yu/7mao-novel-downloader` 已归档且 README 说明 4.0+ 开源部分为 AGPL-3.0、核心模块部分代码私有；本仓库不要直接复制其私有下载逻辑，七猫功能应以本地自测接口结果维护。
- 番茄小说主功能放在 `功能文件/管理功能/小说功能/小说/番茄小说.py`，`main.py` 只调用 `获取番茄小说回复流`。该模块只接入本地番茄畅听详情、目录和 App 正文下载链路，不接入音频、媒体、探测、命令行调试、外部备用下载源或下载切换命令；支持普通书籍和 `short-story-share` 一章短篇。
- 番茄小说下载前必须先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，不发封面图片；正文按章节数动态分批，单批最多 1500 章、最多 6 个动态并发，完成后按目录顺序合并。畅听正文源返回缺章时，使用本地阅读 App 正文源补拉；最终仍缺章则停止下载，不写失败占位；下载日志只在开始、约 10% 分段进度和完成时使用 INFO，不逐章刷屏。
- 所有小说下载、正文和文件发送失败只向 QQ 用户回复固定短提示，不展示下载源名称、响应原文、IP、URL、账号、Cookie、密钥或请求参数；完整原因仅写受控后端日志。
- 番茄小说文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 顶部写入免责声明并使用 CRLF 换行。生成的 txt 只写入 `功能文件/下载缓存/` 供 UC 上传和百度后台备份；上传成功后发送 UC “点击打开”链接按钮，UC 未配置或失败时只回复固定文件发送失败提示。
- 数字撤回规则：消息中出现独立连续 9 到 12 位数字就触发。
- `你好1078887813` 和 `你好A1078887813 你好` 应触发。
- `107888A7813`、`1234567890123`、`123 456 789` 不应触发。
- 包含明文链接、URL 编码链接或常见域名路径的链接消息不应触发数字撤回。
- 普通 `@人` 消息不应触发数字撤回或群名片撤回；当前不提供 `@用户 踢` / `@用户 踢了` 管理员踢人入口。
- 修改群管功能后必须至少运行语法检查：`python -m py_compile main.py '功能文件\管理功能\群聊功能\群管功能.py'`。
