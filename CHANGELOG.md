# 更新日志

## v1.6.3

### 修复

- 恢复消息文本清理，支持回复、@ 机器人或带 CQ At 片段时继续命中触发词。

## v1.6.2

### 修复

- 数字撤回历史拉取兼容需要 `message_seq` 的 OneBot 实现，避免只撤回当前数字消息。
- 数字撤回按消息序号或时间取最近历史消息，不再依赖历史 API 的返回顺序。

## v1.6.1

### 修复

- 修复插件半更新时 `main.py` 导入新版数字撤回函数失败的问题。
- `main.py` 现在兼容新版 `处理数字撤回` 和旧版 `是否需要撤回数字消息 + 尝试撤回当前消息`。
## v1.6.0

### 调整

- 数字撤回改为按需拉取历史消息，不使用运行时缓存。
- 命中 9 到 12 位纯数字后，撤回该用户数字消息之前最近 20 条历史消息，并撤回当前数字消息。
- 历史消息 API 不可用时，至少尝试撤回当前数字消息。
## v1.5.0

### 新增

- 新增 `管理功能/数字撤回.py`。
- 当用户发送整条消息为 9 到 12 位连续数字时，插件会尝试撤回该消息。
- 数字撤回命中后不发送回复，避免刷屏。

### 调整

- `main.py` 在 oiapi 功能匹配前先执行数字撤回检查。

## v1.4.0

### 新增

- 新增 `古诗词名句` 功能，接入 `https://oiapi.net/api/Sentences`。
- 新增 `随机英文单词` 功能，接入 `https://oiapi.net/api/RandEnglishDict`。
- 新增 `随机一言` 功能，接入 `https://oiapi.net/api/AWord`。
- 新增 `疯狂星期四` 功能，接入 `https://oiapi.net/api/KFC`。

### 调整

- 将功能代码分类存放到 `功能文件/oiapi/` 目录。
- `main.py` 只保留插件注册、消息监听和功能调用。
- README 改为功能导航和折叠详情结构。

### 依赖

- 添加 `aiohttp` 作为异步 HTTP 请求依赖。




