# 更新日志

## v6.1.34

- 排查：记录本地图片临时 Markdown 地址，便于验证 QQ 服务端是否能访问服务器临时媒体路由。

## v6.1.33

- 修复：本地图片创建临时 Markdown 地址改为正确的同步调用，避免运行时 `TypeError` 导致图片发送失败。

## v6.1.32

- 排查：本地图片 Markdown 地址异常现在记录受控错误类型，便于定位运行时模块加载问题。

## v6.1.31

- 修复：本地图片 Markdown 地址优先使用当前已启动的帮助网页服务地址，补充地址不可用原因诊断，避免服务已启动时仍回退失败。

## v6.1.30

- 排查：补充本地图片 Markdown 临时地址创建状态日志，便于确认公网地址和运行时模块是否同步。

## v6.1.29

- 新增：本地图片可通过服务器内存中的短时公网地址发送为 QQ 官方 Markdown 图文，发送完成后自动过期，不写入本地图片文件；视频、语音和普通文件继续使用官方富媒体。

## v6.1.28

- 修复：QQ 阅读免费正文接口兼容只返回 `msg/data` 的响应，并在章节数量不足时记录请求范围、实际数量和预期数量，避免把 VIP 缺章误判成解密失败。

## v6.1.27

- 修复：QQ 图片消息中的 `￼` 对象占位符现在会被识别为图片锚点，前后文字按“前文、图片、后文”显示，兼容历史记录和发送回显。

## v6.1.26

- 修复：图片前后文字发送前会移除编辑器的 `￼` 占位符，并按前文、图片、后文保存媒体元数据，避免 QQ 把占位符显示为 `OBJ`。

## v6.1.25

- 修复：群列表不再使用最后一条群消息发送者的头像；群聊仅使用独立群头像字段或已填写群号生成的群头像，避免多个群复用同一成员头像。

## v6.1.24

- 修复：网页消息展示过滤 `OBJ: 数字` 内部对象占位符；有媒体时仅显示实际预览，无媒体时显示“消息对象”。

## v6.1.23

- 修复：网页图片发送按 QQ 官方 10 MB 以下富媒体单请求上传，并保留 `content`，避免发送图片和文字时接口异常。
- 修复：网页自发消息使用机器人资料显示名称和头像，不再显示“我”或字母占位。

## v6.1.22

- 修复：QQ 官方返回“消息内容违规”时，保留原 Markdown、按钮、状态和链接，仅清空书名、作者内容后自动重发，避免下载提示和完成链接丢失。

## v6.1.21

- 修复：消息行不再使用 `content-visibility:auto` 的估算高度，避免文字布局完成后把滚动条从底部推回中间；每次点选会话都会重新执行首屏到底部定位。

## v6.1.20

- 修复：QQ 官方语音附件的 `0x03 + #!SILK_V3` Tencent SILK 前缀现在正确识别并使用内置 Silk 解码器转换为 WAV，不再错误交给 ffmpeg 当作 AMR/WAV 解析。

## v6.1.19

- 修复：QQ 历史附件签名地址过期时，图片代理改为返回同源占位图并在内存中短时缓存失败地址，不再重复请求上游或在浏览器控制台产生 404。
- 修复：切换会话时缓存消息只用于隐藏预热；正式历史消息以真实布局高度完成到底部定位后再显示，避免先显示中间位置再跳到底部。

## v6.1.18

- 修复：会话缓存和最新历史同时完成时，旧的到底部校准任务可能提前显示消息，随后新任务再次推到底部造成闪动；现在仅最新定位任务可以显示内容和修改滚动位置。
- 优化：历史消息媒体识别先按占位符和 QQ 域名筛选，再执行匹配，避免普通长消息触发图片 URL 正则回溯，降低打开含媒体记录群聊的等待时间。

## v6.1.17

- 优化：历史消息分页使用快速规范化路径，避免每条已保存记录重复解析原始 JSON；仅缺少头像、提及或媒体资料时回看原文，降低打开聊天的后端耗时。

## v6.1.16

- 修复：首屏消息滚到底部时持续校准图片懒加载产生的高度变化，回到底部按钮也会在下一帧补齐滚动位置。

## v6.1.15

- 修复：打开会话首次重建消息时先完成两帧布局和到底部定位再显示消息，避免先出现在顶部后跳到底部造成闪烁。

## v6.1.14

- 优化：打开已缓存会话时，网络返回与当前消息页相同则只更新标题和资料，不再重复重绘整页消息，减少等待和闪屏。

## v6.1.13

- 修复：刷新时临时事件列表与正式群列表请求的竞态导致滚动位置恢复到底部；正式列表首次绘制完成前保持置顶，之后才保留用户位置。

## v6.1.12

- 修复：粘贴网页复制的图片时解析剪贴板 HTML 和 `data:image`，转换为图片文件后发送，支持单独发送复制出来的图片。

## v6.1.11

- 修复：网页单独发送图片改用 `image_file` multipart 上传，避免图片 Base64 JSON 请求过大导致发送失败；图文发送继续兼容原有流程。

## v6.1.10

- 修复：网页刷新后群列表首次渲染固定从顶部开始，不再恢复上次停留在底部的滚动位置；后续刷新继续保留用户位置。

## v6.1.9

- 修复：群禁言状态或机器人资料查询没有变化时不再重绘整页消息，避免切换会话后出现闪屏和二次滚动校准。

## v6.1.8

- 修复：滚动群列表时暂缓轮询和实时状态触发的列表重绘，停止滚动后恢复原位置，避免卡顿和回弹。

## v6.1.7

- 修复：群权限、禁言状态或机器人资料异步刷新消息后，已在底部的会话继续保持到底部；用户上滑查看历史时保留原滚动位置。

## v6.1.6

- 修复：切换群聊或私聊后首屏始终定位到最新消息，并在消息和懒加载内容完成布局后再次校准到底部。

## v6.1.5

- 修复：网页聊天图片复制改为优先写入真实图片数据，HTTP 页面增加图片数据回退，避免只复制图片节点或 HTML。
- 修复：输入框拖动调整高度使用合帧更新并停用拖动过渡，尺寸跟随指针且减少重排卡顿。
- 修复：群列表宽度恢复使用可持久化变量，扩大拖动命中区域，支持正常拉大和缩小。

## v6.1.4

- 优化：网页视频和普通文件改用 `FormData` 直传并在服务端分块落临时文件，减少 Base64 膨胀和内存峰值。
- 优化：复用媒体代理连接，实时资料按需返回，限制成员资料缓存，列表事件委托并跳过无变化重绘。
- 优化：消息历史请求增加超时，实时连接建立前保持快速轮询，消息列表和加载态补充滚动隔离与动效降级。

## v6.1.3

- 优化：网页媒体代理复用连接、消息历史和实时资料按需传输，减少重复请求、深拷贝和浏览器内存占用。
- 优化：视频/普通文件使用 `FormData` 直传，列表采用事件委托和内容隔离，历史请求增加超时与缓存淘汰。

## v6.1.2

- 修复：恢复腾讯官方 AppID + OpenID 群成员头像地址，非数字 OpenID 不再被错误过滤，历史记录继续优先使用已保存头像。

## v6.1.1

- 修复：保留 QQ 官方群成员头像字段并写入消息记录数据库，历史分页和网页重载后仍可显示。
- 修复：合并重复的机器人发送回调，限制被动消息过期重试，避免一次消息显示或发送两次。
- 修复：消息持久化失败重试、群信息聚合时间错配、单聊撤回、禁言时区和群信息刷新频率问题。
- 优化：网页上翻页与轮询互不取消、原始数据缓存按当前消息回收、失效图片保留占位；多网盘续传恢复同时处理待上传平台。
- 优化：百度异步删除后确认文件消失，UC 单分片上传使用文件流，释放得间/塔读解密线程池并修正本地缓存保留今天和昨天。

## v6.1.0

- 新增：执行找书搜索前立即提示“正在为您找关键词请稍等”，搜索结果完成后继续发送原有结果。

## v6.0.0

- 破坏性调整：消息收发、群管和文件发送统一只保留 QQ 官方适配器；移除 OneBot/CQ 消息段、通用动作调用、数字群成员查询、群列表接口和跨群禁言同步路径。

## v5.77.3

- 调整：自定义禁言时长改为输入天数，最大 30 天，输入超过 30 的数值自动按 30 天处理；快捷时长保持不变。

## v5.77.2

- 修复：已撤回消息保留原文时继续显示“引用”；活跃禁言同时提供“禁言”和“解除禁言”，禁言到期后恢复“禁言”入口，便于重新设置禁言时长。

## v5.77.1

- 修复：消息记录统一规范化官方消息 ID，并按同一原始网关事件去重，避免全量/At 回调或历史旧记录导致同一条消息重复显示。

## v5.77.0

- 修复：网页消息记录不再把 QQ 官方 OpenID 当作 QQ 号请求 qlogo；优先使用事件中实际提供的头像，未提供时显示姓名首字母，避免出现默认企鹅头像。

## v5.76.0

- 新增：网页消息记录页支持直接解除群成员禁言，使用 QQ 官方 `op=del` 请求并立即刷新禁言状态。

## v5.75.0

- 新增：消息记录页按 QQ 官方 `restrict_chat_setting` 接口显示当前被禁言成员、灰色头像和实时禁言倒计时；状态按群缓存 30 秒，降低官方接口请求频率。

## v5.74.0

- 新增：管理员白名单与 MySQL `mantou_runtime_state` 双向同步；AstrBot 配置变更会写入数据库，数据库外部变更会在启动、网页读取和权限校验时同步回 AstrBot 配置。

## v5.73.8

- 优化：番茄畅听正文解密改用独立动态线程池，按当前 CPU 核心数运行，避免受 asyncio 默认线程池上限影响；继续使用 `gmpy2` 完成 DH 运算、PyCryptodome 完成 AES 解密。

## v5.73.7

- 优化：所有小说平台章节进度日志最多输出 5 条（含 0% 起始进度），短篇下载不再逐章刷屏；开始下载和完成汇总日志保持不变。

## v5.73.6

- 修复：群资料和机器人状态接口返回“机器人非群成员”时正确标记群为已移除，不再只显示通用 `ServerError`。
- 改进：群资料请求日志增加阶段、群标识、appid、错误分类、HTTP/业务码、受控错误详情和冷却时间，便于定位接口失败原因。

## v5.73.5

- 修复：QQ阅读按详情 `free` 字段优先区分整本免费、VIP 和单章付费，单章付费书不会因 `vip_free` 标记误走第三方整本接口。
- 修复：同一 QQ 官方消息同时投递全量事件和 @机器人事件时按消息 ID 去重，避免同一本书重复启动下载任务。

## v5.73.4

- 调整：移除聊天媒体图床和图片 COS 转存，收到 QQ 图片、视频和文件时只记录官方附件地址，网页查看时按需代理，不再后台下载转存。
- 修复：网页和机器人发送的字节媒体改用 QQ 官方富媒体单条消息，文字与图片/文件一并发送；发送字节媒体按 MD5 保留本地副本供历史展示。

## v5.73.3

- 新增：聊天图片、视频和普通文件统一直接转存图床，默认使用 `https://0x0.st`；收到的 QQ 媒体和网页发送媒体不再保存到服务器本地，消息记录只保留图床外链，图床上传失败不落地媒体文件。
- 新增：图床上传支持字节、远程 URL 和流式转发，设置页可替换兼容 `multipart` 的图床地址并保存可选 Token。

## v5.73.2

- 修复：消息记录图片、视频和文件缓存不再只依赖系统临时目录；改为持久化到 AstrBot 数据目录并保留 30 天，历史页面携带消息 ID 回退读取已保存媒体，减少 QQ 签名地址过期或重启后出现 404。
- 修复：网页发送的 Markdown 图片令牌缓存延长至 30 天，并将缓存数量上限提高到 256。

## v5.73.1

- 调整：网盘远端小说清理改为保留今天和昨天，每天零点删除当前日期前两天及更早的 TXT；UC、夸克和百度保持一致。

## v5.73.0

- 新增：每天按服务器本地时间零点清理 UC、夸克和百度上传目录中早于当天的小说 TXT，支持多账号，跳过文件夹、无法确认日期的文件和当天文件。

## v5.72.1

- 修复：QQ阅读非 VIP 账号仅对免费书和 VIP 书请求第三方整本正文；单独付费书继续限制为免费章节，避免请求付费内容。

## v5.72.0

- 调整：QQ阅读非 VIP 账号不再按 `max_free_chapter` 截断目录，改为按原始目录整本请求第三方明文正文；VIP 账号继续走官方账号正文接口。

## v5.71.3

- 调整：QQ阅读下载日志在 INFO 级别显示账号 VIP 状态和实际正文来源，便于确认非 VIP 是否走免费明文接口。

## v5.71.2

- 修复：补充 QQ 网关删除/撤回事件的兼容分发，后续撤回会同步消息记录；撤回消息保留原始正文、引用和媒体，仅显示红色撤回状态。

## v5.71.1

- 修复：撤回消息在控制台中保留原始正文、引用和媒体，仅使用红色气泡与“已撤回”标签；兼容网关删除/撤回事件并同步内存与数据库状态。

## v5.71.0

- 新增：QQ阅读按当前登录账号的 `isVip` 状态分流；非 VIP 账号只请求免费章节明文接口，VIP 账号继续使用原账号正文接口，并保留免费章节筛选。

## v5.70.8

- 修复：本地 Markdown 图片缓存改为写入 AstrBot 数据目录，容器重启或插件重载后仍可恢复三天内的图片令牌。

## v5.70.7

- 修复：本地图片 Markdown 令牌改为临时磁盘缓存并保留三天，插件重载后仍可读取，减少历史消息出现图片 404；图片正文不再常驻进程内存。

## v5.70.6

- 修复：AstrBot 重装插件时不再替换正在上传的小说 TXT 缓存目录；所有小说平台统一使用稳定数据目录，并兼容清理旧目录与恢复旧续传任务。

## v5.70.5

- 修复：QQ 图片、视频和文件的临时签名地址在消息接收后异步预缓存到临时磁盘，地址过期后网页仍可查看；预缓存使用有界并发且重载时取消，不阻塞消息接收。

## v5.70.4

- 性能：帮助网页重载前主动取消 SSE/WebSocket 长连接，避免 AstrBot `runner.cleanup()` 等待连接超时；插件重新安装不再因打开的消息页面额外卡住约两分钟。

## v5.70.3

- 修复：QQ 图片签名链接过期后由媒体代理缓存的文件继续显示；缓存使用服务器临时磁盘并自动清理，不把图片长期放在进程内存中；移动端返回按钮隐藏前主动移除焦点，消除 `aria-hidden` 聚焦警告。

## v5.70.2

- 修复：QQ 官方自动禁言仅依据事件中的成员角色执行，角色缺失时跳过处罚；官方服务端 5xx/429 临时错误增加最多 3 次退避重试，权限或成员错误不重复请求。

## v5.70.1

- 性能：已读状态改为后台持久化，网页不再等待 MySQL 提交；管理员权限扫描改为延迟、单群串行执行，避免首屏多个 `group-roles` 请求拖慢消息加载。

## v5.70.0

- 优化：群列表改为全部加载，不再只显示前 50 个会话；后端聚合查询取消群列表数量截断，消息历史仍按 100 条分页加载。

## v5.69.0

- 新增：后台低频轮询已知 QQ 官方群基本信息，按单群至少一小时、每次请求间隔三秒的节奏更新群名、简介、分类、标签和成员数，失败与限流继续使用冷却；手动刷新仍可立即强制更新。

## v5.68.2

- 修复：补齐 QQ 官方事件发送和主动会话发送的机器人消息记录，兼容发送接口返回空值或未更新会话消息 ID 的情况，避免部分机器人消息在控制台历史中消失。

## v5.68.1

- 修复：自动撤回、网页撤回和批量撤回都会同步消息记录状态；撤回消息隐藏原正文，只显示“已撤回”，避免原始消息继续出现在气泡中。

## v5.68.0

- 新增：主题选择简化为浅色模式和深色模式两项，控制台与登录页切换时增加主题和图标动画；不再显示“跟随系统”。

## v5.67.0

- 新增：消息编辑区改为单一可编辑区域，图片作为内联元素插入光标位置，支持文字在图片前、后或中间；发送时按官方 Markdown 图文格式保持原顺序。

## v5.66.0

- 新增：消息编辑区支持选择并发送 MP4 视频和任意格式文件，按 QQ 官方富媒体分片上传流程生成 `file_info` 后发送。
- 优化：群列表宽度可拖动到 680px；列表和编辑区收起按钮移动到顶部，箭头会随状态变化并带过渡动画；编辑区顶部增加图片、视频和文件入口。

## v5.65.0

- 新增：帮助网页设置页仅展示监听端口、登录账号和登录密码；密码输入支持显示/隐藏和修改，留空时保持原密码不变，监听地址与域名配置继续保留兼容。

## v5.64.0

- 新增：控制台首页快捷入口和状态卡操作改为可点击页面按钮，直接跳转到机器人配置、小说功能、网盘配置、系统设置和运行状态页面。

## v5.63.0

- 新增：消息记录页增加本地消息轮询校准；Gateway/WebSocket/SSE 在线时每 60 秒校准，事件通道不可用时每 15 秒校准，仅读取本地消息接口，不调用 QQ 官方群历史接口。
- 依据 QQ 官方消息收发文档，群消息继续以 `GROUP_MESSAGE_CREATE`/`GROUP_AT_MESSAGE_CREATE` Gateway 事件为主，轮询只用于补齐网页偶发漏事件。

## v5.62.28

- 修复：群资料或机器人状态接口返回群不存在、机器人不在群时，立即持久化“已移除群聊”状态；网页列表会把已退出群放到下方并显示移除提示。
- 优化：群成员状态缓存改为最多 30 分钟复核，网页会重新检查过期状态，避免旧的管理员缓存遮住已移除群。

## v5.62.27

- 修复：入群欢迎和其他 QQ 官方主动消息发送前查询群机器人状态；根据 `allow_proactive_msg` 判断是否允许主动推送，明确无权限时不再调用发送接口，并缓存群状态避免重复请求。

## v5.62.26

- 修复：群主和管理员消息不再显示撤回、禁言或多选撤回操作；引用操作继续保留。

## v5.62.25

- 优化：消息历史首屏只加载当前 100 条，删除自动预取隐藏消息的请求；用户滚动到顶部时才按 100 条分页加载更早消息。

## v5.62.24

- 修复：配置保存回滚快照改为递归复制 JSON 配置值，避免复制 AstrBot 配置对象内部锁导致读取阶段 `TypeError`。

## v5.62.23

- 修复：帮助网页配置保存兼容 AstrBot 新版 `save_config_async()` 和旧版 `save_config()` 签名，避免配置保存时出现 `TypeError`；错误日志增加保存阶段。

## v5.62.22

- 修复：夸克 OSS 上传和合并分片根据夸克/OSS `Date` 响应头校准签名时间，遇到 `RequestTimeTooSkewed` 自动重新获取签名并重试一次。

## v5.62.21

- 优化：缓存 QQ 官方主动消息权限结果，已确认无权限的会话在 30 分钟内直接跳过发送；收到新消息后清除旧拒绝状态并重新判断，减少“主动消息失败，无权限”请求和日志。

## v5.62.20

- 优化：实时 WebSocket/SSE 正常时停止消息会话列表的重复轮询；历史旧消息预取延后执行；群角色失败重试和单群角色探测冷却调整为 5 分钟，减少消息页加载期间的重复请求。

## v5.62.19

- 优化：手机端进入聊天后隐藏顶部品牌栏、机器人资料和导航，只保留聊天界面；点击返回列表或离开消息页后恢复。

## v5.62.18

- 优化：手机网页版压缩顶栏、机器人资料区和导航占用高度，改为紧凑横向导航带，并收紧消息筛选与搜索区域，让消息列表更早进入首屏。

## v5.62.17

- 修复：发送或拖入 QQ 图片时先经同源媒体代理读取并重新生成短时公网地址；Markdown 图文发送启用官方图片资源校验，避免消息已发出但图片显示为空。

## v5.62.16

- 修复：图片消息和自己发送的消息可使用按钮或右键菜单引用；保存 QQ 官方发送响应中的 `ext_info.ref_idx`，发送引用时优先使用官方 `REFIDX`。

## v5.62.15

- 修复：图片消息和自己发送的消息支持引用；引用机器人消息时保存并使用 QQ 官方发送响应中的 `ext_info.ref_idx`。

## v5.62.14

- 修复：网页本地图片与文字发送不再依赖官方富媒体接口返回不存在的 `raw_url`；图片暂存到服务端短时公网地址后，按官方 Markdown 规范发送单条图文消息。

## v5.62.13

- 修复：网页本地图片与文字发送改用短时公网图片地址，按 QQ 官方 Markdown 图片规范发送单条图文消息；富媒体 `file_info` 不再误判为可嵌入图片地址。

## v5.62.12

- 优化：消息输入框添加图片后支持在图片前后分别输入文字，发送时按前文、图片、后文顺序合并为一条图文消息，历史记录保留文字位置。

## v5.62.11

- 修复：消息编辑区拉高时，输入框自动填充剩余高度，避免编辑区底部出现空白；手机端同步取消输入框固定高度限制。

## v5.62.10

- 优化：消息记录页新增 QQ 手机端布局，手机先显示会话列表，点击会话后进入独立聊天页，可通过顶部返回按钮回到列表；桌面端继续使用双栏布局。

## v5.62.9

- 优化：消息会话列表将已移除群聊统一放到列表底部，并增加“已移除群聊”分隔标识；移除群聊区域不显示举报文案。

## v5.62.8

- 修复：识别 QQ 官方 `GROUP_DEL_ROBOT` 事件并持久化群成员状态，网页会显示“你已被移除群聊”并停用该会话操作。
- 优化：群成员状态探测增加 24 小时缓存，避免重复调用 `bot_state` 接口。

## v5.62.7

- 新增：按 QQ 官方 `GROUP_DEL_ROBOT` 和 `bot_state` 判断机器人群成员状态，持久化群状态并在网页列表显示“你已被移除群聊”。
- 优化：群状态探测增加 24 小时缓存，避免频繁调用官方接口。

## v5.62.6

- 修复：消息会话列表和编辑区恢复拖动、收起功能，宽度、高度及两处收起状态写入 MySQL 运行状态，刷新页面后保持。

## v5.62.5

- 新增：通过 QQ 官方 `GET /users/@me` 获取并缓存机器人名称、头像和 ID，网页侧栏、机器人配置和消息记录统一展示真实资料。

## v5.62.4

- 优化：小说下载失败的用户提示统一为“下载失败 请重试”，后台日志仍保留详细排错信息。

## v5.62.3

- 修复：网页图片和文字按 QQ 官方 Markdown 图文协议发送；本地图片使用官方分片上传取得临时公网地址，无法取得时回退为合法的两条消息。

## v5.62.2

- 修复：消息图片支持右键复制，并可拖动到输入框作为图片附件，不再拖动消息文字内容。

## v5.62.1

- 修复：网页消息发送改为乐观显示，输入框立即清空并允许连续发送，失败消息显示感叹号且可点击重试。
- 优化：引用消息统一自动插入 QQ 官方 Markdown 提及；隐藏消息页冗余发送方式、类型标签和备注筛选控件。

## v5.62.0

- 新增：重设计帮助网页网盘中心，按平台工作区集中管理开关、目录、账号和群账号选择，移动端保持单列布局。
- 优化：找书查询词自动去除标点和装饰符，仅保留中文、字母、数字及必要空格，统一各平台搜索和缓存键。
- 修复：单网盘完成消息按钮及备用文字链接统一显示“点击打开”，多网盘继续显示平台名称并限制每行两个。

## v5.61.2

- 性能: 多网盘分享改为显式创建并发任务，所有已开启且已配置的平台在同一批次调度上传。
- 优化: 单个网盘生成的 QQ 官方按钮和备用文字链接统一显示“点击我打开”；多个网盘继续显示平台名称并限制每行两个按钮。
- 说明: UC、夸克和百度当前上传响应均未返回可验证的分片地址或分片大小，继续使用已验证的单分片协议，避免使用未确认参数导致上传合并失败。

## v5.61.1

- 修复: 多网盘完成消息按钮直接显示平台名称，并限制为每行最多两个按钮。

## v5.61.0

- 新增: 网盘出口会并发上传到所有已开启且已配置的 UC、夸克和百度网盘，并为每个平台生成独立分享链接和打开按钮。
- 变更: 百度网盘取消后台备份和上传状态配置，改为与 UC、夸克一致，仅在启用时参与主分享。

## v5.60.4

- 新增: 网盘配置页为 UC、夸克和百度增加独立的开启/关闭开关，状态写入 `mantou_runtime_state`，上传与百度后台备份统一遵守开关状态。
- 修复: 网盘已关闭时禁止从聊天或控制台切换为主分享网盘，并在控制台显示运行状态。

## v5.60.3

- 性能: 消息记录列表只查询会话骨架和最后消息摘要，历史原始消息按需分页并在前端缓存，切换会话先显示缓存再异步刷新。
- 修复: 群管理员状态恢复时保留数据库中的 `is_admin`，并校验角色请求的会话；临时请求失败按冷却重试，避免群名红色状态闪烁或永久不恢复。

## v5.60.2

- 修复: 番茄小说下载入口不再把整条纯数字消息误识别为书籍 ID；分享链接、卡片和找书结果下载保持不变。

## v5.60.1

- 优化: 控制台消息记录页面增加群管理员状态全量后台自动扫描与实时更新；无需手动点击群聊即可全自动识别所有管理员群并染红；结合 MySQL 与浏览器本地持久化存储，解决刷新页面后群名称红色高亮状态丢失的问题。

## v5.60.0

- 新增: 控制台消息记录页面支持群管理员身份状态展示；当机器人在对应 QQ 群具备群管理员/群主权限时，左侧会话列表与聊天窗口顶部的群名称自动高亮显示为红色，方便快速区分具有管理权限的群聊。

## v5.59.1

- 删除: 移除 OneBot 兼容接口，禁言功能只保留 QQ 官方成员禁言接口；删除 `是OneBot适配器` 函数和 `使用_set_group_ban禁言` 函数，统一改为 `执行成员禁言` 直接调用 QQ 官方接口；清理帮助说明和注释中的 OneBot 相关描述。

## v5.59.0


- 新增: QQ 官方群广告拦截支持 `开广告`、`关广告` 和 `广告状态`，每个群独立保存开关（默认关闭）；网页消息会话顶部可直接切换同一状态。
- 调整: 自动数字、卡片、合并转发和 QQ 闪传撤回只保留 QQ 官方群消息路径，只撤回和禁言当前触发群；删除 OneBot 自动撤回、跨群自动禁言、历史消息读取与兼容分支。

## v5.58.4

- 修复: 追书链接兜底识别限制在追书域名和协议，避免带有 `book_id` 的番茄长读/畅听链接被误判为追书。

## v5.58.3

- 修复: 控制台消息记录兼容 `mqqapi://aio/inlinecmd` 内联指令链接，文件搜索结果不再显示原始链接，点击条目可填入对应指令。

## v5.58.2

- 修复: 控制台消息记录不再把 QQ Markdown、@用户标签和 `qqbot-cmd-input` 原样显示；消息内容会安全渲染，书名/作者指令标签可点击填入消息输入框。

## v5.58.1

- 性能: 追书正文请求并发上限提升至 400 路动态并发，继续复用单本下载的异步 HTTP 会话和连接池。
- 性能: 追书章节密钥 AES-GCM 解析与正文 AES-CBC 解密统一交由按 CPU 动态限制的 PyCryptodome 解密线程池执行，减少高并发时事件循环阻塞。

## v5.58.0

- 新增: 追书小说支持 `m.zhuishushenqi.com/books/<书籍ID>` 等追书神器链接和找书结果。
- 新增: 自动建立追书神器游客会话，按目录逐章并发获取密钥与正文，使用 PyCryptodome 解密并完整合成 TXT；正文和密钥失败时重试，缺章不生成残缺文件。
- 新增: 找书聚合、小说开关、帮助菜单和网页平台状态加入追书小说。

## v5.57.0

- 性能: 消息历史首次只加载 100 条，首屏显示后后台预缓存上一页；滚动到顶部优先使用预缓存，切换会话或刷新时自动取消旧预取。
- 修复: 消息历史接口统一限制单次最多返回 100 条，避免旧页面继续一次请求 300 条造成卡顿。

## v5.56.1

- 性能: 消息记录缓存裁剪改为增量计数并一次裁剪到上限，避免消息洪峰时反复扫描全部会话拖慢事件循环。
- 性能: 消息记录的消息、未读、群资料和昵称数据库写入改用独立线程池，未读状态按批次单连接写入，避免与各小说下载的解析/解密线程争用。
- 性能: 机器人发送记录改为 DEBUG 级别，避免每条小说进度回复触发同步 INFO 日志输出。
- 修复: 未配置数据库时不再为每条消息启动无效的持久化队列任务。

## v5.56.0

- 新增: QQ阅读找书预检保留有免费章节的单章付费书籍；目录按免费、VIP、单章付费三类处理，单章付费书无论账号是否为 VIP 都只请求免费章节，完全没有免费章节的书籍才会被过滤。

## v5.55.5

- 修复: QQ阅读按当前 `isVip` 状态选择章节：非会员只下载 `chapter_fee` 和 `maxfreechapter` 标记的免费章节，会员下载完整目录，避免对非会员必然为空的会员章节批量请求和重复补拉。

## v5.55.4

- 修复: QQ 官方消息记录中的 `@用户` 优先显示成员昵称，兼容原始字典、botpy 对象和嵌套消息结构。
- 修复: 会话列表预览和实时消息同步提及昵称，避免显示完整 openid。
- 修复: 图片改为当前页面灯箱放大，移除“打开图片”新窗口链接。

## v5.55.3

- 修复: 实时消息事件缺少未读字段时，仅首次事件递增红点，重复事件不会重复计数。

## v5.55.2

- 修复: 消息会话列表不再把未读会话强行排到已读会话前面，已读会话继续按最后消息时间保持原有位置。
- 修复: 每条首次记录的消息都计入未读红点，包括自己和机器人发送的消息；重复 `message_id` 回调仍只计一次。

## v5.55.1

- 修复: 点击消息会话时先立即切换右侧会话标题、选中状态和加载占位，下一帧再请求历史消息，避免等待接口返回后才跳转。
- 修复: 快速切换会话或离开消息页时取消尚未开始的历史请求，防止旧会话结果覆盖当前会话。

## v5.55.0

- 性能: 消息记录启动时只恢复会话骨架和最后消息摘要，历史正文改为打开会话后按页读取；会话列表不再遍历全量历史消息。
- 新增: 消息历史初始加载 300 条，滚动到顶部自动加载更早消息并保持滚动位置。
- 修复: 群资料只在打开群会话时按需刷新，成功后持久化到 `mantou_group_infos`；列表刷新不再重复调用 QQ 官方群信息接口，失败和限流使用长冷却，后台请求间隔保持在官方 30 QPM 限制以内。
- 修复: 历史分页不重复请求群内角色接口，避免向上滚动时产生无关官方请求。

## v5.54.5

- 性能: 消息记录会话列表直接使用内存摘要和数据库最后消息摘要，不再遍历、规范化和排序全部历史消息；保留旧记录时间修正，并正确更新纯媒体/空文本消息的最新预览。
- 修复: 会话列表在没有待写状态时合并内存与持久化未读数，避免重启或多标签页刷新后红点回退。

## v5.54.4

- 修复: 帮助网页消息列表和历史查询改用独立线程池，避免小说下载任务占满默认线程池后导致消息无法加载；列表只读取最后消息摘要，减少大字段传输。

## v5.54.3

- 性能: QQ阅读自定义 csigs 签名改用兼容的 bcrypt 原生实现，并将签名与正文解包解密放入独立线程池，避免下载任务阻塞消息记录网页和机器人事件循环；消息列表元数据缓存空结果，减少重复数据库连接；网页消息会话请求去重、超时可重试并保留已有列表。

## v5.54.2

- 修复: 帮助网页桌面端侧边导航独立固定在视口高度内，右侧长内容单独滚动；移动端继续使用普通页面流。

## v5.54.1

- 修复: 帮助网页在 340px 以下窄屏时顶栏品牌被压缩的问题，保留机器人标识并压缩非必要顶栏信息，主题选择仍可用。

## v5.54.0

- 新增: 帮助网页控制台和登录页增加浅色模式、深色模式和跟随系统三种外观选择；选择保存在当前浏览器，跟随系统时会响应系统外观变化，并覆盖消息记录、表单、弹窗和移动端布局。

## v5.53.7

- 修复: 关闭的小说平台不再参与找书搜索，搜索缓存、翻页和选书下载会按当前可用平台重新过滤；管理员测试模式继续保留原有测试例外。

## v5.53.6

- 修复: 消息记录中的 QQ 图片和文件通过登录保护的同源媒体入口加载，兼容签名地址、跨域限制和 `application/octet-stream` 图片响应；文件显示文件名并支持下载，原地址保留为回退入口。

## v5.53.5

- 修复: 消息记录页兼容 QQ 官方附件和带空格的图片/文件 URL；图片支持预览及失效后打开，文件显示文件名和下载入口，历史旧消息会从 raw_data 补回媒体信息。

## v5.53.4

- 性能: 帮助网页控制台批量读取小说开关和网盘账号摘要，消息历史使用 `LIMIT + 1` 判断分页并补充会话索引；实时消息合并渲染、请求取消和已读提交节流，减少页面卡顿与数据库往返；HTML/JSON 响应启用压缩，群资料后台刷新复用单个任务。

## v5.53.3

- 修复: QQ 官方同一条消息同时触发 at/group 回调时被消息记录重复保存；接收、发送回显、MySQL 写入和网页历史现在按会话内 `message_id` 幂等，旧库中的重复行也只显示一次。

## v5.53.2

- 修复: 消息记录置顶状态在并发刷新或备注/昵称保存时被旧数据覆盖；置顶现在只更新自身元数据并等待最新列表刷新。

## v5.53.1

- 修复: 机器人自动回复不再把会话未读数清零，只有网页打开会话并执行已读操作时才移除未读红点。

## v5.53.0

- 新增: QQ 官方群资料使用 `/v2/groups/{group_openid}/info` 获取并长期保存到 `mantou_group_infos`，启动时恢复群名、简介、分类、标签和成员数；普通访问复用 24 小时缓存，网页手动刷新才强制请求最新资料，接口失败时保留旧缓存。

## v5.52.14

- 修复: 帮助网页 WebSocket 在握手或首条就绪消息阶段异常断开时也会释放实时订阅，避免重连后旧任务残留。

## v5.52.13

- 性能: QQ 官方消息事件回调改为有界队列非阻塞入队，解析、未读统计、昵称补查和数据库写入由顺序 worker 处理；插件重载后会重新启用接收队列，避免消息接收被同步处理拖慢或停止。
- 修复: 消息记录页改用 WebSocket 作为实时消息主通道，断线自动降级 SSE；补齐 WebSocket 关闭时的任务和订阅清理，保持查阅历史时的滚动位置与右下角新消息提示。

## v5.52.12

- 修复: 消息接收不再在事件回调中同步读取 MySQL 未读数或执行总量裁剪；消息先写入内存并由后台持久化，降低高并发接收时的事件循环延迟。
- 新增: 消息记录页增加有界 SSE 实时事件通道，收到消息后立即刷新会话列表和历史；数据库写入尚未完成时也会合并内存消息，未读红点不再等待固定轮询周期。

## v5.52.11

- 修复: 消息记录未读会话现在会参与列表排序，未读数和消息写入在插件重载/退出前会等待落库；已读接口会确认清零写入完成。
- 修复: 置顶/备注保存失败时网页不再误报成功，消息记录 MySQL 兼容字典游标并修复历史表结构检查使用已关闭游标的问题；旧表长字段自动扩容，元数据统一使用配置的运行状态表。
- 维护: 主模块重载时显式刷新消息记录存储和运行状态数据库依赖，避免网页继续使用旧的持久化代码。

## v5.52.10

- 性能: 书旗正文下载复用异步 HTTP 会话并将连接池与动态并发上限提升到最多 400 路；章节调度采用有界任务窗口，避免整本一次性创建大量任务。
- 性能: 书旗字符变换改用转换表快速路径，标准库 Base64/HTML 解码移入受限解码执行器，网络请求与正文解码不再互相阻塞；失败章节按动态并发递减重试。

## v5.52.9

- 修复: 书旗目录改用 iOS 明文目录接口并动态生成签名，正文按目录逐章解码并校验完整性；移除会把不同 UID 返回的乱码批量包误判为成功的旧 VIP 批量链路。

## v5.52.8

- 清理: 删除 `功能文件/管理功能/基础功能/帮助网页.py` 旧路径兼容文件，帮助网页统一从 `功能文件/页面功能/` 加载。

## v5.52.7

- 修复: 帮助网页消息记录在查看历史时收到新消息会被强制拉到底部；现在保留当前滚动位置，并在右下角提供新消息和回到底部入口。

## v5.52.6

- 重构: 将帮助网页从管理功能单文件拆分到 `功能文件/页面功能/`，按服务入口、后端 API、HTML 结构、CSS、JavaScript 和登录页资源分模块维护；保留旧路径兼容导出，更新插件重载时会显式刷新页面依赖。

## v5.52.5

- 修复: 消息记录在 UTC 服务器上误用系统时区解析 QQ 官方 `+08:00` 时间，导致会话排序错位；统一按 Asia/Shanghai 规范化历史时间，机器人来源记录稳定显示，并使用消息 ID 分页避免旧时间戳漏消息。

## v5.52.4

- 修复: 后台并发写入 MySQL 时消息自增 ID 按完成顺序分配，导致对话页消息顺序错位；现在后台写库串行化，同时保持收发不等待数据库。

## v5.52.3

- 性能: 消息记录的 MySQL 写入和未读数持久化改为后台执行，不再阻塞 AstrBot 收发消息事件循环；机器人发送先执行 QQ 发送，再异步落库。

## v5.52.2

- 修复: 控制台优先读取数据库历史时可能遮住本进程刚捕获的机器人发送记录；现在会合并未落库的机器人消息，并增加不含正文的捕获诊断日志。

## v5.52.1

- 修复: 发送消息记录缺少会话标识，导致机器人主动消息未真正写入 MySQL；现在主动消息重载后仍可在控制台历史记录中查看。

## v5.52.0

- 新增: 消息记录支持机器人主动发送的消息——机器人主动推送（帮助菜单、下载完成按钮、群管提醒、群成员欢迎、主动文本回复等）发送成功后写入消息记录，控制台聊天记录里以"机器人"昵称和"机器人"标签展示，与网页手动发送的"我"区分，可与普通消息一样引用和检索。

## v5.51.5

- 修复: 控制台消息记录页未读红点——当前正在网页上打开/查看的会话，新消息不再显示红点（与 QQ 正在查看的会话一致），切走后才累计未读；正在查看的会话收到新消息时自动标记已读。

## v5.51.4

- 修复: 控制台消息记录未读红点不准确——未读计数原来只保存在进程内存，重启或会话被淘汰后全部丢失（红点读取不到、多条消息只剩 1 条）；现在把每个会话的未读数同步持久化到 MySQL，重启后从库恢复，聊天列表聚合时内存与库双向兜底，打开会话清零时内存与库同步清除。

## v5.51.3

- 改进: 书旗下载顺序调整为"整本压缩包优先，VIP 批量包兜底"——整本压缩包走固定 UID 通道稳定，多数书直接一次成功，不再每次下载都先触发书评借 UID、失败、清除缓存换新；整本地址缺失时会重试一次自愈；整本不可用（地址缺失/缺章）才回退 VIP 批量包，未解锁保留缓存不换新，仅真正的 UID 失效才清除缓存换新重试。

## v5.51.2

- 改进: 书旗某本书 VIP 批量"未解锁"不再被当作 UID 失效——未解锁属于该书权限范围问题，保留已保存 UID 缓存直接回退整本压缩包，避免每次下载同一本书都清除缓存、重新扫描换 UID；只有真正的 UID 失效错误才清除缓存换新。

## v5.51.1

- 改进: 书旗 VIP UID 失效后不再重复使用同一个已失效 UID——记录失效 UID，重新扫描书评时跳过它并尝试下一个不同的年费 VIP UID，找到后替换保存；平时内存/MySQL 有已保存 UID 时不扫描，只有失效才重新获取。
- 修复: 消息记录页全部消息显示"已撤回"——消息记录存储读取 MySQL 时 recalled/is_self 布尔误判（bool("0") 恒为 True），改为按值判断。

## v5.51.0

- 新增: 书旗自动获取的年费 VIP UID 持久化到 MySQL（命名空间 shuqi_vip_uid），获取到新 UID 直接替换旧值保存，插件重载/重启不丢失，避免每次下载都重新扫描获取；未配置数据库时自动回退原内存缓存。
- 改进: QQ阅读 缺少任意章节导致下载失败时，WARNING 日志输出具体缺失章节号和失败分类（空正文/解密失败），便于排查单章失败原因。

## v5.50.0

- 新增: 控制台登录会话持久化到 MySQL（命名空间 console_session），有效期延长至 30 天——同设备浏览器无需因插件重载、更新代码或服务重启重新登录，滑动续期避免长期使用掉线；不同设备（新浏览器/Cookie 缺失）才需要重新登录；未配置数据库时自动回退原内存会话。

## v5.49.0

- 新增: 控制台、消息记录页与登录页全部页面支持深色模式与浅色模式，自动跟随系统外观（prefers-color-scheme）切换；浅色样式保持不变，深色下采用深蓝灰配色并覆盖全局卡片、表单、弹窗、消息气泡与网页控件。

## v5.48.4

- 优化: 参考 ElainaBot_v2 的消息存储方案，控制台消息保存与获取改为以 MySQL 为核心——聊天列表改由数据库单次 GROUP BY 聚合会话（最后消息 id/时间/条数，再按 id 批量补查最后内容），消息历史改为从数据库按 id/时间分页查询（支持加载更早），数据库不可用时自动回退原内存缓存，重启后历史消息与完整会话列表不再依赖进程内存。

## v5.48.3

- 修复: 消息记录群信息刷新请求过密触发 QQ 官方接口限流（错误码 100017）并反复重试已注销群的问题——刷新任务加并发锁防多个控制台请求同时刷新、冷却期内跳过请求、已注销群冷却一天、限流冷却 5 分钟，其他失败冷却 60 秒。

## v5.48.2

- 修复: 控制台页面前端脚本初始化时因 `closeMsgLightbox` 顶层引用触发 JS 暂时性死区（TDZ）错误导致脚本中断、小说/数据库/网盘数据全部不显示的问题——改为延迟引用，恢复控制台完整加载。

## v5.48.1

- 修复: 消息记录 MySQL 写入可能因字段超长触发 DataError 的问题——写入前按表列宽截断各 VARCHAR 字段，异常详情日志加长便于定位。

## v5.48.0

- 新增: 书旗小说整本下载自动获取年费 VIP 用户 ID（书评扫描，缓存 UID 仅失效时更新），下载改为 VIP 批量包链路（downloadbatch/freedownurl），突破 20 章试读限制；VIP 链路失败自动回退整本压缩包。

## v5.47.12

- 修复: 自己发送的图片在消息记录里显示为"[图片]"文字的问题——发送时记录图片占位，收到 QQ 回显后用真实图片地址补全；原始数据弹窗放大显示。

## v5.47.11


- 修复: 自己在群/私聊里发的消息（管理员本人及网页发送回显）不再计入未读红点；回显消息按已发送消息 ID 识别并去重。

## v5.47.10


- 修复: 消息记录/置顶/备注等 MySQL 读写全部使用字典索引，但连接是元组游标，配置真正启用后读取必然 TypeError；全部改为按列位置访问，并兼容两种游标。
- 修复: 消息表旧结构（utf8 字符集或缺列）写入报 DataError；启动时自动转为 utf8mb4 并补齐缺失列。
- 新增: 消息历史图片点击可放大查看（全屏预览，点击遮罩或 Esc 关闭）。

## v5.47.9


### 修复

- 消息记录：修复 MySQL 数据库配置从未注入的问题（AstrBot Context 无 config 属性，getattr 恒为 None），导致置顶/备注/消息记录只保存在进程内存、重载后全部丢失；现在插件真实配置（含 database_settings）显式传入，置顶等元数据真正持久化到 MySQL。


### 修复

- 消息记录：会话排序时间戳 last_ts 可能被 QQ 官方回推/补推的历史消息时间戳或数据库恢复覆盖而倒退，导致会话从列表最前突然掉到后面；last_ts 改为永不倒退（取旧值与新值的较大者）。
- 消息记录：QQ 官方回推机器人自己消息时不再重复追加记录（按 message_id 去重），避免消息历史重复。


### 修复

- 消息记录：群信息刷新被 QQ 官方限流（接口调用超过频率限制，code 100017）导致 ServerError 刷屏；批量刷新增加逐个间隔、失败冷却 60 秒、缓存过期阈值放宽至 600 秒。


### 修复

- 消息记录：置顶后列表立即刷新不生效（写入缓存缺少 global 声明，内存缓存未更新）；恢复误删的会话头像尺寸样式（40px）。


### 修复

- 消息记录：发送图片+文字时，图片与文字分两条消息发送后，文字消息未写入消息记录导致网页只显示图片；现补发文字消息也会记录，图片消息记录为 [图片]（带文字时 [图片] 文字）。


### 调整

- 消息记录：会话列表置顶改为右键菜单操作（置顶/取消置顶、刷新群信息），移除悬停置顶按钮；置顶会话使用深色背景标识（同 QQ 风格），不再显示 📌 图标。


### 修复

- 消息记录：修复网页发送图片+文字时图片消失的问题（QQ 官方不支持图文混排，改为图片与文字分两条发送）；媒体模式新增“媒体说明”文本补发。

- 消息记录：修复网页发送成功后控制台报 Cannot set properties of null 的脚本错误（判空清理引用/图片占位元素）。


### 调整

- 消息记录：会话列表未读红点位置调整到最后一条消息右侧（时间下方），与 QQ 会话列表一致。


## v5.47.1

### 修复

- 消息记录：置顶、备注、私聊昵称全部持久化到 MySQL 元数据表，不再写入插件目录内的本地 JSON 缓存文件；插件更新/重载不再丢失置顶数据。
- 运行状态数据库：数据库名（database_name）未填写时自动使用数据库用户名，host/端口/账号/密码齐全即视为已配置，不再静默降级。
- 运行状态数据库：目标数据库不存在时自动创建（含 utf8mb4 字符集），消息记录初始化时同时确保元数据表存在。
- 消息记录：启动恢复时置顶/备注/昵称中的会话即使暂无消息也保留显示，置顶会话不因空消息丢失。


## v5.47.0

### 新增

- 消息记录：会话列表新增未读红点，收到新消息时显示数量，打开会话后自动清零，与 QQ 一致。
- 消息记录：置顶会话按最新消息时间排序，置顶始终排在前面，未置顶按最新消息时间排序。

### 修复

- 消息记录：机器人自动回复的消息不再遗漏，发送挂钩改为包装 AstrBot 事件发送入口，覆盖所有插件回复路径。

## v5.46.1

### 修复

- 消息记录：QQ 官方单聊/群聊事件昵称丢失问题，事件本身带有的用户昵称现在可以正确显示。
- 消息记录：机器人自动发送的消息不再遗漏，发送挂钩兼容更多 AstrBot 版本。
- 消息记录：网页右键复制消息在 HTTP 环境下可正常使用。

## v5.46.0

### 新增

- 消息记录、置顶、群备注与私聊昵称持久化到 MySQL：消息记录写入新表 `mantou_message_records`（自动建表、带会话/时间索引），置顶/备注/昵称写入 `mantou_runtime_state`（namespace 隔离）。
- 插件启动或重载时从数据库恢复会话与最近消息，置顶会话重启后不再丢失。
- 未配置数据库时消息记录接口静默返回空，不影响其他功能。

### 修复

- 修复置顶会话重启后不显示的问题：聊天列表自动从数据库补齐不在内存的会话。

---

## v5.45.11

- 书旗小说：支持 QQ 卡片分享 `t.shuqi.com/v2/query/书籍ID/章节ID` 链接识别。

## v5.45.10

- 消息记录：QQ 官方无用户详情接口，私聊昵称改用可读兜底名（用户+后6位），不再反复请求无效接口。

## v5.45.9

- 消息记录：输入框改为 QQ PC 风格，图片与引用条仅在存在时显示。

## v5.45.8

- 消息记录：私聊昵称支持历史会话补查并持久化，昵称重启后仍可显示。

## v5.45.7

- 消息记录：左侧会话列表支持置顶/取消置顶，置顶会话优先显示。

## v5.45.6

- 消息记录：修复私聊用户昵称获取不到的问题，通过 QQ 官方用户详情接口补查昵称。

## v5.45.5

- 消息记录：修复右键菜单被立即隐藏，机器人发送的消息接入记录，输入框改为 QQ 风格并支持图片内嵌。

## v5.45.4

- 消息记录：修复被动发送 msg_id 过期问题，窗口收紧至 100 秒并自动降级主动推送重试。

## v5.45.3

- 消息记录：修复右键菜单位置偏移，菜单显示在鼠标右下角。

## v5.45.2

- 消息记录：修复消息不实时刷新（轮询误加载更早消息），页面放大，退出登录移入管理员菜单，移除刷新状态按钮。

## v5.45.1

- 消息记录：撤回他人消息与禁言仅机器人是群管理员时显示，撤回后隐藏原文只显示已撤回。

## v5.45.0

- 消息记录：QQ 电脑版风格排版，右键头像可 @提及/禁言，右键消息可引用/复制/撤回/多选，支持多选批量撤回。

## v5.44.14

- 消息记录：群信息自动更新（超过 5 分钟未刷新自动标记并批量刷新），消息轮询时同步更新群成员数量显示。

## v5.44.13

- 消息记录：优化普通群被动发送逻辑，仅使用 2 分钟时效内的近期 msg_id，并修复时间戳解析。

## v5.44.12

- 消息记录：修复普通群发送失败（QQ 官方被动消息需近期 msg_id），默认优先被动发送并给出明确提示；错误详情写入日志。

## v5.44.11

- 消息记录：会话详情头部显示群成员数量，刷新群信息后同步更新。

## v5.44.10

- 消息记录：修复快速点击发送或回车导致重复发送的问题。
- 消息记录：机器人发送的消息（下载小说提示、识别结果等）也会显示在消息记录中。

## v5.44.9

- 消息记录：发送框支持粘贴图片，图片与文字可在同一条消息发送（QQ 官方富媒体消息）。

## v5.44.8

- 消息记录：QQ 官方表情标签（<faceType...>）解码为可读表情文本，历史与列表中的旧表情消息也会清洗。
- 修复消息记录页引用发送失败：引用改用 QQ 官方 REFIDX（从 message_scene.ext 提取）并带 ignore_get_message_error，找不到 REFIDX 时回退完整消息 ID。
- 聊天列表与历史读取本地缓存文件改为进程内存缓存，群信息刷新高峰期不再卡顿。

## v5.44.7

- 消息记录：群备注弹窗新增删除备注；历史消息按 Elaina 方式从事件附件提取图片链接，图片消息可正常显示。
- 成员头像地址对齐官方原图接口，会话列表与消息气泡头像更清晰。
- 修复禁言弹窗样式丢失导致无法弹出的问题，禁言改为弹窗选择时长并细化失败提示。
- 历史消息轮询优化：无新消息时不重复渲染，群角色查询限流，消息加载更快。

## v5.44.6

- 消息记录页界面重做为 QQ PC 端风格：左侧会话列表（圆形头像、名称+时间、最后消息预览），右侧聊天窗口（蓝色气泡自己/白色气泡他人、时间分隔线、QQ 风格配色）。
- 修复新消息会话不置顶的问题：会话列表改为按数字时间戳倒序排序，收到新消息的会话自动跳到第一条。
- 会话列表时间 QQ 化显示（今天显示时分、昨天、更早显示日期），消息窗口时间显示时分。

## v5.44.5

- 修复消息记录页引用消息只显示 ID 不显示内容的问题：收到消息时提取引用消息 ID，历史接口返回被引用消息的昵称和内容，气泡中直接展示被引用内容。

## v5.44.4

- 消息记录页发送框支持回车发送消息，Ctrl+Enter 换行；中文输入法选字回车不会误发送。

## v5.44.3

- 修复消息记录页用户头像加载失败时显示破图的问题：头像图片加载失败自动回退显示昵称首字，不再留空或显示错误图标。
- 群聊会话列表头像需要先在“群备注”中填写群号，QQ 官方接口不返回群头像地址，填群号后可显示群头像。

## v5.44.2

- 修复消息记录页群聊显示问题：群名自动从 QQ 官方群信息接口刷新（含后台批量刷新），不再显示为最后发言成员昵称。
- 修复消息时间戳全部为空的问题：兼容 QQ 官方 ISO 格式时间戳（如 `2026-08-24T23:14:48+08:00`），聊天列表与消息窗口时间正常排序展示。
- 消息成员资料提取群角色（owner/admin/member），消息气泡显示群主/管理/群员标签；成员头像改用会话 appid 生成 QQ 头像地址；消息中的 `<@openid>` 提及按已缓存成员昵称渲染。

## v5.44.1

- 修复消息记录页面无法获取会话、发送消息提示“QQ官方平台未加载”的问题：补齐 main.py 中消息记录事件挂钩的安装调用，机器人收到的群聊/私聊消息现在会进入消息记录缓存，网页可读取并回复。

## v5.44.0

- 帮助网页新增“消息记录”页面，接入 QQ 官方机器人消息：左侧会话列表支持全量、已备注、群聊和私聊过滤与搜索；右侧消息窗口展示群聊/私聊历史、成员昵称、引用、撤回标记和媒体预览。
- 支持在网页直接发送文本、Markdown、富媒体、ARK 模板和图文卡片消息，可选择默认、被动（msg_id）、主动、自定义 msg_id 和自定义事件 ID 发送方式，支持引用回复。
- 消息气泡提供撤回、引用、禁言（10 分钟至 1 天或自定义）、查看原始数据操作；群聊支持刷新群信息、设置群备注和群号。
- 机器人收到的 QQ 官方群聊/私聊消息在插件进程内缓存供网页查询，会话上限 200 个、每会话最多 500 条；群备注与群号保存在 `功能文件/下载缓存/消息记录缓存.json`。
- 全部消息接口使用控制台会话鉴权，未登录不可读取或发送；发送、撤回和禁言失败只返回固定短提示，详细原因写后端日志。

## v5.43.0

- 网盘配置改为 UC、夸克、百度三个顶部页签，每次只展示一个网盘页面，保留原有账号、目录、群选择和主网盘操作。

## v5.42.3

- 管理台右上角显示当前登录账号、控制台角色和插件管理员白名单数量，管理员入口改为可展开的真实身份摘要。
- 网盘配置页增加 UC、夸克和百度上传目录的直接保存入口，夸克账号资料刷新、账号管理和 Cookie 写入入口保持可用。
- 网盘登录态继续只接受写入，页面仅展示已保存状态和账号资料，不返回 Cookie、密码或会话值。

## v5.42.2

- 重整帮助网页的网盘配置页面，移除重复的顶部页面标签，只保留左侧栏导航。
- UC、夸克和百度网盘集中在一个独立工作区，保留账号、目录、主网盘和群账号选择操作。

## v5.42.1

- 重做帮助网页的“小说功能”页面，改为总开关、管理员测试模式和双列平台控制工作区。
- 平台状态、已开启数量和全局入口状态实时展示，保留原有开关接口与数据库写入行为。

## v5.42.0

- 帮助网页控制台接入真实配置写入，管理员可直接修改白名单、帮助网页账号密码、网盘目录、百度后台备份状态和数据库连接配置。
- 网盘配置页新增账号添加、删除、夸克资料刷新、按群选择账号；账号仍按平台和群隔离，Cookie 不回显。
- 增加 QQ阅读 `ywguid/ywkey` 登录态的网页保存和清除入口，敏感值只写入后端。
- 配置请求改为先完整校验再写入，避免部分字段保存成功；控制台前端补齐保存失败和会话失效处理。

## v5.41.1

- 分离帮助网页登录页和管理员控制台；未登录请求只返回独立身份验证页面，不暴露后台侧栏、页面标签和功能结构。
- 会话过期或接口返回未授权时重新加载独立登录页，避免登录框再次嵌入控制台。

## v5.41.0

- 帮助网页改为账号密码登录，登录成功后使用 HttpOnly 会话 Cookie；访问地址不再携带令牌或其他登录凭据。
- 在 `_conf_schema.json` 的 `help_web_settings` 中新增 `help_web_admin_username` 和 `help_web_admin_password`，账号密码可在 AstrBot 插件配置中修改，密码不返回网页和日志。
- 增加网页退出登录、会话过期和未配置账号密码提示；控制台接口只接受有效会话。

## v5.40.1

- 帮助控制台新增页面进入、卡片错峰、侧栏高亮和开关反馈动画，并尊重系统的减少动态效果设置。
- 页面标签、概览卡片和快捷入口改为只读展示，页面导航仅保留左侧边栏。

## v5.40.0

- 将帮助控制台拆分为控制台、机器人配置、小说功能、网盘配置、运行状态、帮助指令和系统设置独立页面。
- 移除没有网页接口的事件、群组、用户、统计、日志和插件假入口；小说开关与主网盘切换继续使用真实后端接口。
- 增加访问令牌失效专用提示和重新验证入口，修复页面只显示无效令牌黄条的问题。

## v5.39.2

- 按 QQ 机器人后台参考图重新制作帮助控制台，改为固定侧栏、五项配置标签和左右两列配置工作区。
- 将小说平台开关、运行状态、网盘摘要和网页连接配置放入对应卡片，保留原有安全摘要与移动端适配。

## v5.39.1

- 按 QQ 机器人后台参考风格重新调整帮助控制台 UI：增加白色顶栏、机器人身份侧栏、浅紫色选中态、配置分区标签和轻量二次元头像装饰。
- 保留原有小说开关、网盘切换、运行状态和访问令牌逻辑，仅调整页面布局、配色和移动端适配。

## v5.39.0

- 重做帮助网页为管理员控制台，提供总览、小说功能开关、管理员测试模式、主分享网盘切换、网盘配置摘要和服务器运行状态面板。
- 控制台适配手机和电脑，所有写入接口使用独立访问令牌；页面不展示 Cookie、Token、数据库地址或密码。
- 新增 `help_web_admin_token` 可选配置，留空时自动生成随机令牌；原有文字帮助、回调菜单和自动服务器 IP 访问方式保持不变。

## v5.38.0

- 帮助网页默认自动获取服务器 IPv4 并启动，无需填写域名；发送 `帮助` 即可看到网页版入口。
- 保留自定义域名和反向代理配置，补充服务器位于 NAT 或容器网络时的端口映射说明。

## v5.37.0

- 新增响应式帮助网页：管理员在插件配置填写帮助网页域名后，插件启动本地网页服务，帮助菜单增加“打开网页版帮助”按钮；原有文字帮助和 QQ 官方回调菜单继续保留。
- 帮助网页支持手机和电脑访问，并提供分类导航和关键词搜索；监听地址与端口可在 `help_web_settings` 中配置。

## v5.36.0

- 新增夸克账号管理：管理员私聊 `夸克账号` 可查看账号数量、名称和脱敏手机号；资料接口未提供时显示“未获取”。
- 新增 `删夸克N` / `删除夸克N` 删除指定夸克账号，删除后自动整理群账号选择并至少保留一个账号；插件配置中的账号1不能直接删除。
- 夸克扫码或 Cookie 保存成功后自动尝试更新账号资料，重复扫码仍按稳定身份更新原账号。

## v5.35.0

- 夸克扫码登录按 `__uid`、`b-user-id` 或 `__kps` 判断账号身份；重复扫码同一账号只更新 Cookie，不再新增账号序号。

## v5.34.2

- 修复夸克扫码成功后缺少 `__puus` 登录态的问题；账号信息接口成功后按网页端参数调用登录态刷新接口，并合并返回的 Cookie 后再保存。

## v5.34.1

- 修复夸克扫码登录成功后因重定向响应或 JSON 登录态未被收集，导致提示 `missing_required_fields` 的问题；现在合并重定向链、响应头、相关夸克域名 Cookie 和接口返回的 Cookie 字段。

## v5.34.0

- 新增 UC、夸克、百度网盘多账号管理：插件配置 Cookie 作为账号1，管理员继续保存同平台 Cookie 会追加账号，并在网盘状态中显示账号数量和当前账号。
- 新增按群独立选择网盘账号的指令 `UC换N`、`夸换N`/`夸克换N`、`百换N`/`百度换N`；未设置时默认账号1，三个网盘选择互不影响。
- 小说上传续传任务保存各网盘账号序号，插件重载恢复和百度后台备份继续使用原任务账号，避免群间账号串用。

## v5.33.1

- 优化 QQ 阅读正文下载日志：按批次记录响应大小、网络请求耗时和解包解密耗时，便于定位慢在网络还是本机解密；保持每批 200 章和动态并发策略不变。

## v5.33.0

- 新增小说下载缓存每日清理任务：保留插件启动或重载时清理上次遗留缓存的机制，运行期间按服务器本地时间每天零点删除文件日期早于当天的小说 TXT，当天文件继续保留。
- 每日清理继续跳过正在上传、后台备份或等待续传的文件；插件停止或重载时会取消并等待定时任务结束，避免遗留重复任务。

## v5.32.3

- 优化 QQ 阅读正文下载：网络请求信号量与解密信号量分离，释放网络并发槽位；按当前 `fuid` 和动态密钥池缓存正文解密材料，密钥池刷新时自动失效缓存。

## v5.32.2

- 将知乎小说更名为盐言小说，保留 `story.zhihu.com` 分享链接和现有正文接口。
- 删除旧的 `知乎分享恢复客户端.py` 及本地恢复分支，正文失败仅重试当前盐言章节；同步更新入口、开关、帮助菜单、欢迎列表、README 和模块文件名。

## v5.32.1

- 修复菠萝包详情字数获取：使用接口返回的 `charCount` 作为书籍字数，不再显示未知。

## v5.32.0

- 新增数字撤回禁言跨群累计：同一用户在不同群触发数字撤回时按累计次数递增禁言时长，不再从单群 0 开始重新计算。
- 新增菠萝包 `/b/{书籍ID}` 短链接支持，修复菠萝包卡片跳转地址无法解析书籍编号导致的下载失败。

## v5.31.1

- 修复晋江目录中 `isvip=0` 但 `islock` 明确锁定的章节被误请求后导致整本下载失败的问题。
- 兼容晋江 JSON 分享卡片中转义的 `https:\/\/` 链接。

## v5.31.0

- 新增晋江小说模块，支持晋江链接/分享卡片、搜索、详情、目录和公开正文下载。
- 仅请求接口确认可公开读取的章节，跳过付费章节；复用异步 HTTP 会话并发下载，任意公开章节缺失时停止合成。
- 排除目录明确标记为锁定的章节，并兼容 JSON 卡片中 `https:\/\/` 形式的晋江链接。
- 晋江接入找书聚合、`选N` 下载、小说状态开关、帮助菜单、链接撤回白名单和统一小说网盘出口。

## v5.30.3

- QQ阅读普通正文批量章节数调整为每批 200 章，继续使用最多 100 路动态并发和复用的异步 HTTP 会话。

## v5.30.2

- QQ阅读正文解密替换为 `D:\\Tencent\\Downloads\\qqread\\qqread` 中 `DecryptChapter` 的完整逻辑，按参考实现处理二进制密钥池、FUID/正文校验、AES/DES/CTR 组合、宽松分块填充和 gzip 正文。

## v5.30.1

- QQ阅读普通正文调整为每批 30 章、最多 100 路动态请求并发，继续复用同一个异步 HTTP 会话；正文解密仍使用受限 CPU 并发，不创建 100 个解密线程。

## v5.30.0

- 小说下载回复流改为每本书独立的异步后台任务，移出消息事件的等待生命周期；不设置全局下载队列或共享并发闸门，QQ 阅读下载期间其他平台、同平台其他书籍和后续消息可同时处理。
- 插件重载或停止时统一取消并等待仍在运行的小说下载任务，避免后台任务遗留。

## v5.29.3

- 修复 QQ 阅读正文全部下载成功后汇总日志键名错误导致的 `KeyError`，避免成功结果被外层误判为下载失败。

## v5.29.2

- 番茄正文改为先并发完成全部批次请求，再统一解密和合并结果；保留每批最多 1500 章、最多 5 路请求并发和 HTTP 会话复用。

## v5.29.1

- 修复知乎 `soia.zhihu.com` 和 `story.zhihu.com` 分享卡片被群管卡片撤回规则误撤回的问题，白名单判断继续优先于卡片撤回。

## v5.29.0

- 新增宜搜、米读、猫眼、酷我、酷匠、连城和菠萝包七个独立小说下载模块，分别接入各自的搜索、详情、目录、正文、TXT 和统一网盘出口。
- 七个平台均使用独立异步 HTTP 会话与连接复用；宜搜使用分页目录和 PyCryptodome DES-CBC 解密，酷匠使用 PyCryptodome AES-CBC 解密并 gzip 解压，其余平台按接口格式清理或读取正文。
- 七个平台接入找书聚合、`选N` 下载、小说状态列表、全局/平台独立开关、帮助菜单和链接撤回白名单；补搜联想与普通聚合保持一致。

## v5.28.0

- 得间小说改用 `batchDownloadChapteres` 批量获取整本章节正文地址，不再为每章重复请求章节清单；正文下载继续复用异步 HTTP 会话，章节授权按平台协议执行，正文使用现有库解密并发处理，缺少任意批量地址时停止合成。
- 统一小说平台后端日志中的书籍编号、进度、百分比、成功、失败、并发数、重试次数、错误和文件等字段为中文，保留请求参数和业务逻辑字段不变。

## v5.27.2

- 点众接口响应兼容参考源码的明文回退：加密 `data` 解包失败时保留 `data_plain` 并继续尝试解析明文 JSON，避免错误响应或未加密响应被直接丢弃。

## v5.27.1

- 严格对齐得间参考源码的单章客户端生命周期：每章和该章的重试复用一组新生成的会话参数，避免整本复用同一个 `usr`；全书仅复用异步 HTTP 会话和连接池，每章按参考逻辑重新取得章节地址清单。

## v5.27.0

- 得间正文改为完全参考 `D:\新建文件夹\source-code-master\source-code-master\得间\得间.py` 的单章流程：每章独立定位正文地址、申请授权、下载和解密；`batchDownloadChapteres` 仅作为章节地址清单入口，不再作为批量正文接口。
- 得间下载复用同一个 `aiohttp` 异步 HTTP 会话，按章节数动态调度最高 500 路并发；删除旧的 7 章正文地址预取，单章失败独立重试。
- 得间正文继续使用仓库内自定义 CTR 协议，保留 `PyCryptodome` 异或和 `gmpy2` 模幂库加速；该协议不是标准 AES，避免替换为不兼容的 AES 解密。

## v5.26.9

- QQ 阅读普通正文调整为每批 200 章、最多 5 路并发；继续复用同一个异步 HTTP 会话，正文解密继续使用 PyCryptodome 库并通过受限 CPU 队列执行。

## v5.26.8

- 修复 QQ 阅读批量正文在高并发突发请求下返回空包或部分包导致缺章：保留 300 路连接池/动态上限，正文请求按网关实测以 5 路安全并发调度，失败窗口继续复用异步会话并重试。
- 增加 QQ 阅读正文首轮、缺章重试和密钥池刷新的受控 DEBUG 汇总，区分响应缺失、解密缺失和 HTTP 请求失败，不输出响应原文或登录信息。

## v5.26.7

- 番茄正文调整为每批最多 1500 章、最多 5 路动态并发；继续使用异步 HTTP/2 会话和连接复用，正文解密仍使用库实现并单独限流。

## v5.26.6

- 将来源为 QQ 阅读的微信小程序/卡片分享加入群管撤回白名单，兼容可见卡片文本和 JSON 消息段；其他卡片仍按原规则处理。

## v5.26.5

- 番茄正文继续使用每批 300 章的批量接口和复用的异步 HTTP/2 会话，动态批量请求并发上限调整为 50；正文解密仍单独按 CPU 并发限制执行。

## v5.26.4

- 修复百度阅读 `is_yuedu_source=1` 分享页被误当作旧数字书号的问题；按 `doc_id` 接入出版源详情、目录和正文接口，并在正文页不可用时停止生成部分 TXT。

## v5.26.3

- 修复番茄下载流程在详情、目录请求结束后提前关闭 HTTP/2 会话的问题；正文批量请求现在继续复用同一异步客户端，正文完成后再关闭连接。

## v5.26.2

- QQ阅读普通正文调整为每批 30 章、最多 300 路异步并发；继续复用同一 `aiohttp` 会话，批量正文每批只请求一次。
- QQ阅读正文包继续使用 PyCryptodome 的 AES/DES/CTR 解密，解密任务单独受 CPU 并发限制，不创建 300 个解密线程。
- 番茄正文调整为每批 300 章；每批通过批量接口一次请求，并继续复用 HTTP/2 异步会话和 PyCryptodome 解密。
- 复核番茄 `1021002` 日志：该响应表示正文业务源当时返回“书籍不存在或已停止合作”；同一书籍当前 300 章批次已实测返回完整章节，未发现批量或解密代码故障。

## v5.26.1

- 修复百度阅读 `boxnovel/yuedu/wapdetail` 分享页只有 `doc_id` 时无法识别的问题，改为异步读取服务端详情并提取正式 `novel_book_id`，同时使用百度阅读移动端请求头获取正文地址。
- 修复小米小说搜索接口失效问题，改用当前 dushu 搜索、详情、目录和正文链路，并兼容旧 reader.browser 分享链接。

## v5.26.0

- 新增百度小说和小米小说平台，模块统一放在 `功能文件/管理功能/小说功能/小说/`，支持链接识别、详情、目录、正文、完整 TXT 校验和当前主网盘分享。
- 找书聚合新增百度小说和小米小说，平台开关、欢迎列表、卡片撤回白名单和帮助菜单同步更新。
- 兼容小米当前 `/api/v2/search` 入口并过滤无关推荐；百度正文严格使用 AES 库解密，后台备份失败时保留待恢复缓存。

## v5.25.0

- 新增全局 `开小说` / `关小说` 总开关，状态对全部群聊生效；关闭后拦截所有小说链接下载、找书搜索、翻页和选书下载。
- 重新 `开小说` 不改动各平台原有的单独开关状态；找书选书下载同步遵守各平台开关。

## v5.24.4

- 夸克网盘完成分片时兼容预上传接口返回的已 Base64 回调字符串，避免再次编码导致对象存储回调失败；原始 JSON 回调与对象回调路径保持不变。
- 对 OSS 已接收的 `203 CallbackFailed` 响应不再重传分片，改为继续调用已有上传任务的完成接口确认结果。

## v5.24.3

- 七猫小说 App 批量正文下载调整为每批 50 章、最多 200 个批次异步并发；连接池和批量失败后的单章回退同步使用 200 个请求槽位。
- 七猫正文继续使用已声明的 `pycryptodome` 库执行 AES-CBC 与 PKCS7 填充解密，不保留手写密码算法。

## v5.24.2

- QQ 官方群欢迎菜单的小说列表改为统一展示“分享链接或分享卡片”，补齐 QQ 浏览器小说。
- 小说列表会按当前小说功能开关动态过滤，已关闭的平台不再展示；全部关闭时明确提示当前没有开启的小说下载功能。

## v5.24.1

- 更新 QQ 阅读请求核心：`/sk`、目录和正文批次按最终请求参数动态生成网关签名，已保持现有异步会话复用与数据库登录态加载方式。
- 正文包优先按 `info.txt` 中的章节 ID/UUID 匹配密文文件，兼容服务端文件名调整；详情异常响应不再触发空值比较错误。
- 出版书授权保留 App 登录包格式并启用 `onlycteb=1`；EPUB 多正文文件按 OPF spine 顺序合并，不再只取单个最大文件。
- QQ 阅读不新增本地账号池、HAR、Cookie 或密钥缓存文件；`ywguid/ywkey` 继续仅保存到 MySQL 运行状态表，动态密钥池仅留在进程内。

## v5.24.0

- 新增 QQ 浏览器小说支持：可搜索书籍、读取详情和目录，并按番茄格式下载完整正文 TXT。
- 只接入 QQ 浏览器公开阅读接口，不引入源项目的 EPUB、封面、Cookie、代理或独立上传逻辑。

## v5.23.9

- 调整知乎分享下载详情与番茄小说一致：字数仅显示“万字”或“字”，TXT 头部不再写入起始章节编号。

## v5.23.8

- 知乎单篇实际字数达到一万时同时显示万字换算与精确字数，例如 `1.2万字（12,358字）`。

## v5.23.7

- 知乎单篇下载提示改在正文获取完成后发送，按实际正文显示精确字数，不再显示“未知”。
- 删除知乎下载提示和 TXT 头部中的“下载范围：当前分享内容”。

## v5.23.6

- 知乎分享链接改为只下载链接中的当前分享内容：不再遍历整本专栏目录或请求其他章节。
- 正文只请求分享链接对应的 `q=专栏ID&a=章节ID`；成功后单章合成 TXT，保留同一章节的标题校验、重试和恢复流程。

## v5.23.5

- 知乎专栏下载提示和 TXT 头部新增分享章节与“下载范围：整本专栏”，明确分享章节与整本专栏标题的关系。
- 外部正文提取结果包含标题时，必须与知乎目录章节标题一致；标题错配的正文会被拒绝并进入既有恢复流程，TXT 章节标题固定采用官方目录标题。

## v5.23.4

- 修复书旗 `t.shuqi.com/book/{book_id}` 书籍链接未进入识别器的问题。

## v5.23.3

- 修复跨群禁言把异步 `get_group()` 协程对象记录成群号的问题，过滤污染群列表。
- QQ 官方群按 `user_openid` 维护各群独立的 `member_openid` 映射，只对已确认在目标群的成员执行禁言或解禁；没有目标群成员映射时静默跳过。
- 移除官方群禁言失败后继续调用通用回退接口的逻辑，降低重复请求和错误刷屏。

## v5.23.2

- 修复得间付费章节被当作网络失败反复重试的问题。
- 下载前读取 App 返回的实际可下载章节数和最大可下载章节号；发现目录包含未购买章节时直接停止，不再启动 700 路正文请求，也不生成部分 TXT。
- 复用下载前已获取的批量下载清单，避免正文地址预取阶段再次请求同一清单。

## v5.23.1

- 优化找书速度：首轮只请求每个平台 15 条候选，并行限制单个平台最长等待 12 秒。
- 书旗联想改为结果不足 5 条时才请求，联想补搜改为多词并发。
- QQ 阅读详情和目录预检复用同一个异步连接池，并加入单候选超时；重复关键词 60 秒内直接使用缓存结果。

## v5.23.0

- 建立跨平台找书排序：先按书名/作者相关性、实际匹配字数和覆盖率筛选，再保留平台原始搜索位次作为弱排序信号。
- 平台原始顺序只在相关性相近时参与排序，随后才比较目录可用性、平台优先级、评分、热度和跨平台共识，避免无关结果或热度数值压过文字匹配。

## v5.22.0

- 优化自动找书排序：每本候选分别比较书名和作者，按实际匹配字数优先排序。
- `找赵心姚` 可优先命中作者，`找斗破` 可优先命中书名，不再对整批结果强行判定单一搜索类型。
- 同等匹配结果使用平台优先级和各平台搜索接口原始顺序稳定排序。

## v5.21.7

- 修复同一条群消息被重复投递时重复撤回、重复递增广告禁言的问题。
- 增加按群号和消息 ID 的并发去重及 120 秒完成缓存，不影响不同消息的正常禁言递增。

## v5.21.6

- 解除成员禁言后静默处理，不再发送 `@用户 已解除 X 个成员的禁言` 回复。

## v5.21.5

- 修复 QQ 官方消息的 `message_str` 未包含末尾数字时，数字撤回提前返回导致群号消息未触发的问题。
- 数字撤回现在同时检查事件文本和消息段文本，继续保持 9–12 位独立数字规则。

## v5.21.4

- 修复跨群禁言对 QQ 官方群错误执行成员预检、导致所有 OpenID 群都被跳过的问题。
- QQ 官方群改由官方成员禁言接口直接处理；目标不属于该群时只记录受控日志并跳过，不再向用户显示失败统计。
- OneBot 数字群继续使用成员存在预检。

## v5.21.3

- 修复 QQ 官方群使用 OpenID 执行“解”时被 `get_group_member_info` 误判为成员不存在的问题。
- QQ 官方群当前命令目标来自有效 @ 时直接调用官方成员禁言接口，OneBot 数字群仍保留成员存在检查。

## v5.21.2

- 隐藏手动禁言回复中的跨群同步数量和失败数量提示，保留跨群禁言同步及后端日志。

## v5.21.1

- 修复跨群禁言在目标群没有该成员时仍调用禁言接口的问题。
- 成员查询没有返回明确成员标识时按“不在群内”处理，避免因适配器空响应产生误禁言请求。

## v5.21.0

- 新增 QQ 官方机器人群权限检查，使用 `bot_state` 的 `member_role` 判断机器人是否为群主或管理员。
- QQ 官方机器人不是群主/管理员或权限查询失败时，撤回、成员禁言和跨群禁言静默跳过。
- 权限状态按群短时缓存，避免每条消息重复请求官方接口。

## v5.20.1

- 修复番茄分享链接详情接口偶发返回业务错误时被误判为空详情的问题。
- 详情请求按业务成功状态重试，连续失败时停止下载准备，避免显示占位书名和未知作者。
- 新增番茄详情重试、非字典响应和占位信息防护回归测试。

## v5.20.0

- 删除只能查询状态、不能直接开关的“全量消息”功能及相关帮助、配置和文档入口。

## v5.19.2

- 撤回广告消息成功禁言后只保留一条提醒：@发送者、请勿发送此类消息、如果是小说请联系群主。
- 删除下方重复的管理员/发送者撤回详情通知。

## v5.19.1

- 撤回广告消息后保留禁言结果，删除重复的“请勿再发送此类消息”提示。
- 撤回通知首行新增“如果是小说请联系群主”，并保持换行格式。

## v5.19.0

- 删除无用的网页群文件清理模块及其指令、Cookie 保活和群列表维护入口。
- 新增成员禁言/解禁跨群同步，当前群成功后会并发同步到机器人所在的其他群。
- 全量消息改为明确提示：插件只能查询官方状态，不能直接开关。

## v5.18.6

- 修复 QQ 官方平台重载时清理旧欢迎回调后未重新安装，恢复新成员欢迎消息。

- 修复 QQ 官方收到 `group_member_add` 后只进入解析器、未进入欢迎回调的问题。
- 恢复 `on_group_member_add` / `on_group_add_robot` 回调安装，确保事件继续投递到欢迎消息。

## v5.18.4

- 更新入群欢迎的小说列表，补充知乎小说和塔读小说。

## v5.18.3

- 恢复 QQ 官方入群欢迎提示及“使用教程 / 小说列表 / 如何下载”按钮。
- 修复欢迎事件桥未调用成员加入监听同步、欢迎按钮回调未进入分发的问题。

## v5.18.2

- 修复 QQ 官方帮助桥始终识别不到已加载平台的问题：兼容 AstrBot 当前适配器返回的 `meta().name="qq_official"`。

## v5.18.1

- 修复 QQ 官方新人欢迎订阅位错误：由 `GROUP_AND_C2C_EVENT (1<<25)` 改为官方 `GROUP_MEMBER_EVENT (1<<24)`。
- 诊断日志改为明确显示 `group_member_event`，避免“订阅成功但平台不下发新人事件”的误判。

## v5.18.0

- 新增小说上传断点续传任务清单，主网盘上传前持久化 TXT 路径、网盘和任务状态。
- 插件重载时不再仅按 PID 判断 `.uploading`，会保留待上传缓存并自动恢复主网盘上传。
- 主网盘失败、百度后台备份未完成时保留缓存，避免更新插件期间出现“上传文件不存在”；所有小说平台共用同一套缓存删除保护。

## v5.17.1

- 删除全员禁言及跨群全员禁言命令和适配器调用。
- QQ 官方机器人只保留官方成员禁言接口；全员禁言状态可查询，但没有公开写入接口。
- 同步帮助菜单、README 和回归测试，避免再次调用 `set_group_whole_ban`。

## v5.17.0

- 新增广告撤回递进禁言：同一群同一成员第 1 次 3 分钟、第 2 次 10 分钟、第 3 次 30 分钟、第 4 次 1 天、第 5 次及以后 30 天。
- 撤回成功后自动调用成员禁言接口，并 @发送者提示“请勿再发送此类消息”。
- 支持 OneBot QQ 号和 QQ 官方机器人 `member_openid`，禁言失败时只记录后台日志。
- 增加递进时长、发送者识别和自动禁言回归测试。

## v5.16.3

- 修复 QQ 官方群成员禁言命令 `禁30` 未被识别的问题。
- 支持禁言命令和数字时长直接连写，`禁30` 等同于 `禁 30`，无单位数字按天计算。
- 增加无空格禁言命令回归测试。

## v5.16.2

- 修复塔读详情字段不完整：兼容接口返回的作者字段 `bookAuthor` 和真实字数 `bookTotalSize`。
- 塔读下载提示和 TXT 头部现在会显示正确作者与字数。
- 增加塔读详情字段回归测试。

## v5.16.1

- 修复番茄长读分享链接因携带 `book_id` 参数被塔读下载入口抢先识别的问题。
- 塔读入口现在只接受塔读域名链接，明确排除番茄域名和其他平台链接。
- 增加番茄长读分享路由回归测试。

## v5.16.0

- 单成员禁言成功后改为 @被禁言成员，并提示“你已经被禁言，请联系群主说明情况”。
- 新增全员禁言快捷别名：`禁全体`、`禁言全体`、`全体禁`、`全体禁言`、`解全体`、`解禁全体`、`全体解禁`、`全体解`。

## v5.15.0

- 新增 QQ 官方单成员禁言和解除禁言：支持 `禁言 @成员`、`禁 @成员 1`、`解除禁言 @成员`、`解 @成员`；默认禁言 7 天，不带单位的数字按天计算。
- 单成员禁言按适配器分流：OneBot 使用 `set_group_ban`，QQ 官方使用官方 `POST /v2/groups/{group_openid}/restrict_chat_setting`，支持官方群成员 OpenID。
- 补齐 `开启全员禁言` / `关闭全员禁言` 当前群命令别名；现有撤回、群文件、群列表和群管功能继续统一放在 `功能文件/管理功能/群聊功能/`，小说目录未调整。

## v5.14.0

- 接入塔读游客 Token 自动续期：按服务端 `expire` / `earlyTime` 判断续期时机，优先调用 `/user/api/token/get` 获取新 Token。
- Token 续期失败时自动重新调用游客注册接口，避免旧 Token 过期后搜索和下载间歇性失效；登录态只保存在当前进程内。
- 增加 Token 续期成功和续期失败回退注册的单元测试。

## v5.13.0

- 新增塔读小说下载与找书支持，识别 `reader.tadu.com` 书籍链接和聚合搜索结果。
- 塔读详情、目录和正文使用异步 HTTP 连接复用，目录缺少地址时回退单章地址接口，正文最高 400 路动态并发。
- 将塔读 AES/DES、TDZ 容器解密和 UTF-16LE 正文解析全部合并到 `塔读小说.py`，缺章时不生成部分 TXT。
- 同步小说开关、帮助菜单、主分发和版本信息。

## v5.12.2

- 撤回通知同时提及配置管理员和被撤回消息的发送者，方便管理员定位消息来源。

## v5.12.1

- 修复撤回通知错误提及发送者的问题，改为提及配置中的群文件清理管理员；QQ 官方群使用管理员 OpenID。
- 清理撤回通知中的卡片元数据、数字、域名和链接，卡片消息统一显示为“卡片消息”。

## v5.12.0

- 将知乎下载从单篇正文改为整本专栏：通过 `km-indep-home/catalog/{专栏ID}` 分页获取真实目录，并用 `km-indep-home/.../header` 补充章节详情元数据。
- 复用异步 HTTP 会话并发提取章节正文，失败章节单独重试，按目录顺序合成并校验完整性后再生成 TXT。
- 真实正文提取服务恢复 HTTP 200 后，保留内置恢复客户端作为单章回退链路。

## v5.11.0

- 接入知乎 `story.zhihu.com/manuscript/paid_column/{问题ID}/{回答ID}` 分享链接和分享卡片。
- 优先请求知乎正文提取服务，服务不可用时使用仓库内置恢复客户端；完整正文统一生成 TXT、上传主网盘并发送“点击打开”链接。
- 新增知乎小说功能开关：`开知乎`、`关知乎`。

## v5.10.23

- 补充 `关番茄`、`关七猫`、`关书旗`、`关QQ阅读`、`关得间` 和 `关点众` 简写开关，避免未识别的关停命令落入小说下载流程。

## v5.10.22

- 优化点众小说目录并发上限为 500 路。
- 识别无广告解锁入口的会员章节并跳过无效重试，避免把会员权限限制误判为下载失败。

## v5.10.21

- 修复撤回接口返回失败结果时仍发送撤回通知的问题；只有确认撤回成功后才继续处理历史消息和通知。

## v5.10.20

- 修复同一群并发触发撤回通知时可能绕过 5 分钟限频并重复发送的问题，增加群级异步锁。

## v5.10.19

- 撤回通知改为提及被撤回消息的发送者；QQ 官方使用发送者 OpenID，OneBot 使用发送者 QQ 号，保留同一群 5 分钟限频。

## v5.10.18

- 修复 QQ阅读章节付费过滤后仍按连续序号请求正文，改用目录真实 `cid`，兼容非连续免费章节并修复缺章合成失败。

## v5.10.17

- 移除新人入群欢迎及其 QQ 官方事件桥接，不再订阅和处理 `GROUP_MEMBER_ADD`。
- 普通成员消息成功撤回后，读取消息中的发送者昵称通知发送者；同一群连续触发时按 5 分钟限频，避免重复刷屏。

## v5.10.16

- 修复 QQ 官方群成员欢迎只在客户端字段中设置订阅位、但新 WebSocket 会话仍可能沿用旧 `intent` 的问题；现在在每条 `IDENTIFY` 发包前强制同步 `GROUP_AND_C2C_EVENT`，并增加不含凭据的实际订阅诊断日志。

## v5.10.15

- QQ阅读详情识别 `free==0` 的章节付费书，仅下载目录中标记为免费的章节；付费章节不请求、不合成，并同步放宽找书预检。

## v5.10.14

- 书旗下载核心替换为 `D:\Tencent\Downloads\download_shuqi.php` 的整本下载流程：目录和下载地址并行请求，使用 M9 加密/解密、SQB/SQC XOR 解密和完整章节校验；保留现有 TXT 合成、主网盘分享与百度后台备份。

## v5.10.13

- 修复 QQ 官方交互桥在平台重载时重复包装适配器构造函数导致递归崩溃的问题；改为仅安装类级回调，并在实际平台实例加载后同步订阅状态。

## v5.10.12

- 修复官方群平台延迟加载时生命周期 handler 注册在子模块导致的内部异常；主模块接收平台加载事件，并将兜底等待延长到 180 秒。

## v5.10.11

## v5.10.10

- 修复 QQ 官方平台晚于插件初始化时欢迎桥同步过早的问题；接入 AstrBot `OnPlatformLoadedEvent`，平台实例加载后重新同步 intent、parser 和欢迎回调。

## v5.10.9

- 修复 QQ 官方群成员欢迎只检测 `GROUP_AND_C2C_EVENT` 但没有实际打开订阅位的问题；适配器初始化和登录前会同步平台与客户端 intent，确保新连接能收到 `GROUP_MEMBER_ADD`。

## v5.10.8

- 增加入群欢迎诊断：记录 QQ 官方 `GROUP_MEMBER_ADD` / `GROUP_ADD_ROBOT` 是否到达 WebSocket、是否命中 parser、是否进入回调和欢迎消息发送结果；补齐连接建立后的 parser 同步，便于定位未订阅、未下发和发送失败。

## v5.10.7

- 修复 QQ 官方入群欢迎只处理普通成员事件的问题；同时接入机器人入群事件，并同步已建立连接的网关解析器，确保新人加入和机器人刚加入群聊都能触发欢迎消息。

## v5.10.6

- 修复 QQ 阅读详情使用 `needOpenVip`、`vipdisc` 或 `vipTips` 标记会员专享时，因只识别 `vipFree` 而被误判为章节单独付费的问题；仍拦截没有明确会员标记的单章收费书籍。

## v5.10.5

- 修复 QQ 阅读图文 H5 卡片只出现在原始消息 `jump_url`、`raw_data` 或 `msg_elements` 中时无法触发下载的问题；继续沿用现有链接白名单和下载核心。

## v5.10.4

- 修复 QQ 官方入群欢迎在部分 AstrBot 版本中因空消息事件未进入插件过滤器而不发送的问题；改为网关事件直接发送，并兼容插件热重载替换旧桥接逻辑。

## v5.10.3

- 修复六个平台合成 TXT 时正文自带章节标题导致章节标题重复的问题。

## v5.10.2

- 修复 QQ 官方群成员欢迎事件桥在部分 qq-botpy 版本未暴露 `public_messages` 属性时跳过解析器注册的问题；现按 `GROUP_AND_C2C_EVENT (1<<25)` 实际 intent 位判断，并同步 SDK 的连接与状态解析器表。
- 插件加载时记录版本和源码路径，成员欢迎桥记录 intent、解析器、连接状态及收到网关事件，便于确认 AstrBot 是否已加载最新代码与平台订阅是否生效。

## v5.10.1

- 入群欢迎的“小说列表”改为逐项展示当前支持下载的番茄、七猫、书旗、QQ阅读、得间和点众小说，以及各平台可识别的链接或分享卡片形式。

## v5.10.0

- QQ 官方机器人新增入群欢迎：收到 `GROUP_MEMBER_ADD` 后主动发送提及新成员的欢迎 Markdown，并在同一条消息中提供“使用教程”“小说列表”“如何下载”原生回调按钮。
- 欢迎按钮说明直接分享书籍链接/卡片、`找关键词`、`找书 关键词`、`找书名 关键词`、`找作者 关键词`、`选N` 与翻页流程；小说列表展示当前支持的六个平台。

## v5.9.0

- QQ 官方机器人新增官方群成员加入事件桥：订阅既有 `GROUP_AND_C2C_EVENT` 后补齐旧版 qq-botpy 的 `GROUP_MEMBER_ADD` 网关解析，并将 `group_openid`、`member_openid`、可选 `user_openid` 投递到独立群成员事件模块。
- 默认仅记录受控后端日志，不自动向群内发送欢迎消息，也不进入普通消息、群管或小说下载流程。

## v5.8.12

- 得间下载启动时只获取一次正文 URL 清单，并发预取每 7 章的直链窗口，目录与正文同样最高 700 路动态并发；单章仍独立授权，直链缺失或过期时自动回退原目录定位，减少首批下载前的大量重复目录请求。

## v5.8.11

- 得间单章解密改用独立线程池，按章节数动态调度，最多同时运行 200 个解密任务，不再受 Python 默认线程池约 32 线程的限制。

## v5.8.10

- 得间自定义 CTR 解密保留原有协议输出，改用 PyCryptodome 的 C 级异或、缓存轮密钥，并使用 gmpy2 模幂解包授权令牌，减少高并发下载时的 CPU 占用。

## v5.8.9

- 得间小说下载改为参考 `得间.py` 的单章目录定位、单章授权、RSA 签名和正文解密流程，移除批量授权与 normal/speed 双通道。
- 得间单章下载使用一个复用的异步 HTTP 会话，最高 700 路并发，单章失败独立重试 3 次。

## v5.8.8

- 番茄小说下载前提示和 TXT 头部的字数统一转换为万字展示，例如 `1,126,006` 显示为 `112.6万字`。

## v5.8.7

- 得间普通版与极速版改为独立 HTTP 会话；同一通道串行完成一批 7 章的授权、正文和解密后才申请下一批，避免后续授权覆盖正在使用的短时令牌。

## v5.8.6

- 点众章节目录窗口抓取改为最高 700 路异步并发，并修正目录连接池上限。
- 点众正文改为独立 App 身份的异步通道池：首轮从 16 路开始，成功率稳定时动态提升至 60 路；限流或失败时自动降速，失败章节使用新身份低并发重试，避免 700 路突发造成大面积缺章。

## v5.8.5

- 修复得间双通道高并发下载时共享 App 会话导致批量授权返回空 token 的问题；授权按通道串行，正文请求保持异步并发和连接复用。

## v5.8.4

- 书旗、点众正文下载改为最高 700 路动态异步并发，并复用 HTTP 连接。
- 书旗正文字符变换改用转换表，Base64 继续使用标准库解码；点众继续使用 PyCryptodome AES 解密。

## v5.8.3

- QQ阅读、七猫、书旗、得间、点众和番茄的活动正文链路统一使用异步调度与可复用 HTTP 连接；网络请求不再占用下载工作线程，解压、HTML 处理和解密仍通过受限 CPU 队列执行。
- QQ阅读普通书继续固定为每批 500 章、最多 5 批并发；详情、目录、密钥池和出版书资源也改为同一异步会话，出版书解包使用受限并发。
- 番茄正文与缺章补拉使用 HTTP/2 异步客户端复用连接，补充 `httpx[http2]` 依赖；AES-CBC、DES 和 CBC 加密内容继续使用 PyCryptodome。
- 得间详情、目录和搜索移除同步 requests 调用；其自定义 CTR/密钥派生格式和书旗的轻量正文编码并非标准 AES，保留原算法以保证结果兼容。

## v5.8.2

- QQ阅读下载不再识别 QQ 浏览器 `novel.html5.qq.com` 链接，避免没有 QQ 阅读目录的链接触发失败下载。

## v5.8.1

- 番茄下载详情与 TXT 文件头部只使用接口返回的原始总字数，显示精确整数，例如 `2,760,000字`；畅听副本未返回字数时，以书名、作者和章节数严格核验同书规范记录后补查，无法核验则显示“未知”，不再以章节正文或“万字”展示值推算。

## v5.8.0

- “找书”聚合新增 QQ 阅读 App 搜索，并接入现有跨平台去重、相关度排序和 `选N` 下载流程。
- QQ 阅读候选在展示前先校验详情和完整目录：全免费与明确标记“会员免费”的书籍保留；章节单独付费、目录不完整或预检失败的候选直接剔除。
- 跨平台同书优先级调整为番茄 > 七猫 > QQ阅读 > 书旗 > 得间 > 点众。

## v5.7.5

- 得间正文下载接入普通版与极速版双通道：两端独立获取批量下载目录，并从同一组 7 章任务块动态抢占，避免静态分段造成尾部等待。
- 批量目录、章节直链或 DRM 授权失效时，自动重新获取对应通道的短时目录并重新申请授权；同一旧目录并发失败只刷新一次。
- 本地实测 224 章完整下载中，双通道 24 路并发为最快配置，完成 224/224 用时 10.72 秒；32 路并发完成 224/224 用时 11.65 秒，因此默认保持 24 路。

## v5.7.4

- 得间正文下载改为 App 批量链路：每批获取 7 章下载地址并申请一次批量授权，任务队列按书籍规模动态分配，最高 24 路并发。
- 兼容批量授权的最终章节授权类型；每章严格按自身授权节点处理，失败章节最多重试 3 次，任一章节缺失时不生成部分 TXT。

## v5.7.3

- 修复“找书”选中番茄书籍后，详情接口返回字符串 0 时下载提示显示“字数：0字”的问题：零值统一视为缺失。
- 找书会话保留番茄搜索结果中的原始字数字段；详情没有有效字数时优先复用该字段，否则抽取 5 个均匀章节正文，以中位数计算并显示“约 X 万字”。

## v5.7.2

- QQ 阅读在目录解析后检查每章费用：存在章节单独付费且详情未标记“会员免费”时，下载前直接回复“不支持章节单独付费的书籍\r\n只支持免费书籍和vip书籍”。
- `vipFree` / `vipFreeMsg` 标记的会员免费书籍及全免费书籍保持原下载流程，不再先下载免费试读章节后才失败。

## v5.7.1

- 修复点众详情提示未读取 App 返回的 `totalWordSize`，现在会展示书籍总字数；同时兼容 `totalWords` 与 `wordSize` 字段。

## v5.7.0

- 点众正文下载改为按章节数动态分配独立 App 会话，最高 60 路并发；每路复用自己的 HTTP Session 并串行完成正文请求、广告解锁和正文复取，避免单一身份并发时解锁状态互相覆盖。
- 点众目录改用 App 目录窗口并发获取，不再携带旧章节 ID 重复拉取累计目录；正文首轮失败后只重试缺失章节，每轮使用新 App 会话，最多重试 3 轮。
- 点众下载现在要求目录与正文全部完整后才合成 TXT；开始、约 10% 分段进度和完成汇总保留 INFO 日志，单章与重试细节降为 DEBUG。

## v5.6.0

- 书旗下载整文件重做：删除自动 UID、书评会员探测、批量包、SQB/SQC 和旧正文分支，签名、目录 URL、字符变换与 Base64 解密统一以 `content.py` 单章实现为核心。
- 目录只请求一次并校验返回章节数，正文按目录顺序使用单章 URL 下载；动态并发上限根据本地压测从 80 调整为 64，仅失败章节进入下一轮，累计最多尝试 3 次，缺少任意章节时不生成部分 TXT。
- 书旗搜索、联想和搜索签名保留在 `书旗小说.py`，找书聚合模块只调用公开搜索入口；详情提示、CRLF TXT、下载缓存占用标记、当前主网盘分享按钮和百度后台备份流程保持不变。

## v5.5.0

- 找书新增书名与作者双模式：支持 `找书名 关键词`、`找作者 关键词`，原有 `找关键词` / `找书 关键词` 会根据候选结果自动判断搜索意图。
- 推荐排序改为严格相关度：依次比较精确、前缀、包含、反向包含和保守模糊匹配，再按实际匹配字数、查询覆盖率与跨平台同书共识排序；平台热度只作为同平台次级参考。
- 无直接书名或作者关系的结果不再展示；多词查询要求每个词都在书名或作者中命中，作者模式会过滤仅在书名中偶然出现作者关键词的记录。
- 番茄目录预检只检查最终可能展示的相关候选，严格相关结果不足一页时才执行联想补搜，补搜结果仍按原关键词过滤。

## v5.4.3

- 修复机器人生成的新夸克二维码仍被 App 判断为过期的问题：扫码 Token 和状态轮询请求现在与夸克网页当前 UOP 请求器一致，固定携带 `client_id=532`、`v=1.2`，并为每次请求生成独立 UUID `request_id`。
- 删除上一版额外添加但网页端未使用的 `_` 防缓存查询参数和临时缓存请求头；二维码登录 URL 保持网页端当前参数顺序与编码，轮询继续复用同一 Token。

## v5.4.2

- 修复重复发送 `夸克登录` / `登录夸克` / `刷新夸克Cookie` 时可能继续看到上一张二维码的问题：新命令会先取消并等待旧轮询任务完成，再获取二维码。
- 二维码 Token 请求增加纳秒级唯一查询值和 `no-cache/no-store` 请求头，明确绕过中间缓存；回复文案标明已重新获取新二维码。

## v5.4.1

- 修复夸克扫码轮询把网页端可瞬时返回的非成功状态直接判为失败的问题；现在仅在取得 `service_ticket` 后进入换取登录态，其余状态继续等待至成功或超时。
- 同步夸克网页当前二维码参数，并为 `/account/info` 补齐 `fr=pc`、`platform=pc` 与业务成功校验；扫码 Cookie 兼容网页使用的 `__kps` 身份字段，失败日志增加 `token` / `poll` / `auth` / `cookie` 阶段标识且不输出登录凭据。

## v5.4.0

- 新增夸克网页扫码登录：群文件清理管理员可在私聊发送 `夸克登录` / `登录夸克` / `刷新夸克Cookie`，机器人返回 5 分钟有效二维码；扫码成功后自动保存 `b-user-id` / `__uid` 与 `__puus` 登录态并覆盖 MySQL 旧值，重复登录会取消上一轮等待，插件停止时会清理后台轮询任务。
- 夸克请求不再把短时 `ctoken` 加入查询参数，也不再访问主页探测补全；所有网页 API 默认携带当前网页端的 `pr=ucpro`、`fr=pc` 与空 `uc_param_str`，响应刷新 `__puus` 时继续按并发保护规则回写。
- 按夸克网页端实测请求重做上传参数与 OSS 签名：预上传使用网页 MIME、时间戳和并行上传字段，哈希请求只提交网页实际字段，完成分片时加入 XML `Content-MD5`、`application/xml` 与网页 OSS User-Agent。
- 删除同名旧文件现在提交网页端 `exclude_fids` 字段并轮询删除任务完成后再上传；目录列表和“我的分享”查询参数同步为当前网页端格式，避免旧文件仍处理中时发生同名冲突。

## v5.3.0

- 新增 UC、夸克和百度三种网盘的聊天 Cookie 登录：群文件清理管理员白名单 QQ 可直接粘贴普通 Cookie、`Cookie:` 请求头、curl、Cookie Editor JSON 或 Netscape cookies.txt，无需先进入插件配置。
- 自动按关键字段识别平台：UC 使用 `UDRIVE_TRANSFER_SESS`，夸克使用 `b-user-id` / `__uid` 与 `__puus`，百度使用 `BDUSS` 与 `STOKEN`；也支持 `UC Cookie:`、`夸克 Cookie:`、`百度 Cookie:` 前缀明确指定平台。夸克 Cookie 未携带 `ctoken` 时会先访问网盘主页尝试补全。
- 网盘登录态写入 MySQL `mantou_runtime_state` 的 `novel_pan_auth` 命名空间，状态键分别为 `uc`、`quark`、`baidu`；新 Cookie 覆盖旧登录态，上传时数据库登录态优先于插件配置，未保存时继续使用原配置 Cookie。
- UC 与夸克请求期间刷新 Cookie 后会在原数据库登录态未被并发替换的前提下回写最新值；Cookie 原文不写入插件日志，白名单外发送 Cookie 继续静默处理。

## v5.2.1

- 修复书旗单章链路在目录只返回部分可读章节时仍可能生成不完整 TXT 的问题：自动 UID 请求目录后，只有目录的可读章节数与总章节数一致才开始下载；正文下载后再次校验完整性。
- 本地实测书籍 `91533`：自动 UID、目录与首章单章接口均可用，但目录仅返回 `103/1822` 个当前可读章节，因此现在受控结束，不发送 103 章的部分文件。

## v5.2.0

- 新增小说主网盘路由，群文件清理管理员可发送 `网盘状态`、`换UC`、`换夸克`、`换百度` 查看配置并切换；选择结果写入 MySQL `mantou_runtime_state` 的 `novel_share_pan/active`，默认 UC，后续番茄、七猫、书旗、QQ阅读、得间和点众统一使用当前主网盘，不自动回退。
- 新增夸克网盘上传与分享：自动逐级创建目录、删除同名旧文件、执行预上传/秒传检测/OSS 单分片上传，并从分享任务或“我的分享”取得链接；新增 `quark_pan_settings` 配置分类。
- 百度网盘现在既可继续按书籍状态做后台备份，也可作为主分享网盘；主分享上传后调用 `/share/set` 创建带四位提取码的永久链接，提取码写入按钮 URL，且跳过重复后台备份。
- 六个小说平台不再直接绑定 UC 模块，统一通过 `小说网盘.py` 上传和发送完成链接；当前主网盘未配置或失败时仍只返回固定文件发送失败提示。
- 书旗下载删除批量包、`freedownurl` 与 SQB/SQC 路径，改为自动获取 UID 请求目录后，按目录返回的免费、已购或短篇预览 URL 使用单章接口下载；最多 80 个动态并发，失败章节重试 3 次。
- 本地回归验证覆盖三网盘命令映射、百度分享请求参数、夸克配置顺序、主网盘单路由和六个平台统一接入。

## v5.1.7

- 修复 QQ阅读普通书籍在首批批量请求出现大量空正文或尾批为空时长时间停在 `progress=0` 的问题：先固定保留 500 章批次结果，再把缺失章节按连续最多 31 章窗口重试。
- 失败章节重试最多使用 5 个动态并发、执行 3 轮；某轮没有恢复章节时立即停止，避免对失败章节逐章串行等待。
- 重试结果按原章节位置合并，尾批为空也会进入缺章校验；只有所有章节完整时才合成 TXT。重试细节降为 DEBUG，INFO 仅保留开始、分段进度和完成汇总。
- 本地回归验证：509 章任务保持完整结果长度，434 个连续失败章节收敛为 14 个窗口；无恢复时只执行一轮窗口重试并受控结束。

## v5.1.6

- 书旗小说下载不再把固定游客 UID `8000000` 用作目标书下载身份；每次下载开始按旧源码的书旗 App 搜索、目录和书评链路自动筛选当前有效年费 VIP UID，仅接受 `vipStatus.status=2` 且 `annualVipStatus=1` 的结果。
- 同一下载任务内把自动 UID 贯穿目标书目录、下载批次、`freedownurl` 批次 key、`_public` 公参和 SQB/SQC 异或解密，不使用全局可变下载 UID，也不生成本地 UID 缓存文件。
- 本地实际接口验证：自动结果为数字且不是游客 UID；测试书目录返回 1274 章、43 个批次均取得包 URL，首个批量包成功解密 30 章。

## v5.1.5

- 修复 QQ阅读分享链接 `site=4` 被直接误判为出版书的问题：目录类型改为先请求普通 App 目录，仅在普通目录为空或返回 HTTP 400 时回退出版资源目录。本地实测书籍 `58675363` 普通目录、正文和 TXT 合成均为 188/188。

## v5.1.4

- 修复 QQ阅读超短章节解密失败：当 libfock 解密头的内嵌正文区为 128 字节全零占位时，正文改从密文偏移 256 开始解密；普通章节继续使用原内嵌正文拼接逻辑。本地实测《不灭战神》由 6199/6227 修复为 6227/6227，失败章节为 0。

## v5.1.3

- QQ阅读普通正文调整为每批 500 章、最多 5 批动态并发；每个批量范围只请求一次，仅对空正文、解密失败或批量包缺失的章节逐章重试 3 次，已成功章节不再重复请求。
- QQ阅读批量正文按响应中的真实章节键对齐，避免缺少成员时后续章节错位；普通目录返回 HTTP 400 且详情存在章节数时自动切换出版资源目录。本地实测出版书 `47460033` 目录与正文均为 12/12。
- QQ阅读和番茄小说恢复受控 INFO 进度日志，只记录开始、约 10% 分段进度和完成汇总，不逐章刷屏。

## v5.1.2

- 小说上传 UC 成功后，QQ 官方机器人把完成文案、备用“点击此文字打开”Markdown 链接和“点击打开”按钮合并为同一条消息，不再单独补发文字链接。

## v5.1.1

- 修复 QQ阅读直接登录态识别：除标准 `ywguid=值; ywkey=值` Cookie 外，现在也接受聊天中常见的 `ywkey: 值` / `ywguid: 值` 两行文本，并继续覆盖 MySQL 中同一登录态记录。

## v5.1.0

- QQ阅读新增无前缀 Cookie 识别：群文件清理管理员可直接粘贴普通 Cookie、Cookie 头、curl、Cookie Editor JSON 或 cookies.txt，插件提取并校验 `ywguid/ywkey`。
- QQ阅读登录态写入 MySQL `mantou_runtime_state`（namespace=`qq_reader_auth`，state_key=`login_state`）；同一状态键使用数据库 UPSERT 覆盖旧值，下载开始时读取最新值覆盖固定 `uid/usid`，不改动固定 `fuid/qrsn`。
- QQ阅读固定 CONFIG 改为每次模块加载只初始化一次，避免后续详情、目录或正文请求把数据库登录态重置；Cookie 分发提前到数字撤回之前，未授权用户命中时静默处理。
- 不恢复手机号、验证码、滑块或本地登录态文件，动态密钥池继续仅存当前进程；同步更新帮助、配置提示和使用说明。

## v5.0.0

- 删除原有 QQ阅读模块并整文件替换为本地 `source-code-master/QQ阅读/qq阅读.py` 参考核心；CONFIG、App `csigs` 签名、动态密钥池、libfock 多模式解密及 Fetcher 均直接来自新核心，不保留旧批量器、旧解密路径或旧算法兜底。
- 删除 QQ阅读手机号登录、浏览器 Cookie、Cookie 状态和数据库登录态的全部代码与分发入口；动态密钥池改为仅在当前进程内保存，不再读写 `keypool.cache` 或登录态文件。
- 在新核心内只增加 AstrBot 薄适配：识别 QQ阅读链接、请求 App 详情与目录、调用参考 Fetcher 的 31 章窗口/4 线程正文下载、严格校验完整章节，并按统一 CRLF 格式合成 TXT。
- 更新固定 App 账号授权并补齐 `site=4` 出版书路径：批量目录同时识别 `ctebchaptercosurl`、`epubPureUrl` 和 `epubResourceUrl`，一次取得出版书密码后最多 4 线程解包；本地实测《狙击涨停：短线打板高手技术进阶》54/54 章完整合成。
- QQ阅读 TXT 继续写入下载缓存并创建占用标记，完成后上传 UC、发送“点击打开”按钮，再执行百度网盘后台备份和缓存清理；新增 `requests` 运行依赖。
- 小说上传 UC 成功后，QQ 官方机器人在“点击打开”按钮后追加一条纯 Markdown“点击此文字打开”链接，兼容无法使用链接按钮的鸿蒙客户端。
- 精简 QQ阅读模块，删除 CLI 单章入口、未使用算法包装、重复整数转换及本地密钥池路径等运行链路不可达代码，只保留实际使用的签名、解密、目录、正文、TXT 与网盘适配。

## v4.10.12

- QQ 阅读的 libfock 解密、多模式正文解密与 App `csigs` 签名算法完整对齐本地参考项目；固定 App `fuid` 同时用于密钥池、正文请求和章节解密，已保存登录态不再覆盖加密身份。
- QQ 阅读详情、目录、普通正文和出版书资源统一通过 App 请求构造器发送；普通正文固定使用 `type=2` 且不混入浏览器 Cookie，浏览器登录态仅保留给登录流程和出版书授权。
- 全书下载继续按每 500 章一个业务批次、最多 4 批并发，批次内部改按参考项目的 31 章窗口顺序请求；新增固定身份、请求签名、窗口拆分和并发上限回归测试。

## v4.10.11

- 修复 QQ 阅读出版书章节资源请求：目录资源固定使用 App 签名请求头，已保存的浏览器登录态仅用于授权密码请求，避免两种请求上下文混用。
- 出版书 ZIP 解包新增密码头、长度、CRC 和正文可读性校验，错误资源密码或异常二进制内容不再写入 TXT。
- 番茄、QQ 阅读和 UC 网盘的下载进度、缓存、上传成功与缺章补拉等常规日志改为调试级别；失败告警继续保留。

## v4.10.10

- 修复 QQ 阅读大批量正文包偶发遗漏章节：首轮仍保持每批 500 章、最多 4 批并发；少量缺章改按 App 已验证的 31 章连续窗口补拉，并始终使用 tar 成员的实际章节编号解密。
- 优化 QQ 阅读出版书资源补齐：缺失资源先按 31 章窗口批量请求，只有窗口仍未返回资源时才单章兜底；新增受控汇总日志记录批次数、批量恢复数、单章兜底数和耗时，便于定位授权或资源响应问题。

## v4.10.9

- 修复 QQ 阅读出版书授权：保留已保存登录态的认证信息，识别分享卡片中 Base64 包装的 `qrsn`，并按 App 授权头叠加登录 Cookie 后有限尝试可用认证值。
- 出版书获取到授权密码后，会仅补齐批量目录中缺失的章节资源地址，再并发下载；游客试读目录不再被误当成完整资源列表。
- QQ 阅读出版书授权日志改为单条受控诊断，仅记录请求状态、认证来源、请求头模式和是否取得资源密码，不输出 Cookie、认证值或响应正文。
- 普通 QQ 阅读首轮大部分成功但存在瞬时缺章时，少量缺章补拉改为最多 3 轮；每轮刷新动态密钥池，并兼容章节序号与实际章节 ID，不会对接口拒绝或大比例失败的书放大请求。
- 移除帮助 MD 键盘的群聊/私聊发送成功日志及响应转储，保留失败告警和原有消息发送功能。

## v4.10.8

- 修复 QQ 阅读出版书分享链接：site=4 现在改走 App 的出版书目录参数并解析章节资源；普通目录返回 HTTP 400 的书籍也会自动切换到该路径。
- 出版书正文按 App 返回的资源和当前登录态授权解包，不再把出版书误判为无目录。
- 少量缺章补拉由固定 20 章改为按总章节数的 2% 动态计算，最少 20、最多 120；6227 章书籍的 33 个瞬时缺章会进入补拉，大比例失败仍不会放大请求。

## v4.10.7

- 修复番茄网页 `reader` 链接下载：链接中的章节 ID 在畅听目录为空时，会仅通过现有番茄阅读 App 正文请求解析真实书籍 ID，再使用该书籍 ID 获取目录和批量正文；不接入网页接口。

## v4.10.6

- 修复 QQ阅读已登录会员下载时的少量缺章误判：首轮仍保持每批 500 章、最多 4 批并发；仅在接口未拒绝且失败章节不超过 20 章时，刷新 App 动态密钥池并定向补拉。
- 会员状态已确认时，少量传输或解密失败不再回复“收费书籍不支持下载”；只有已识别收费且未获会员权益、且大比例章节无法读取时才显示该提示。

## v4.10.5

- 移除群管卡片撤回的完整消息诊断输出，不再在常规日志写入卡片标题、描述、链接、消息 ID、原始消息或组件转储。
- 网页群文件的清理选择与备注流程日志改为不含用户输入、会话标识、群号列表和备注内容的调试日志；异常日志仅保留异常类型。

## v4.10.4

- QQ阅读恢复已登录账号的 App 正文链路：保存的 `ywguid/ywkey` 现在使用最小登录请求头与 `type=0` 授权包参数，不再被游客正文请求忽略。
- 详情请求同步使用最小登录头，并在现有下载开始日志记录非敏感的服务端登录和会员状态，便于定位登录态过期或当前书籍不在账号权益范围的情况。
- 本机使用本地登录态验证登录路径：登录请求头未混入游客签名字段；服务端对 `304890` 返回 `service_login=no`、`service_vip=no` 且仅下发 137 章，因此该登录态未获得该书完整授权。

## v4.10.3

- QQ阅读正文请求严格对齐本地 QQ 阅读项目的 App 签名头和参数：不再把 Cookie、登录字段、`text_type` 或额外 `fuid` 请求头混入正文请求，修复批量包被接口拒绝的问题。
- 保持每批最多 500 章、最多 4 批并发；删除失败后的多轮逐章补拉，正文包部分可读时直接结束并按现有规则提示收费书籍，避免数千章无效请求导致下载缓慢。
- 本机实测：免费书籍 218/218 章完成；2071 章样本以 500 章批次、4 并发完成请求后不再进入缺章补拉。

## v4.10.2

- 修复 QQ阅读已登录下载时未初始化下载态导致流程中断的问题。
- 修复 QQ阅读详情请求混入正文签名后返回空数据的问题，分享卡片可正常显示书名、作者、状态和字数。

## v4.10.1

- 修复 QQ阅读书籍目录请求：目录改用 App `type=0` 授权包，避免分享卡片书籍返回拒绝访问或空目录。
- QQ阅读正文批量范围调整为每批最多 500 章、最多 4 批并发；目录与正文请求参数保持分离。

## v4.10.0

### 调整
- QQ阅读下载统一迁入 source-code-master 的 App 签名与动态密钥池链路：详情、目录和正文均使用 App 请求，正文固定 31 章一批、最多 4 批并发。
- 移除静态密钥池、网页正文参数和预探测下载分支；动态密钥池只保留进程内缓存，不生成本地 `keypool.cache` 文件。
- QQ阅读登录态继续仅存 MySQL；手机号和一次性登录字段不写入数据库。浏览器 Cookie 兼容 `ywguid/ywkey` 与 `yw_guid/yw_key`。

### 验证
- 本地联调公开书籍 `27408660`：目录 218 章，App 批量正文解密 218/218。

## v4.9.6

### 修复
- QQ 阅读登录失败日志改为受控协议诊断：仅记录返回码、会话状态、非敏感字段名和设备指纹摘要，不再输出原始响应、手机号或登录凭证，便于定位短信接口拒绝原因。

## v4.9.5

### 修复
- QQ 阅读手机号登录恢复使用解密算法调整前已验证的 YWLogin SDK 客户端版本号；仅影响短信登录请求，不改动正文下载和解密算法。

## v4.9.4

### 调整
- QQ 阅读重新登录改为独立的新账号会话：旧登录态在新验证码登录成功前继续保留，成功后才覆盖；新登录请求不再携带旧账号凭证。

## v4.9.3

### 修复
- QQ 阅读手机号登录不再把源码设备参数 `Android 28 8.5.2.890 417` 强制改为其他版本号，短信请求保持与当前本地 App 设备指纹一致。

## v4.9.2

### 修复
- QQ 阅读手机号登录发送短信时，已保存登录态中的 `ywguid/ywkey` 会同时写入 App 表单与 Cookie，和本地 App 登录链路保持一致，避免已登录态被遗漏后触发通用登录失败。

## v4.9.1

### 调整
- 小说下载完成卡片首行文案改为“宝宝你的”。

## v4.9.0

### 调整
- QQ 官方机器人群聊中的所有文本回复统一经 Markdown 发送，并在首行使用 `<@member_openid>` 提及发起人；帮助、找书、小说下载、平台开关、群文件清理进度及 UC 下载完成消息均覆盖，私聊保持不提及。

## v4.8.0

### 调整
- QQ 官方机器人群聊中的管理回复统一改为 Markdown 发送，并使用官方群成员 OpenID 以 `<@member_openid>` 提及操作人；私聊与非 QQ 官方适配器保持原回复形式。

## v4.7.0

### 新增
- 新增管理员测试模式：`开测试` / `开启测试` 开启，`关测试` / `关闭测试` 关闭。测试模式开启时，白名单管理员可继续测试已关闭的小说平台，普通用户仍受平台开关限制。

### 调整
- 群文件清理、群聊管理、全量消息、帮助、状态、小说开关、QQ阅读登录与 Cookie、群禁言等受限管理指令在白名单外统一静默忽略，不再发送权限提示。

## v4.6.0

### 新增
- 新增 `小说` / `小说列表` 状态查询，显示番茄、七猫、书旗、QQ阅读、得间和点众下载功能的开启或关闭状态。
- 小说功能开关新增所有平台的短指令：`开番茄`、`开七猫`、`开书旗`、`开QQ阅读`、`开得间`、`开点众`；QQ阅读名称大小写均可识别。

## v4.5.0

### 调整
- 全量消息功能完全改为 QQ 官方群内状态接口：`全量消息` / `全量消息状态` 请求 `GET /v2/groups/{group_openid}/bot_state`，读取 `recv_msg_setting` 与 `allow_proactive_msg`。
- 仅 `recv_msg_setting=all` 显示为“群内全部消息已开启”；接口标注为内邀开放，无法查询时显示状态暂不可查询，不伪造结果。
- 删除旧的数字群号、机器人 QQ、UID 转换与非官方跳转链接逻辑；官方 Markdown 回复使用 `<@member_openid>` 提及操作人。

## v4.4.1

### 修复
- 小说 TXT 写入缓存后立即创建上传占用标记；插件重载清理会跳过当前仍在上传或进行百度后台备份的文件，避免 UC 网盘读取时出现本地文件已不存在。
- UC 网盘上传前记录受控的缓存存在性与文件大小诊断，便于后端定位缓存时序问题，QQ 用户侧仍只显示固定短提示。

## v4.4.0

### 新增
- 新增 `全量消息`、`全量消息 数字群号` 和 `全量消息 数字群号 机器人QQ`，由群主打开 QQ 群全量消息开启链接后接收群内全量消息事件。

### 调整
- 原 `授权` 入口统一改名为“全量消息”，帮助、Markdown 标题、按钮、配置说明和文档同步更新；旧 `授权` 指令不再响应。

## v4.3.3

### 修复
- 插件重载时先刷新运行状态数据库子模块，再加载状态功能，避免 Python 子模块缓存导致缺少数据库检查函数而无法加载插件。

## v4.3.2

### 修复
- 未填写数据库配置时，运行状态读取不再尝试连接 MySQL 或刷小说功能开关告警；小说下载功能按默认开启状态运行。
- 插件启动或重载不再读取数据库启动群文件 Cookie 保活；数据库检查仅在输入 `状态` 时执行。

## v4.3.1

### 调整
- 小说功能目录按职责拆分：平台下载模块归入 `小说功能/小说/`，找书、开关和缓存清理归入 `小说功能/功能/`。

## v4.3.0

### 新增
- 插件启动或重载时自动清理 `功能文件/下载缓存/` 中上次运行遗留的小说 txt，避免下载中断后缓存残留。

## v4.2.0

### 新增
- QQ阅读支持直接识别并保存管理员发送的浏览器 Cookie、curl、Cookie Editor JSON 和 Netscape cookies.txt，无需命令前缀。
- 新增 `QQ阅读cookie状态`，仅显示登录 Cookie 是否已保存。

### 修复
- 支持识别 Netscape `#HttpOnly_` 格式的 QQ阅读 Cookie。

## v4.1.2

### 调整
- 将 QQ阅读、得间、点众的协议/解密源码合并进各自小说文件，删除独立源码文件。
- 点众设备态固定为内存会话，不生成 device.json。

## v4.1.1

### 调整
- QQ阅读只保留 _qq阅读解密 多模式 libfock，删除旧解密兜底。
- 整理 QQ阅读.py 分区顺序：工具/登录态/解密算法/章节包/请求/判断/登录滑块/文件/入口/下载/指令。

## v4.1.0

### 新增
- 新增得间小说、点众小说本地下载。
- 找书聚合搜索补充得间、点众；优先级番茄>七猫>书旗>得间>点众。
- 小说开关新增开启/关闭得间、点众。

### 调整
- QQ阅读正文解密对齐 source-code-master 多模式 libfock，旧算法兜底。
- 撤回白名单补充 palmestore/zhangyue/ireader/dianzhong。

## v4.0.0

### 破坏性变更
- 彻底删除用户激活、卡密、收费、付费和每日免费额度模块、配置及数据库依赖；小说下载保持全部免费。
- 运行状态数据库改为独立通用实现，继续保存群文件 Cookie、群列表、小说功能开关和 QQ 阅读登录态，并兼容旧数据库配置字段。

### 调整
- QQ 官方机器人的「清理群文件」选择键盘改为每页最多 9 个群号，每行 3 个群号，最后一行放翻页和返回；键盘固定不超过 4 行，群数量超过 9 个时可继续翻页。
- `状态` 改为九项系统面板：系统位数、CPU、物理内存、磁盘、进程数、操作系统、框架版本、运行时间和当前时间。

## v3.0.0

### 破坏性变更
- 删除 `功能文件/API功能/` 全部模块，不再提供 OIAPI、析API、崩溃API、聚合API 或 Y6api 下载分支。
- 删除随机英文单词、随机一言、疯狂星期四、古诗词名句和番茄/QQ阅读 API 开关、切换指令。
- 番茄小说和 QQ阅读统一只使用小说功能内置的本地下载链路。

## v2.18.0

### 调整
- 小说下载完成后的 QQ 文件发送统一改为 UC 分享链接按钮：番茄、七猫、书旗、QQ阅读及旧番茄 API 均显示书名、作者和“点击打开”，不再发送原小说 txt 或链接 txt。
- QQ 官方 Markdown 键盘发送在原消息已过期时会自动改用主动发送，避免长篇下载完成后按钮因 `msg_id` 失效而丢失。
- 百度网盘后台备份改在完成按钮或降级链接消息发出后执行，缓存文件在后台处理结束后清理。

## v2.17.17

### 调整
- 帮助菜单的分类、触发项、返回和详情快捷命令按钮全部改用 QQ 官方原生回调；点击不再向聊天发送指令文本。
- 新增 QQ 官方帮助互动桥，订阅 `INTERACTION` 并把回调安全投递到现有帮助菜单和命令分发流程。

## v2.17.16

### 调整
- 找书结果改为正文文字指令链：书名、作者和翻页文字均可点击，移除底部找书键盘与静默互动桥。

## v2.17.15

### 修复
- 找书点击番茄候选时直接把搜索结果中的书籍 ID 交给下载入口，不再拼接页面链接后二次解析。

## v2.17.14

### 修复
- 找书会预检靠前的番茄候选；已明确没有章节目录的记录不再展示，避免点击后才下载失败。
- 目录预检在跨平台去重前完成，使同书的七猫或书旗可用结果能够正常补位。

## v2.17.13

### 修复
- 修复找书静默点击把互动 ID 当成 QQ 消息 `msg_id`，导致私聊发送反复重试的问题；私聊点击下载改用主动发送。

## v2.17.12

### 修复
- 找书 QQ 官方按钮恢复原生 `action.type=1` 回调：点击书名后不在聊天发送“选N”，直接开始下载或翻页。
- 增加 QQ 官方 WebSocket 互动桥：启动时订阅 `INTERACTION`，把找书原生回调安全投递到现有下载流程，并立即回应互动事件。

## v2.17.11

### 修复
- 找书按钮：AstrBot QQ官方 WebSocket 不转发 INTERACTION，改回 type=2 指令按钮(enter) 才能点到下载
- 说明：type=1 原生回调当前后端收不到，不是节点或业务逻辑问题

## v2.17.10

### 修复
- 找书回调：增强 INTERACTION 字段解析与 openid 会话匹配，点击书名无消息直接下载更稳
- 找书回调：解析失败时写后端诊断日志，不向前端暴露细节

## v2.17.9

### 调整
- 找书下载改为 QQ 官方回调按钮（action.type=1）：点击书名不在聊天发送任何消息，直接回调后台开始下载。
- 找书结果 Markdown 正文只展示书名作者；底部键盘每本一书名按钮，翻页并入末行。
- 收到回调后 PUT /interactions/{id} 结束客户端 loading，再进入下载或翻页。

## v2.17.8

### 调整
- 找书跨平台同名同作者去重优先番茄，其次七猫，最后书旗（下载更快优先）。
- 私聊找书：书名旁使用 `qqbot-cmd-enter` 直接发送 `选N`，点击后立刻进入下载流；群聊仍用 `qqbot-cmd-input` + `show`。

## v2.17.7

### 修复
- 找书指令链按官方文档修正：`qqbot-cmd-enter` 仅支持 `text`，自定义书名/作者外显改用 `qqbot-cmd-input` 的 `show`。
- 找书跨平台同名同作者去重，只保留最优一本，避免结果看起来重复。

## v2.17.6

### 调整
- 找书排序改为通用机制：精确书名优先、多平台同名同作者共识加权，再按与第一本的相似度聚拢（同作者/同系列/同名多源）。
- 不跨平台硬比阅读量，热度只作平台内参考且不展示给用户。
- 不写死具体书名，适用于任意搜索词。

## v2.17.5

### 调整
- 找书 QQ 官方机器人改为 Markdown 指令链点击下载（私聊 `qqbot-cmd-enter` 直接发送，群聊 `qqbot-cmd-input` 填入输入框），不再使用内联键盘按钮，避免行数超限。
- 找书结果展示热度，并按热度从高到低排序；有阅读量显示阅读热度，否则显示评分。
- 找书结果分隔线改为 `————————`；不展示链接、来源和序号。

## v2.17.4

### 调整
- 找书下载改为仅按钮点击：书名/作者按钮发送隐藏选书指令，不再使用也不可见数字 1-5。
- 结果文案、帮助和 README 去掉“发送数字下载”提示，仅提示点击书名/作者开始下载。

## v2.17.3

### 调整
- 找书在 QQ 官方机器人下使用 Markdown + 键盘：每本书提供书名、作者两个可点按钮，点击后直接开始下载。
- 结果正文仍不展示链接和来源；普通机器人继续发送数字 1-5 下载。

## v2.17.2

### 调整
- 找书结果去掉序号展示，仅保留书名、作者；仍发送数字 1-5 下载当前页对应书籍。

## v2.17.1

### 调整
- 找书结果不再显示链接和来源，只显示书名、作者、序号；发送 `1-5` 下载当前页对应书籍。
- 移除激活功能入口：主流程不再处理激活/卡密/收费门禁，功能全部免费。
- 帮助菜单移除激活功能分类；状态固定显示收费模式已全部免费。

## v2.17.0

### 新增
- 新增找书功能：发送 `找关键词` 或 `找书 关键词`，聚合搜索番茄/七猫/书旗。
- 结果按来源分开展示，同平台同书去重，每页 5 条，支持 `上一页` / `下一页`。
- 结果链接点击后走现有番茄/七猫/书旗下载流程。

### 调整
- 全站改为免费：关闭收费门禁与下载免费额度拦截；收费开关指令提示当前已全部免费。

## v2.16.6

### 修复
- QQ阅读按所有书通用规则判断：整本免费/广告全本直接游客批量下载；广告部分免费、试读、VIP 不再误判成整本免费。
- 详情 `maxfreechapter > 0 且 < 总章数` 时直接提示收费书，不硬下整本、不空转探测。
- 非全免书探测同时尝试 `text_type=1/2`，减少出版书探测漏判。
- 本地测试：`33077460` 全免 196/196（约 5 秒）；`27408660`/`52289342` 判定收费书。

## v2.16.5

### 修复
- 修复 QQ阅读大批量时 `text_type=1` 仅 1 章成功就提前返回、错过 `text_type=2` 的问题（实测 196 章整本 1/196）。
- 批量改为每批 50 章、最多 6 并发；整批失败时跨 type 取最高成功率结果。
- 缺章补拉第 1 轮改为最多 20 章连续小批，再单章补拉。
- 本地实测 `book_id=33077460`：196/196，约 6 秒。

## v2.16.4

### 优化
- QQ阅读下载改为大批量优先：每批最多 500 章、最多 6 并发，对齐小说大全默认 `--batch 500`。
- 免费/广告书跳过全量章节探测，直接批量正文。
- 收费书只探测前 30 章判断试读，避免先串行扫完全目录。
- 批量解密重试从 6 次降到 3 次；九成已成功时不再整批重拉，改由缺章补拉。

## v2.16.3

### 修复
- 对齐小说大全 QQ阅读最新策略：免费/广告书自动游客下载（去 Cookie 保留 fuid，`adState=0`），绕过登录广告 5 章闸。
- 批量正文参数支持 `adState/noclick`；登录 VIP 路径仍用 `adState=1`。
- 按书籍信息识别 `text_type`（网文 1 / 出版 2），并与缺章补拉共用。

## v2.16.2

### 修复
- QQ阅读本地下载在主批量结束后对失败章节做最多 3 轮单章定向补拉（并发 8），避免 193/196 这类少量缺章直接整本失败。
- 补拉仍有固定上限，补不齐才返回下载失败。

## v2.16.1

### 调整
- 精简 QQ阅读下载日志：批次重试/每批细节改为 debug，INFO 只保留进度分段与最终汇总，避免日志刷屏。

## v2.16.0

### 调整
- QQ阅读下载进度日志对齐七猫：`progress/percent/success/failed` 分段输出。
- QQ阅读 TXT 正文结构对齐七猫：`名称/作者/状态/字数/书籍ID/章节数` + 章节标题与正文。

## v2.15.9

### 修复
- QQ阅读本地下载无论是否登录都先探测服务端可下发章节。
- 收费/VIP 书若仅有试读章（如登录后仍只有 5 章）直接提示收费书不支持下载，不再按整本 200+ 章空转重试后失败。
- 空章节包减少无效重试。

## v2.15.8

### 修复
- QQ阅读滑块公网 IP 探测过滤 Docker/局域网私网地址（如 172.x / 192.168.x），避免下发手机打不开的链接。
- 登录提示改为可复制到浏览器打开，并明确需放行端口。

## v2.15.7

### 修复
- QQ阅读手机验证码登录兼容接口返回的 `ywGuid` / `ywKey` 字段，避免 code=0 仍判定登录失败。

## v2.15.6

### 修复
- QQ阅读滑块只绑定固定端口 `8765-8770`，不再跳到未放行的随机端口。
- 滑块页仅在验证结果成功回传到服务器后才提示成功，避免“显示成功但机器人未收到”。
- 登录提示补充需要放行的端口号。

## v2.15.5

### 修复
- QQ阅读滑块服务改为监听 `0.0.0.0`，自动获取服务器公网 IP 生成可点击链接，不再下发 `127.0.0.1`。
- 无需插件配置；请确保服务器放行默认滑块端口 `8765`。


## v2.15.4

### 修复
- QQ阅读登录滑块改为返回可点击链接，不再阻塞等待本机自动开浏览器。
- 滑块本地服务最多保留 5 分钟，完成后或超时自动关闭端口，避免 Address already in use。
- 管理员完成滑块后发送“完成”继续登录；取消/过期时同步关闭滑块服务。

## v2.15.3

### 修复
- QQ阅读手机号登录同步新版 YWLogin 签名、版本号和 okhttp 请求头，修复发短信返回 code=3 的问题。
- QQ阅读滑块二次发短信补齐 captchaticket/captcharandstr 参数。

## v2.15.2

### 修复
- QQ阅读收费书无可下载章节时，用户侧提示：`收费书籍不支持下载` / `请下载VIP或者免费书`。

## v2.15.1

### 修复
- QQ阅读本地下载改为纯 App 设备态：免费/试读章不强制账号登录，使用内置设备 fuid + keypool 解密。
- 未登录时先探测服务端可下发章节，避免对无权限章节空耗批量请求。
- 正文密文下载增加有限重试，兼容服务端可解密格式轮换。
- 清理 QQ阅读.py 重复定义，详情补充免费章字段。

## v2.15.0

### 新增

- QQ阅读迁入小说功能：默认本地 App 下载，固定最多 5 批动态并发。
- 新增 `开启QQ阅读API` / `关闭QQ阅读API`；开启后走析API content。
- 新增管理员 `登录QQ阅读` 手机号登录流；登录态仅保存到 MySQL。
- QQ阅读滑块页改为 QQ阅读风格白卡片暖橙样式，不展示敏感调试信息。

### 调整

- 番茄 API 开关仅保留 `开启番茄API` / `关闭番茄API`，不再兼容裸 `开启API` / `关闭API`。
- QQ阅读相关实现合并为单文件 `功能文件/管理功能/小说功能/QQ阅读.py`。

## v2.14.0

### 新增

- 新增 QQ阅读 小说功能开关，支持 `开启QQ阅读` / `关闭QQ阅读`，关闭后 QQ阅读链接只提示功能已关闭，不进入下载流程。

### 调整

- 状态命令、帮助菜单、README 和配置说明同步加入 QQ阅读 开关。

## v2.13.4

### 修复

- 番茄小说下载遇到接口已返回章节但正文为空的审核中章节时，不再把整本书判定为缺章失败；会保留章节标题并继续生成 TXT。
- 番茄小说目录补充读取章节元数据，优先使用真实章节标题，避免空正文或特殊章节只能显示默认序号。
- 恢复番茄阅读正文补拉链路：畅听正文源返回书籍状态业务错误或单章缺失时，按 30 章上限补拉并合并回原下载结果。

## v2.13.3

### 修复

- QQ 阅读析 API 正文下载失败时不再直接终止：正文批次请求会自动重试 3 次，整批仍失败时改为逐章重试，避免上游短暂波动导致整本下载失败；连续重试仍失败时保持停止下载，不生成缺章 TXT。

## v2.13.2

### 修复

- 番茄API开启时，`short-story-share` 一章短篇不再被旧 API 链路接管；短链会先展开识别 `post_id`，确认短篇后固定使用本地短篇下载链路，避免把无目录短篇按普通目录处理。

## v2.13.1

### 修复

- 番茄正文响应的顶层标题为“目录”“章节目录”“正文”或“内容”时，不再写入 TXT 作为章节标题；优先使用章节数据中的真实标题，随后尝试正文 HTML 标题，最后才回退为章节序号。

## v2.13.0

### 新增

- 番茄小说下载支持 `short-story-share` 一章短篇链接：短链展开后识别 `post_id`，读取短篇详情中的关联书籍和章节，再复用原有正文下载、TXT、网盘和文件发送流程。
- 短篇 TXT 使用详情返回的标题、作者和字数，正文章节标题保持为短篇标题。

## v2.12.9

### 重构

- 重新整理番茄小说下载模块，仅保留链接识别、书籍详情、章节目录、正文解密、TXT 生成、网盘备份和 QQ 文件发送所需代码；移除旧抓包请求、手动目录文件和 Windows CNG/纯 Python AES 解密实现。
- 下载业务入口、文件处理和日志统一使用中文命名与中文说明，协议签名函数保留原有名称以保证请求兼容性。

### 修复

- 同步最新番茄畅听正文解密：使用 PyCryptodome AES/CBC/PKCS7 和 gmpy2 DH 模幂运算处理正文密文。
- 保留 HTTP/2 连接复用、1500 章动态分批和完整性校验；已验证目标书 114/114 章及另一书前 10/10 章正文完整返回。

## v2.12.8

### 调整

- 番茄小说下载的书籍详情、章节目录和正文全部固定使用番茄畅听接口，删除 fanqienovel.com 网页目录请求及其回退逻辑。

## v2.12.7

### 重构

- 精简番茄小说模块，只保留链接识别、书籍详情、目录和正文下载、TXT 生成、网盘备份及 QQ 文件发送所需代码，移除上游的搜索、媒体、新闻、探测、调试和命令行功能。
- 同步新版畅听正文请求方式：按章节 ID 请求正文时使用稳定的请求书籍上下文，避免书籍自身上下文失效后再走 30 章阅读接口回退。
- 同步新版 DH 临时私钥位数限制，默认使用 128 位随机私钥完成正文解密所需密钥协商，减少本地幂运算耗时。
- 保留原有动态批量、HTTP/2 连接复用、完整性校验和用户侧固定失败提示。

## v2.12.6

### 修复

- 同步更新后的番茄小说正文源码：畅听正文源返回 `1021001` 时，自动改用番茄小说阅读 App 正文源，不再把这类书直接判定为下载失败。
- 阅读正文源按服务端单次 30 章上限自动分段并合并回原有下载流程；目标书 `7610304382235577406` 已验证 `114/114` 正文成功。
- 阅读正文注册密钥按版本缓存，并复用现有系统 AES 解密加速，避免重复注册和纯 Python 解密拖慢下载。

## v2.12.5

### 调整

- 添加番茄小说分发诊断日志：记录当前走番茄API链路还是本地链路；若番茄API已开启但API链路未识别链接，会输出 warning 便于排查。

## v2.12.4

### 调整

- 番茄链接分发改为先读取番茄API开关：开启时直接走旧番茄API链路，关闭时才走小说功能本地番茄下载链路。

## v2.12.3

### 调整

- 番茄小说正文批量大小和动态并发按当前书籍章节数计算，小书不再显示固定 1500 章批量和 6 并发。
- 番茄小说正文接口返回书籍不存在、停止合作或付费等明确业务错误时，不再继续二分拆分重试，避免无效请求拖慢下载。

## v2.12.2

### 调整

- 番茄小说功能正文单批请求章节数从 200 章调整为 1500 章，减少大书分批请求次数。

## v2.12.1

### 调整

- 番茄小说功能默认 novelfm 请求域名改为 `https://api5-sinfonlinec.novelfm.com/`。
- 番茄小说正文分批下载启用线程内 HTTP/2 连接复用，批次请求复用同一连接以减少握手耗时。

## v2.12.0

### 新增

- 新增小说功能版番茄小说下载，默认识别番茄链接后直接走番茄畅听小说正文批量下载逻辑，只生成小说 TXT，不接入音频、媒体或探测功能。
- 新增 `开启番茄API` / `关闭番茄API`（兼容 `开启API` / `关闭API`）管理员指令；旧番茄 API 默认关闭，开启后才把番茄链接交给 OIAPI/析API/崩溃API/聚合API/Y6api 下载。

### 调整

- 番茄小说默认归入 `功能文件/管理功能/小说功能/番茄小说.py`，下载提示、TXT 头部、本地缓存、UC 网盘和百度网盘后台上传链路对齐七猫/书旗。
- `状态` 和帮助菜单同步显示番茄API开关，不再把默认番茄下载归到 API 功能。

## v2.11.10

### 修复

- QQ 用户侧的小说下载、文件发送和接口切换失败统一使用短提示，不再显示具体 API 名称或业务错误内容。
- `状态` 不再展示当前番茄 API 名称和 `OIAPI key` 标识，仅显示接口已选择及是否已配置。
- 接口名称仅保留在帮助和手动切换菜单中，完整失败原因继续只记录后端日志。

## v2.11.9

### 修复

- 所有面向用户的异常、网络、数据库和文件发送失败不再直接输出原始错误文本，详细信息仅记录后端日志。
- 番茄外部 API 失败仅保留明确的安全业务提示，其余远端返回统一显示为下载失败，避免地址、请求参数或凭据透出。
- `状态` 不再展示数据库主机、数据库名及网盘上传目录，只显示配置是否启用和上传状态。
- 番茄 API 切换菜单不再展示接口域名或 IP 地址。

## v2.11.8

### 修复

- 修复 `d.shuqi.com` 书旗短链无法识别的问题，短链会先解析跳转后复用现有书旗下载逻辑。
- 取消网页群文件入口对普通消息的诊断日志，避免表情等无关消息刷屏；备注等待日志只在确实处于备注状态时输出。
- 调整番茄、七猫、书旗小说发送顺序：UC 链接文件准备完成后先交给 QQ 发送，百度网盘上传和源 TXT 清理改为发送后后台执行，避免百度上传阻塞发文件。

## v2.11.7

### 调整

- 撤回 v2.11.4-v2.11.6 中“群聊按钮消息改走私聊接口”的尝试：群聊 Markdown Keyboard 发送恢复使用 `/v2/groups/{group_openid}/messages`，只保留指令按钮 `enter: True` 点击直接发送。

## v2.11.6

### 修复

- 修复 QQ 官方机器人群聊按钮改走私聊接口后仍携带群聊 `msg_id`，导致 `/v2/users/{openid}/messages` 返回 `请求参数msg_id无效或越权` 的问题。

## v2.11.5

### 调整

- 网页群文件 Cookie 自动保活间隔从每 1 小时改为每 10 分钟。
- `状态` 和 `群文件状态` 的 Cookie 展示改为简洁的 `群cookie：生效/失效/未保存`，不再输出上次保活、失败次数、本地到期和原因等长文本。

## v2.11.4

### 调整

- QQ 官方机器人 Markdown Keyboard 在群聊中优先使用发送者 `openid` 走私聊接口 `/v2/users/{openid}/messages` 发送，拿不到 `user_openid` 时才回退群接口。
- 群聊按钮继续固定 `enter: True`，按钮格式和私聊一致，点击后直接发送。

## v2.11.3

### 调整

- QQ 官方机器人 Markdown Keyboard 指令按钮统一设置 `enter: True`，群聊按钮点击后直接发送，行为对齐私聊。

## v2.11.2

### 调整

- QQ阅读析API新增识别 `book.qq.com/book-detail/数字` 和 `novel.html5.qq.com/portal/novel-intro?bookid=数字` 链接。
- 卡片撤回白名单同步加入 `book.qq.com` 和 `novel.html5.qq.com`。

## v2.11.1

### 优化

- 书旗小说下载优先使用官方约 30 章一组的批量包 URL；批量包已解锁时会解析 `.sqb/.sqc` 正文后按章节顺序合并。
- 书旗批量包未解锁或 URL 为空时自动回退单章动态并发，并给单章网络断流、响应不完整等错误增加最多 3 次重试。

## v2.11.0

### 新增

- 新增 QQ阅读析API下载，支持 `h5.reader.qq.com/9/intro?bid=...`、`ubook.reader.qq.com/api/book/chapter-list?bid=...` 和 `154.12.91.167:7000/content?bookid=...&s=...&e=...` 链接。
- QQ阅读详情和目录使用官方 reader.qq.com 接口，正文使用析API `/content`，每批 50 章、最多 10 个动态并发下载。
- QQ阅读下载提示、文件名和 TXT 头部格式对齐番茄小说，并加入卡片撤回白名单。

## v2.10.1

### 调整

- 暂时移除踢人相关入口和帮助按钮，数字/卡片/闪传/合并转发撤回只撤回不踢人。
- 保留群文件清理、群文件状态和 Cookie 自动保活能力。

## v2.10.0

### 新增

- 群文件 Cookie 支持普通 Cookie 头、curl、Cookie Editor JSON 和 cookies.txt 格式；保存后默认开启后台保活，每 1 小时调用 qun.qq.com 账号级接口，只有 `p_skey` 时不做群文件接口回退保活。
- `状态` 同步显示群文件 Cookie/保活摘要和踢人功能开关，不展示 Cookie 原文。
- 新增 `开启踢人` / `关闭踢人`，状态写入 MySQL；广告/数字撤回累计 3 次后仅在开关开启时自动踢人，关闭时只撤回、不提醒。
- 手动 `@用户 踢` / `@用户 踢了` / `@用户 踢人` 支持批量踢人，不受自动踢人开关影响；优先使用已保存群文件网页 Cookie 调 qun.qq.com 踢人接口，失败回退适配器接口。

## v2.9.5

### 修复

- 修复 QQ 官方机器人数字撤回前没有读取群消息 `author.member_role`，导致 QQ 群管理员被当作普通成员撤回的问题。

## v2.9.4

### 修复

- 修复 QQ 官方机器人消息里的 `<@openid>` 未被识别为普通 @，导致“引用 + @ + 数字”被数字撤回误删的问题。

## v2.9.3

### 调整

- 七猫小说 TXT 头部详情对齐番茄格式，补充状态字段，并统一字数、书籍ID、章节数和简介展示顺序。

## v2.9.2

### 调整

- 书旗小说下载提示和 TXT 头部详情对齐番茄格式，补充状态、字数、书籍ID、章节数和简介字段。

## v2.9.1

### 修复

- 修复七猫短篇详情未读取 `author_info.name`，导致作者显示未知的问题。
- 七猫短篇下载提示会用目录字数字段补齐字数，并默认按完结短篇显示状态。

## v2.9.0

### 新增

- 新增书旗小说下载，支持 `shuqi.com/book/数字.html`、`t.shuqi.com/catalog/数字/`、`t.shuqi.com/cover/数字` 和短篇 `t.shuqi.com/shortNovel/reader/数字` 链接。
- 书旗下载会先请求官方批量下载接口获取批次信息，再以最多 80 个动态并发拉取章节正文；短篇非免费章节会保存预览正文。
- 新增 `开启书旗` / `关闭书旗` 小说开关，并在状态、帮助和卡片撤回白名单中加入书旗。

## v2.8.0

### 新增

- 七猫小说下载新增 App 批量正文接口，默认每批 500 章、最多 4 个批次并发，减少单章请求耗时。
- 七猫短篇分享链接改为走短篇详情、短篇目录和带 `reader_agent=1` 的批量正文接口，避免短篇被当成长篇下载失败。

## v2.7.9

### 修复

- OIAPI正文分段改为每段最多 250 万字、1200 章，识别服务端 Operation timed out / Curl.php 超时并停止下载，用户侧只提示 下载失败。
- 析API少量章节缺失时改为只补拉缺失章节，按 50 章、10 章、单章最多 3 轮重试，仍缺失才停止下载。

## v2.7.8

### 调整

- 析API正文批量接口 IP 从 `103.40.14.12` 切换为 `43.248.187.60`，端口和路径保持 `27042/multi-content` 不变。

## v2.7.7

### 优化

- 析API正文下载改为每批 800 章、最多 6 个批次动态并发，并保持最终章节顺序不变。
- 实测《冰山美女老婆的近身保镖》4297 章正文阶段由约 36 秒降到约 24.75 秒。

## v2.7.6

### 修复

- 析API正文正常批量改为每批 800 章，避免 1800 章大 GET 请求在部分书籍上偶发返回不完整。
- 析API遇到整批正文不完整时，会自动降级为 200 章小批重试，超过 800 章的小说会按多批继续下载。
- 析API单个章节返回空正文但已返回 `item_id` 时，不再导致整本失败，改为保留章节标题继续生成文件，并把空章节记录到日志。

## v2.7.5

### 调整

- 番茄小说下载遇到章节正文不完整、接口异常等内部错误时，用户侧只回复 `下载失败`，详细原因仅保留在日志中，避免长错误刷屏。

## v2.7.4

### 调整

- 析API正文下载切换为 `http://103.40.14.12:27042/multi-content` 批量接口，单次最多请求 1800 章，减少大量单章并发请求。
- 析API准备阶段改为使用番茄官方详情和目录接口，不再调用旧 `biek.top` 的 `search`、`detail`、`catalog`、`content` 流程。
- 析API正文返回不完整时立即停止下载并提示错误，不再把 `【下载失败】` 占位写入 txt。

## v2.7.3

### 调整

- OIAPI 正文范围请求分段改为同时受“每段最多 500 万字”和“每段最多 2500 章”限制，取满足两个限制的最少请求次数并尽量均分章节，避免章节数过多时一次请求过大。

## v2.7.2

### 修复

- 聚合API正文接口返回书籍不存在、停止合作或付费书籍不支持下载时，立即停止节点重试并回复 `该书不存在或已停止合作或付费书籍不支持下载，请去书城阅读新书`，不再等待所有节点重试后输出嵌套错误。

## v2.7.1

### 修复

- 聚合API正文批次失败后改为换完可用节点再判定失败，避免只试 2 个节点就把章节写成 `【下载失败】`。
- 聚合API新增批量正文降级重试：200 章批次失败后自动拆成 100/50 章重试，最终仍失败则停止当前下载并返回错误，不再把失败占位写入 txt。
- 聚合API节点测速增加小批量正文测试，过滤详情可用但正文接口超时或返回不完整的节点。

## v2.7.0

### 新增

- 聚合API改为多节点高速下载：支持 `modtianmingyun-fanqie1` 到 `fanqie10.ms.show` 以及 `fanqieapi.6666633.xyz:9981-9983` 同格式节点，自动测速筛选可用节点。
- 聚合API请求切换为中文文档里的 `/api/v1/books/{book_id}`、`/api/v1/books/{book_id}/directory` 和 `/api/v1/chapters/full`，正文按每批 200 章、最多 40 个动态并发混合节点下载，并兼容 `data.item_infos` 和章节 ID 映射格式。
- 实测《超级兵王》8284 章完整下载：准备约 4 秒，正文约 15.5 秒，全部章节成功获取。

## v2.6.3

### 修复

- OIAPI 正文请求返回 `请检测章节选择是否正确` 时，额外请求一次 OIAPI `method=chapters` 目录，并使用 OIAPI 返回的章节数量单次重试正文请求；第二次仍失败则立即停止，不拆分、不循环，避免继续消耗 key 额度。

## v2.6.2

### 修复

- 群管卡片撤回新增识别 QQ 官方机器人空间相册分享降级展示文本 `暂不能查看该消息内容`，避免该类卡片进入网页群文件等普通命令处理。

## v2.6.1

### 修复

- 修复 OIAPI 正文请求遇到 `key额度不足` 等错误后仍递归拆分重试，导致短时间大量消耗 key 额度的问题。
- OIAPI 正文批次请求现在任意错误都会立即停止当前下载，不再拆分、不再重试，也不继续请求后续批次。

## v2.6.0

### 新增

- 新增番茄小说 Y6api 下载接口（`https://y68-napi.hf.space`），支持通过 `查看API` 后发送 `5` 或直接发送 `y6api` 切换。
- Y6api 使用 `/api/download` 后端打包下载完整 TXT，并重新解析简介和章节正文后复用现有统一 TXT 构建，输出与 OIAPI 一致的文件头部。

## v2.5.4

### 修复

- 修复 UC 网盘同名冲突时改用备用远端文件名导致目录中堆积重复小说文件的问题；现在冲突后会重新分页扫描并删除同名普通文件，再继续使用原小说文件名重试。

## v2.5.3

### 调整

- 聚合API章节正文下载改为每批 50 章、最多 10 个批次并发。

## v2.5.2

### 调整

- 聚合API章节正文下载改为每批 15 章、最多 100 个批次并发。

## v2.5.1

### 修复

- 第 4 个番茄小说下载接口统一显示为「聚合API」，独立模块路径调整为 `功能文件/API功能/聚合API/番茄小说.py`。
- API 快捷切换只保留 `聚合api`，不再保留旧别名兼容入口。
- 删除仓库内测试文件。

## v2.5.0

### 新增

- 新增聚合API下载接口（`http://101.35.133.34:5000`），独立模块位于 `功能文件/API功能/聚合API/番茄小说.py`。
- `查看API` 新增第 4 项「聚合API」，支持发送 `4` 或 `聚合api` 切换；状态页和帮助菜单同步识别该接口。
- 聚合API下载流程使用 `/api/detail` 获取书籍详情、`/api/book` 或 `/api/directory` 获取目录、`/api/content?tab=批量` 获取章节正文，并复用现有统一 TXT 构建、UC/百度网盘和 QQ 文件发送链路。

## v2.4.11

### 修复

- 恢复到新增番茄自建API前的番茄 API 结构：移除自建API模块和切换入口，只保留 OIAPI、析API、崩溃API。
- 番茄 OIAPI 主模块恢复为 v2.3.6 的稳定下载逻辑，避免自建API接入后影响原有接口。

## v2.4.10

### 修复

- 恢复自建API加入前 OIAPI 使用的番茄官方详情和目录接口：优先读取 `fanqienovel.com/api/book/info` 与 `fanqienovel.com/api/reader/directory/detail`，避免当前目录接口拿不到章节时直接报 `没有获取到章节目录`。
- 兼容详情字段里的 `serialCount`、`chapterTotal`、`all_chapter_num` 等章节数字段，作为 OIAPI 目录兜底依据。

## v2.4.9

### 修复

- 修复番茄链接识别时把正则最外层完整匹配 URL 当成书籍 ID，导致 `changdunovel.com/t/短码` 传给 OIAPI/自建API 时变成 URL 而不是数字 `book_id`，从而出现 OIAPI `获取书籍信息失败` 或自建API `没有获取到章节目录` 的问题。

## v2.4.8

### 修复

- 修复添加自建API后 OIAPI 仍从模块级空配置读取 `番茄小说key`，导致 WebUI 已填写 key 但下载时报 `番茄小说key为空` 的问题；现在 OIAPI 使用当前插件实例传入的配置，并兼容 `basic_settings` 分类和旧字段名。

## v2.4.7

### 修复

- 自建API目录解析只在 `功能文件/API功能/自建API/番茄小说.py` 内处理，新增 `/api/directory` 兜底，并兼容 `chapterListWithVolume`、`lists` 等不同目录结构。
- 移除番茄下载前额外发送的 `当前使用xxx下载 / 正在获取书籍信息...` 预提示；下载前只保留统一的书名、作者、状态、章节、字数详情提示。

## v2.4.6

### 修复

- 修复自建API批量正文返回的章节 ID 在 `novel_data.item_id` 时未被识别，导致章节内容被写成 `【下载失败】` 的问题。
- 恢复番茄小说析API和崩溃API的独立模块调用方式，避免新增自建API后影响原有API下载。
- `查看API` 现在会显示当前使用的番茄API，并按提示只在 120 秒内接受数字切换。

## v2.4.5

### 修复

- 修复番茄小说 API 当前选择读取只兼容 `1` / `2` / `3` / `4`，不兼容旧版数据库里保存的 `OIAPI` / `析API` / `崩溃API` / `自建API` 名称，导致升级后原接口可能被误判为 OIAPI 的问题。

## v2.4.4

### 修复

- 修复番茄小说 API 切换调用不存在的 `用户激活.保存运行状态` / `读取运行状态`，导致发送 `自建api`、`查看API` 后选择数字等切换指令时抛出 `AttributeError` 的问题。
- 番茄小说 API 当前选择改为直接使用 `mantou_runtime_state` 对应的 `运行状态数据库` 读写接口；读取失败默认 OIAPI，写入失败会回复明确错误，不再抛到 AstrBot pipeline。

## v2.4.3

### 修复

- 番茄小说 API 切换改为使用插件配置 `group_file_cleanup_admin_qq` 白名单鉴权，避免白名单管理员不是 QQ 群主/管理员时无法切换到自建API。
- README 功能导航补充自建API和 `查看API -> 4` / `自建api` 快速切换说明。
- 移除番茄小说主模块中未使用的 `小说功能开关` 早期取值，降低重载时模块加载顺序影响。

## v2.4.2

### 修复

- 修复 `获取番茄小说回复流` 函数签名与 main.py 调用不匹配（缺少 `config` 参数）的问题。
- 重构番茄小说下载流为外层同步匹配+内层 async generator 模式，与七猫小说保持一致，避免 async generator 无法返回 None 导致的识别逻辑异常。
- 移除回复流中重复的功能开关检查、激活拦截检查和不存在的 `检查是否最后一本免费` 调用，统一由 main.py 处理免费额度提示。

## v2.4.1

### 修复

- 修复 `处理番茄小说API指令` 异步函数调用缺少 `await`，导致协程对象被当作字符串传入 `plain_result` 触发 `str type expected` 报错。

## v2.4.0

### 新增

- 新增番茄小说自建API支持（`http://101.35.133.34:5000`）：
  - 创建独立模块目录 `功能文件/API功能/自建API/番茄小说.py`。
  - 支持书籍详情获取（`/api/detail`）、章节目录获取（`/api/book`）、章节正文批量获取（`/api/content?tab=批量`）。
  - 章节正文按30章为一批下载，并发数自动调整（最多100并发，300章则10并发）。
  - API选择菜单新增选项「4. 自建API」，与OIAPI、析API、崩溃API切换方式一致。
  - 批量下载失败自动重试3次，失败章节标记为【下载失败】。

## v2.3.6

### 修复

- **OIAPI"请检测章节选择是否正确"时不再无效拆分重试**：直接抛出错误，不再二分拆分到单章（拆分对这种错误完全无效，每章都会报同样错误）。
- **OIAPI章节选择错误时调用chapters获取正确章节数后重试**：正常情况按原逻辑用1-n横杠范围下载；仅当OIAPI返回"请检测章节选择是否正确"时，调用OIAPI `method=chapters` 获取真实章节列表（含正确章节数量n），替换目录后用1-n重新下载一次。析API、崩溃API不受影响。

## v2.3.5

### 修复

- **仅OIAPI："请检测章节选择是否正确"时调用chapters刷新目录重试**：正常情况下按原有逻辑使用1-n横杠范围获取章节正文，只有当OIAPI返回"请检测章节选择是否正确"时，才调用OIAPI `method=chapters` 获取真实章节列表后重试一次。析API和崩溃API不受影响。恢复章节参数为1-n横杠格式，恢复原初始目录获取流程，去掉不必要的每批50章限制。

## v2.3.4

### 修复

- **OIAPI章节参数改用英文逗号分隔**：根据OIAPI官方文档，`chapter` 参数使用英文逗号分隔（如 `1,2,3`），不再使用横杠范围格式（如 `1-243`），避免"请检测章节选择是否正确"错误。
- **OIAPI章节选择错误时自动刷新章节目录重试**：仅OIAPI生效。当官方章节目录或构造序号目录下载章节正文返回"请检测章节选择是否正确"时，自动请求OIAPI `method=chapters` 获取真实章节列表（含正确的chapter_id和index），用真实目录重新下载，仅重试一次。
- **每批章节数限制为50章**：防止逗号分隔的章节参数URL过长，同时加快错误检测和重试速度。

## v2.3.3

### 修复

- **区分永久性业务错误和范围类错误的重试策略**：付费内容、Key错误、书籍不存在等永久性错误直接向上抛出，不拆分重试，直接显示具体错误给用户；"请检测章节选择是否正确"等范围/格式类业务错误仍保留二分拆分重试机制，通过拆分成更小范围解决单批请求过大导致的失败。
- **所有章节下载失败时提取具体原因**：不再统一显示"OIAPI没有获取到可用章节正文"，优先提取永久性错误原因显示给用户。

## v2.3.2

### 修复

- **官方目录和OIAPI chapters都失败时，通过OIAPI detail获取章节数量兜底**：当官方章节目录API和OIAPI method=chapters都无法获取章节列表时，新增请求OIAPI method=detail获取书籍信息（包含chapter_count），用章节数构造序号目录继续下载，不再直接报"没有获取到章节目录"。
- **付费内容等业务错误不再无效拆分重试**：`下载章节批次` 中 `用户可见错误`（如"付费内容不解析"）直接向上抛出，不再递归二分拆分重试，外层直接将API返回的错误消息回复给用户；网络超时、HTTP错误、返回不完整等临时性错误仍保留拆分重试。
- **OIAPI chapters接口业务错误不再中断流程**：`请求章节目录数据` 遇到 `用户可见错误` 时返回空字典而非抛出异常，让上层继续尝试其他兜底方案（OIAPI detail、构造序号目录）。

## v2.3.1

### 优化

- 番茄小说书籍详情优先使用App API（`api.fqnovel.com/reading/bookapi/share/detail/v1`）获取：
  - 当分享链接包含 `share_code` 参数时，优先通过App API获取书籍元数据（书名、作者、字数、状态、章节数、简介）。
  - App API请求失败或无 `share_code` 时，自动降级到 `fanqienovel.com/api/book/info` 网页API。
  - 章节目录仍使用 `fanqienovel.com/api/reader/directory/detail` 获取，OIAPI仅负责章节正文。

## v2.3.0

### 重构

- 番茄小说元数据和章节目录改用官方API获取，OIAPI仅用于获取章节正文：
  - 书籍信息（书名、作者、字数、状态、简介）使用 `fanqienovel.com/api/book/info` 获取，不再请求旧的落地页API和网页HTML解析。
  - 章节目录（章节ID、标题、序号）使用 `fanqienovel.com/api/reader/directory/detail` 获取，不再通过OIAPI的 `method=chapters` 请求目录。
  - OIAPI现在只负责获取章节正文内容（`method=chapter`），大幅减少冗余API请求。
  - 旧的落地页API和网页HTML获取方式保留为降级回退方案。
- 修复旧官方落地页API（`novel_ug/share/landing_page`）对 `share_type=0` 链接返回500错误导致详情获取失败的问题，新API完全兼容各类分享链接和直接book_id。

## v2.2.28

### 修复

- 修复番茄小说部分 `changdunovel.com` 分享链接（`wap/share-v2.html` 类型）无法获取书籍详情的问题：官方落地页API对 `share_type=0` 的链接返回500错误，导致书名/作者等信息获取失败。
- 优化番茄小说OIAPI下载请求流程：先请求OIAPI章节目录接口并从响应中提取书籍信息，仅当API未返回足够详情时才请求官方落地页和网页补全；链接中已直接包含 `book_id` 时跳过分享页面请求，减少下载前的HTTP请求次数（从4次减少到1-2次）。
- 修复落地页API错误处理：HTTP状态码非200时快速跳过，不再尝试解析JSON导致异常；请求超时从20秒缩短到15秒。
- 修复 `展开番茄短链` 函数中存在死代码（`return` 后不可达代码）的问题。

## v2.2.27

### 优化

- 登录群文件页面：「🌐 点击登录」按钮改为「🔗 打开链接」，新增「登录账号」按钮，点击后进入60秒Cookie输入等待状态，支持直接粘贴Cookie发送保存。
- 未登录时点击「清理群文件」或「清理全部群文件」改为Markdown+Keyboard按钮页面（包含「🔗 打开链接」「登录账号」「返回上一步」按钮），不再是纯文本提示。
- 「清理全部群文件」执行完成后结果页新增「返回上一步」按钮；无待清理群提示页也添加返回按钮。

## v2.2.26

### 修复

- 修复「删除群聊」「更改备注」按钮位置问题：这两个按钮仅保留在群文件清理详情页，查看群聊页面不再重复显示。
- 修复查看群聊页面的返回逻辑。

## v2.2.25

### 修复

- 修复帮助菜单所有按钮发送数字而非按钮文字的问题：大类、小类、触发项列表按钮现在统一发送按钮上显示的中文名称，不再依赖数字编号导航（仅纯文本版保留数字输入兼容）。
- 修复「登录群文件」页面「返回上一步」按钮跳转错误：新增登录页面状态跟踪，点击返回正确回到群文件清理详情页；保存 Cookie 成功后 QQ 官方机器人自动回到群文件清理详情页。
- 修复「删除群聊」页面返回问题：删除群聊后无剩余群时结果页缺少返回按钮，空列表时也显示返回按钮。
- 修复「授权群聊」页面无返回按钮的问题：QQ 官方机器人下授权链接页面改为 Markdown + Keyboard 按钮交互，新增「🌐 点击授权」跳转按钮和「返回上一步」按钮；错误提示页也带返回按钮。
- 修复更改备注等待状态中发送「返回上一步」无响应的问题：备注输入等待时发送「返回上一步」可取消并回到查看群聊列表。
- 修复网页群文件返回优先级检查，确保备注等待、登录页面等子状态下的返回上一步不被其他处理器拦截。

## v2.2.24

### 修复

- 修复插件重载/安装时因模块加载顺序导致 cannot import name '生成链接按钮' 导入失败的问题：将生成链接按钮函数定义移至网页群文件模块内，并调整 main.py 模块加载顺序，确保基础功能模块（帮助功能等）在依赖它们的群聊功能模块之前完成 reload。

## v2.2.23

### 新增

- 群文件清理「登录群文件」页面改为 QQ 官方机器人 Markdown + Keyboard 按钮交互，新增「🌐 点击登录」跳转按钮（直接打开 QQ 登录链接）和「返回上一步」按钮；普通机器人保持原有纯文本提示。

## v2.2.22

### 修复

- 修复清理群文件选择列表页点击「返回上一步」错误跳转到待清理群列表（查看群聊）的问题，改为正确返回群文件清理帮助详情页。

## v2.2.21

### 调整

- 删除群文件清理「更改备注」等待状态中的「取消备注」按钮，改为超时自动取消；同步更新帮助菜单、README 和 AGENTS.md 文档。

## v2.2.20

### 修复

- 修复查看群聊的「返回上一步」越级到帮助大类的问题：根因是帮助状态过期（120秒）或直接发命令进入时无帮助状态，落到帮助功能「无帮助状态回大类」分支导致越级。现在 `处理查看群聊返回` 直接跳转回「群文件清理」触发项详情页并刷新帮助状态，固定回到详情页，不再依赖帮助状态是否过期，避免越级。

## v2.2.19

### 修复

- 修复查看群聊的「返回上一步」按钮点击后显示「群文件清理」详情页而非真正返回上一步的问题：v2.2.16 把按钮改成发送「群文件清理」导致显示详情页；现改回发送「返回上一步」，新增查看群聊会话状态（300秒有效），查看群聊状态下收到「返回上一步」时退出查看群聊状态并返回 None，让帮助功能按状态机正常回退一层（从详情页回触发项列表，或从触发项回小类，符合帮助菜单层级语义）。

## v2.2.18

### 优化

- 删除帮助菜单「群文件清理」触发项详情页的「关闭删除群聊」快捷命令按钮：该按钮与「查看群聊」功能重复（都退出删除模式回查看群聊），现仅保留「查看群聊」等快捷命令；详情文字里「点关闭删除群聊退出」同步改为「点返回上一步退出」。「关闭删除群聊」命令本身仍可手动发送生效。

## v2.2.17

### 修复

- 修复删除模式下手动发送「关闭删除群聊」命令可能无效的问题：`处理删除模式返回` 现在同时识别「返回上一步」和「关闭删除群聊」作为退出删除模式的方式，两者都会退出删除模式并回到查看群聊列表。删除模式按钮显示的仍是「返回上一步」，但「关闭删除群聊」命令作为兼容入口继续有效。

## v2.2.16

### 修复

- 修复查看群聊页面的「返回上一步」按钮越级返回到帮助菜单「管理功能」小类的问题：根因是按钮发送「返回上一步」被帮助状态机处理，按帮助层级返回上一层（详情→触发项→小类→大类）；现在按钮改为发送「群文件清理」，命中帮助功能的触发项名称匹配，跳转回「群文件清理」触发项详情页，无论从帮助菜单还是直接发命令进入都能正确返回详情页。其余页面（删除模式、更改备注选群、清理选择列表）的返回行为已确认无越级问题。

## v2.2.15

### 优化

- 清理群文件选择单个群清理成功后，现在重新列出剩余群的选择列表（QQ 官方机器人下显示 markdown 群列表 + 群号按钮 + 返回上一步按钮），顶部带「已选择群 xxx，开始清理 + 清理结果」前缀提示，可直接继续点按钮清理其他群；不再只返回纯文本提示。

## v2.2.14

### 修复

- 修复清理群文件选择列表的「返回上一步」按钮点击后提示「已取消清理群文件选择」而非返回上一层的问题：现在「返回上一步」退出清理等待状态后跳转到查看群聊列表（QQ 官方机器人下显示 markdown 群列表 + 删除群聊/更改备注/返回上一步按钮），与删除模式、更改备注模式的返回行为一致；「0」保持原有的取消提示。

## v2.2.13

### 优化

- 清理群文件选择单个群清理成功后不再立即退出清理等待状态，现在保持状态并刷新剩余群列表和过期时间，可继续发送编号清理其他群，发送「0」或「返回上一步」才结束；所有群清理完后自动退出。修复清理「1」后发「2」落到帮助菜单（可能误进全员禁言详情页）的问题。

## v2.2.12

### 修复

- 修复清理群文件选择状态过期后发送数字（如「2」）落到帮助菜单、误进「全员禁言」详情页的问题：状态过期时数字或「返回上一步」返回「清理群文件选择已过期」提示，不再继续下发到帮助功能。
- 修复清理群文件选择按钮点击发送的是按钮文字（群号或备注）而非编号时无法清理的问题：清理选择回复现在同时匹配编号、群号和备注，按钮无论发送编号还是文字都能正确清理对应群。
- 新增清理选择回复诊断日志，便于排查会话标识不匹配等问题。

## v2.2.11

### 修复

- 修复 v2.2.9 调整 main.py 调用顺序后，帮助菜单里点「群文件清理」按钮（发送「群文件清理」）被网页群文件清理直接执行、不再跳转帮助详情页的问题：恢复帮助功能优先处理，但删除模式或清理群文件选择等待状态下「返回上一步」仍优先由网页群文件功能处理（退出删除模式/取消清理选择），避免被帮助功能拦截。

## v2.2.10

### 优化

- 清理群文件选择群列表时的「取消」按钮改为「返回上一步」，按钮显示与点击发送内容一致；清理等待状态下收到「返回上一步」与「0」均会取消清理选择，行为不变。

## v2.2.9

### 修复

- 修复删除群聊模式下「返回上一步」按钮发送的是「关闭删除群聊」而非「返回上一步」的问题：新增删除模式会话状态，进入删除模式时记录状态，按钮 data 改为发送「返回上一步」，删除模式下收到「返回上一步」时退出删除模式并回到查看群聊列表；同时调整 main.py 调用顺序，让网页群文件清理先于帮助功能处理，避免「返回上一步」被帮助功能在无帮助状态时拦截。「关闭删除群聊」命令仍可手动退出删除模式。

## v2.2.8

### 优化

- 「添加群聊 群号」添加成功后自动跳转到「查看群聊」页面（QQ 官方机器人下显示 markdown 群列表 + 删除群聊/更改备注/返回上一步按钮），顶部带「已添加：xxx」「当前共 N 个待清理群」前缀提示，不再只回复纯文本。

## v2.2.7

### 修复

- 修复「查看群聊」「更改备注」等页面点「返回上一步」按钮无效的问题：当用户直接发送命令（非从帮助菜单进入）时没有帮助状态，`返回上一步` 不匹配任何处理返回 None；现在帮助功能（纯文本和 MD 两个入口）收到 `返回上一步` 且无帮助状态时，自动回到帮助大类页面。

## v2.2.6

### 修复

- 修复「更改备注」无法保存的根因：`获取群号` 调用 `event.get_group()` 时，AstrMessageEvent 的 `get_group` 是 async 方法，同步调用返回协程对象，`str()` 变成 `<coroutine object ... at 0x...>` 污染会话标识，导致两次消息的会话标识不一致，备注等待状态无法命中；现在检测到协程对象就主动 close 并跳过，改用后续字段读取（私聊则返回空，标识为 `private:发送者`）。

## v2.2.5

### 修复

- 修复群文件清理按钮交互（查看群聊、删除群聊、更改备注等）markdown 发送成功后返回空字符串，main.py 仍执行 `plain_result("")` 导致「消息链全为 Reply 和 At 消息段, 跳过发送阶段」警告的问题；空字符串现在视为「已自行发送」直接跳过。

## v2.2.4

### 诊断

- 网页群文件清理入口、进入备注等待、处理备注等待回复三处加 `[网页群文件诊断] 日志，记录命令文本、会话标识、等待状态命中情况，用于排查「更改备注」发送备注后无回复的问题。

## v2.2.3

### 修复

- 网页群文件清理入口 `处理网页群文件清理` 加 try/except 异常捕获，内部抛异常时记录 `[网页群文件诊断]` 日志并返回错误提示，不再无回复；用于排查「更改备注」等命令完全无回复的问题。

## v2.2.2

### 修复

- 群文件清理「更改备注」保存成功后改为直接跳转回「查看群聊」页面（带删除群聊/更改备注/返回上一步按钮），不再只回复文本。
- 群号按钮标签改为：有备注直接显示备注（超 12 字符截断加省略号），无备注显示群号，不再使用 `群号（备注）` 格式。
- 「查看群聊」页面新增「返回上一步」按钮，点击后发送「帮助」回到帮助菜单，与删除模式、更改备注选群等页面的返回按钮保持一致。

## v2.2.1

### 新增

- 群文件清理的群号按钮标签现在会显示备注：清理群文件选群、删除模式、删除结果刷新列表、更改备注选群四个页面的群号按钮在有备注时显示为 `群号（备注）`，备注超长自动截断加省略号；按钮 `data` 字段仍发送对应指令或序号，不受影响。

## v2.2.0

### 新增

- 群文件清理新增「更改备注」功能：群文件清理管理员白名单 QQ 发送「更改备注」后列出群号按钮，点群号按钮进入 60 秒等待状态，下一条消息作为该群备注保存（namespace=`cleanup_groups`，state_key=`remark:{群号}`），发送「取消备注」可取消；备注在「查看群聊」「清理群文件」「删除群聊」列表中显示；删除群聊时一并清空对应备注。

### 修复

- 修复群文件清理按钮点击无效问题：所有指令类按钮（删除群聊、关闭删除群聊、更改备注、取消备注、群号按钮、取消按钮）统一显式设置 `data为标签=False`，按钮 `data` 字段发送指令文本而非标签文本，避免「返回上一步」「取消」等按钮点击后发送标签文字导致命令无法识别。

## v2.1.1

### 调整

- 群文件清理相关按钮统一改为点击直接发送（`enter: True`），不再填入输入框，群聊和私聊一致。
- 查看群聊删除模式的「关闭删除群聊」按钮改为「返回上一步」，更直观。
- 「清理群文件」/「群文件清理」列出群列表时，QQ 官方机器人下也显示群号按钮（点击发送编号）和取消按钮，不再只发纯文本。

## v2.1.0

### 新增

- 查看群聊新增 QQ 官方按钮交互：发送「查看群聊」后底部显示「删除群聊」按钮，点击进入删除模式显示每个群号按钮，点击群号按钮直接删除该群并刷新列表，底部「关闭删除群聊」按钮退出删除模式回到普通查看列表。
- 新增指令「删除群聊」（无参数，进入删除模式）和「关闭删除群聊」（退出删除模式，回到普通查看列表）。
- 群号按钮每行 2 个，加关闭按钮超过 5 行自动降级为无按钮 markdown；私聊自动发送命令，群聊填入输入框。

### 调整

- 「删除群聊 群号」删除后在 QQ 官方下自动刷新剩余群按钮列表（保持删除模式），普通机器人返回纯文本结果。
- 普通机器人查看群聊仍为纯文本列表 + 提示，无按钮。
- 帮助菜单「群文件清理」触发项新增「删除群聊」「关闭删除群聊」快捷命令按钮。

## v2.0.2

### 调整

- 网页群文件清理 Cookie 改为存 MySQL `mantou_runtime_state`（namespace=`web_group_file_cookie`，state_key=管理员QQ，state_value 为 JSON `{cookie, expire}`），不再写本地 JSON 文件。
- 读取时检查 expire，过期自动清空对应记录并视为未登录。
- 删除 `功能文件/下载缓存/网页群文件Cookie.json` 相关读写逻辑，移除不再使用的 `pathlib.Path` 导入。
- Cookie 仍按管理员 QQ 隔离，31 天有效，不暴露到 `状态` 命令。

## v2.0.1

### 调整

- 删除「清空群文件 <群号>」指令，「清理群文件」/「群文件清理」改为列出「添加群聊」里的目标群列表后发送编号清理对应群（120 秒内有效，发送 0 取消），等待状态按会话和发送者 QQ 隔离，过期自动失效。
- Cookie 继续存本地 JSON 文件 `功能文件/下载缓存/网页群文件Cookie.json`，按管理员 QQ 隔离，31 天有效，不写入 MySQL。
- 帮助菜单「群文件清理」触发项移除「清空群文件」，详情说明清理群文件的选择交互。

## v2.0.0

### 破坏性变更

- 删除适配器扩展接口版群文件清理 `功能文件/管理功能/群聊功能/群文件清理.py`（`get_group_root_files`/`get_group_files_by_folder`/`delete_group_file` 递归清理逻辑）。
- 群文件清理职责统一由网页接口版 `功能文件/管理功能/群聊功能/网页群文件.py` 承担：`清理群文件` / `群文件清理` / `清理全部群文件` / `清空群文件 群号` / `登录群文件` / `群文件登录cookie` 现全部走 `pan.qun.qq.com` 网页接口，需要管理员先登录拿 Cookie，不再依赖机器人是群主/管理员。
- 群聊管理（`添加群聊` / `删除群聊` / `查看群聊` 及 MySQL 读写）随 `群文件清理.py` 删除一并并入 `网页群文件.py`。
- 行为变化：网页接口只删群根目录文件，不递归子文件夹；清理前不再检查机器人 QQ 群角色，改为按管理员 Cookie 鉴权。

### 调整

- `main.py` 移除 `群文件清理功能` 模块加载和分发链，群文件清理指令统一由 `网页群文件功能.处理网页群文件清理` 入口处理。
- 抽出核心清理函数 `执行网页清理`，供 `清空指定群文件`、`清理全部已添加群` 复用；`清理全部已添加群` 复用一个 aiohttp session 逐群清理并汇总成功/失败。
- 帮助菜单「群文件清理」触发项合并网页接口全部指令，快捷命令新增 `清空群文件`。
- 同步更新 README、AGENTS 反映网页接口统一承担清理职责。

## v1.49.1

### 重构

- 将 `群聊管理.py` 的待清理目标群列表逻辑（`添加群聊` / `删除群聊` / `查看群聊` 及 MySQL 读写）合并进 `群文件清理.py`，删除独立模块 `群聊管理.py`。
- `main.py` 移除 `群聊管理功能` 模块加载和分发链，`处理群文件清理` 入口统一处理清理与群聊管理指令。
- 帮助菜单「管理功能」小类移除独立的「群聊管理」触发项，把 `添加群聊` / `删除群聊` / `查看群聊` 合并进「群文件清理」触发项；该触发项快捷命令新增 `查看群聊`，小类触发项恢复为 8 个。

## v1.49.0

### 新增

- 新增群聊管理功能（`添加群聊` / `删除群聊` / `查看群聊` 指令），仅群文件清理管理员白名单 QQ 可用，支持一次添加或删除多个群号。
- 待清理目标群列表写入 MySQL 内置表 `mantou_runtime_state`（namespace=`cleanup_groups`，state_key=`group_list`），重载后继续生效。
- 帮助菜单「群文件清理」详情页新增 `登录群文件` 快捷命令按钮，管理员可一键进入网页接口版群文件清理的登录流程；该详情页按钮按每行 2 个排列（清理群文件 / 清理全部群文件 / 登录群文件 + 返回上一步），共 3 行，未超 QQ 官方 5 行限制。

### 调整

- `清理全部群文件` 改为只清理「添加群聊」里添加的目标群，不再扫描机器人所在全部群；列表为空时会提示先发送「添加群聊 群号」添加目标群。
- `群文件清理.py` 移除不再使用的 `获取机器人所在群号列表` 导入。

## v1.48.0

### 新增

- 新增网页接口版群文件清理功能 `功能文件/管理功能/群聊功能/网页群文件.py`，与适配器扩展接口版的 `群文件清理.py` 相互独立，指令不冲突。
- 新增指令 `登录群文件`、`群文件登录cookie <cookie>`、`清空群文件 <群号>`，仅群文件清理管理员白名单 QQ 可用。
- 通过 QQ 群文件网页接口 `pan.qun.qq.com` 拉取文件列表（每页 50 并发）和批量删除（每批 20 并发，失败重试 3 次），从 Cookie 提取 `skey` 计算 `bkn` 鉴权。
- Cookie 按管理员 QQ 隔离保存到 `功能文件/下载缓存/网页群文件Cookie.json`，31 天有效，过期自动失效，不暴露到 `状态` 命令。
- `main.py` 加载 `网页群文件功能` 模块并接入 `处理网页群文件清理` 调用，串行分发链路紧贴原 `群文件清理功能` 调用之后。
- `AGENTS.md` 同步新增网页接口版群文件清理规则。

## v1.47.8

### 修复

- 卡密改为全局共享：所有群、私聊、配置同步的卡密统一用 `group_id="global"` 存储，A 群生成的卡密 B 群也能查询和使用，解决 QQ 官方机器人下不同群 `group_openid` 不同导致查不到卡密的问题。
- `生成数据库卡密列表`、`列出数据库卡密记录`、`筛选存在数据库卡密列表`、`使用数据库卡密激活`、`获取卡密天数列表`、`准备配置卡密写入记录`、`解析配置卡密记录` 全部去掉按群过滤卡密的逻辑。
- 卡密激活时仍按当前 `group_id` 写入激活记录（激活状态按群隔离不变）。

## v1.47.7

### 新增

- 卡密查询详情页的「复制」按钮改为动态生成：根据数据库当前群内卡密实际天数生成「复制 N」按钮（如数据库有 30 天卡密就显示「复制 30」），从低到高排列，一行 3 个。
- 新增 `获取卡密天数列表(配置, 群号)` 同步数据库查询函数：取 `mantou_user_activation_cards` 表 `days` 列去重并升序返回。
- 详情键盘（卡密查询等）也接入 `检查键盘行数` 5 行限制保护，超限自动降级为纯文本 markdown。

## v1.47.6

### 优化

- 卡密查询详情页去掉「上一页」「下一页」按钮，翻页命令在查询结果列表里提示，详情页只保留查询筛选和复制按钮：查询卡密 使用 / 查询卡密 没使用 / 复制 + 返回上一步。

## v1.47.5

### 优化

- 触发项名称「生成卡密」改为「卡密生成」、「查询卡密」改为「卡密查询」（与按钮 data 一致，且不触发真实命令）。
- 删除 `按钮data` 字段，名称本身就是安全的字，按钮 data 直接用名称。
- 查询卡密详情页按钮改成完整命令文字：查询卡密 使用 / 查询卡密 没使用 / 复制 / 下一页 / 上一页 + 返回上一步（标签=data=命令文字，点击即执行对应查询）。

## v1.47.4

### 新增

- 查询卡密详情页加快捷命令按钮：已使用/未使用/复制/下一页/上一页 + 返回上一步。
- 「已使用」「未使用」按钮 data 用 `1`/`2`（匹配查询卡密菜单的数字选项），标签显示中文。
- `生成帮助详情键盘` 支持字典格式快捷命令项 `{"data": "...", "label": "..."}`，data 用命令值，label 显示中文。

## v1.47.3

### 修复

- 修复 `按钮data` 字段未生效的 bug：`生成小类触发项键盘` 传参时漏了 `data为标签=False`，导致按钮 data 仍用标签（触发项名称"生成卡密"），点击后触发真实命令而非进入详情页。现在点击「生成卡密」按钮发送 `卡密生成`，「查询卡密」按钮发送 `卡密查询`，都只进入详情页显示快捷命令按钮，不触发真实命令。

## v1.47.2

### 修复

- 触发项名称「生成卡密操作」改回「生成卡密」、「查询卡密记录」改回「查询卡密」。
- 按钮显示「生成卡密」「查询卡密」，但点击后发送 `卡密生成`/`卡密查询`（不触发真实命令），只进入帮助详情页显示快捷命令按钮，不会直接生成卡密或查询卡密。
- 新增 `按钮data` 字段：标记后按钮标签显示名称，但 data 用指定值；`匹配任意层级名称` 也匹配 `按钮data`。

## v1.47.1

### 新增

- 生成卡密操作详情页加快捷命令按钮：生成1张/5张/10张/20张/50张/100张 + 返回上一步（6个按钮每行2个=3行 + 返回1行，不超 QQ 官方 5 行限制）。
- 查询卡密记录详情页加快捷命令按钮：下一页/上一页 + 返回上一步。
- 用户列表详情页加快捷命令按钮：下一页/上一页 + 返回上一步。

## v1.47.0

### 新增

- 激活功能小类下所有触发项按钮 data 改用文字（不再用数字）：激活用户、重置用户、增加时间、减少时间、查询信息、用户列表、生成卡密操作、查询卡密记录。
- 会触发真实命令的触发项名称改为不触发的字：查询→查询信息、查询用户→用户列表、生成卡密→生成卡密操作、查询卡密→查询卡密记录（已验证不匹配激活/卡密命令正则）。
- 新增 `按钮字触发` 小类标记字段，标记后该小类所有触发项按钮 data 用名称，点击后发送名称进入帮助详情页，不会触发真实命令。

## v1.46.9

### 修复

- 修复点击帮助菜单「用户激活」小类按钮会触发激活命令的 bug：根因是小类名「用户激活」匹配激活命令正则 `(?:用户)?(?:激活)`。
- 小类名「用户管理」改为「激活功能」：「激活功能」不匹配激活命令正则（正则要求「激活」后是空格或结束，不能直接跟「功能」），点击按钮发送「激活功能」不会触发激活，只会进入帮助小类。

## v1.46.8

### 修复

- 回滚 v1.46.7 的帮助别名方案（用户反馈不要）。
- 「用户激活」小类名改为「用户管理」，避免按钮 data 含「激活」字被误识别为激活命令。
- 触发项按钮 data 恢复为：有快捷命令的触发项用名称，其余用数字。

## v1.46.7

### 调整

- 帮助菜单所有触发项按钮 data 统一改用帮助别名（格式 `帮助-xxx`，如 `帮助-状态`、`帮助-踢人`、`帮助-查询` 等），这些别名只在帮助菜单内部识别，不会触发真实命令；点击后进入该触发项详情页。
- 所有触发项（含踢人、授权链接、状态、帮助、随机英文单词、随机一言、疯狂星期四、古诗词名句、用户激活系列、小说系列、群管系列）均新增 `帮助别名` 字段。
- `匹配任意层级名称` 增加帮助别名匹配逻辑。

## v1.46.6

### 调整

- 帮助菜单按钮点击发送内容优化：
  - 大类名恢复为 `主动触发` / `被动触发`（非命令字，可安全作按钮 data）。
  - 触发项列表中，有快捷命令的触发项（全员禁言、付费开关、小说开关、群文件清理、番茄小说API切换）按钮 data 改用触发项名称，点击后进入该触发项详情页；其余触发项按钮 data 保持数字。
  - 返回按钮 data 从 `0` 改为 `返回上一步`，点击后返回上一层。
  - 非命令字的触发项名称（全员禁言/付费开关/小说开关/群文件清理）点击后不会触发实际命令，而是进入帮助详情页。

## v1.46.5

### 调整

- 帮助菜单大类名称改名以避开触发指令字：`主动触发` 改为 `主动类`，`被动触发` 改为 `被动类`。避免大类按钮点击后发送的名称与实际命令冲突。

## v1.46.4

### 重构

- 帮助菜单恢复 4 层结构：大类 → 小类 → 触发项 → 详情。v1.46.0 扁平化为 3 层后，主动触发大类 21 个触发项超过 QQ 官方 keyboard 5 行按钮限制，导致按钮整体消失。现恢复小类层级作为可点击的中间页，每层按钮每行 2 个，所有页面按钮行数均不超过 5 行限制：
  - 大类页：主动触发/被动触发（2 个按钮 + 返回）
  - 小类页：API功能/管理功能/用户激活 等（2-3 个按钮 + 返回）
  - 触发项页：各小类下触发项（2-8 个按钮 + 返回，管理功能 8 个触发项每行 2 个 = 4 行 + 返回 = 5 行，正好不超限）
  - 详情页：触发项详情 + 快捷命令按钮（0-6 个 + 返回）
- 所有按钮统一每行 2 个。
- 保留超 5 行自动降级逻辑作为兜底。

## v1.46.3

### 修复

- 修复帮助菜单 Markdown + Keyboard 按钮整体消失的问题。QQ 官方限制单条消息 keyboard 最多 5 行按钮（每行最多 5 个），v1.46.1/v1.46.2 的按钮布局导致主动触发大类（21 个触发项）超过 5 行限制，整个 keyboard 被 QQ 官方拒绝，按钮全部消失。现改为：大类触发项列表按钮每行 4 个、去掉小类标题按钮；当生成按钮行数超过 QQ 官方 5 行限制时自动降级为只发 markdown 文本不发按钮（避免整个 keyboard 被拒绝），并记录受控日志。被动触发大类（6 个触发项）2 行可正常显示按钮。

## v1.46.2

### 调整

- 帮助菜单 Markdown + Keyboard 按钮统一每行 2 个：大类触发项列表的触发项按钮由每行 5 个改为每行 2 个，详情页快捷命令按钮由每行 4 个改为每行 2 个；大类选择页和返回按钮行保持不变。

## v1.46.1

### 修复

- 修复帮助菜单 Markdown + Keyboard 按钮版大类触发项列表未按小类分组的问题。v1.46.0 扁平化时只改了纯文本版的小类分组展示，按钮版仍是全部触发项平铺。现按钮也按小类分组，每组前显示带「【】」的小类标题按钮（点击刷新当前页），下方为该小类触发项按钮，最末为返回按钮。

## v1.46.0

### 新增

- 番茄小说 API 支持直接切换：群文件清理管理员白名单 QQ 直接发送 `oiapi`、`析api`、`崩溃api` 即可切换对应接口，无需先发送 `查看API`。原 `查看API -> 1/2/3` 流程保持兼容。

### 调整

- 帮助菜单扁平化：去掉「API功能 / 管理功能 / 用户激活」中间小类层级，进入大类后所有触发项按原小类名分组平铺展示，全局连续编号，可直接发送数字查看详情；状态机由 4 层简化为 3 层（大类→触发项→详情），`0` 返回上一层。
- 帮助菜单 Markdown + Keyboard 按钮同步扁平化：大类选择后直接展示该大类全部触发项按钮，不再有小类中间页。
- 帮助菜单详情页新增快捷命令按钮，点击即可发送对应命令（私聊自动发送，群聊填入输入框，这是 QQ 官方平台限制）：全员禁言、群文件清理、小说开关、付费开关、番茄小说API切换。

## v1.45.24

### 新增

- 帮助菜单「全员禁言」详情页新增快捷命令按钮：开启禁言、关闭禁言、开启全部禁言、关闭全部禁言，点击即可发送对应命令（私聊自动发送，群聊填入输入框）。同时精简帮助大类菜单文案。

## v1.45.23

### 调整

- 精简帮助大类菜单文案，去掉「请在 120 秒内发送数字或名称查看小类，例如 1 或 主动触发」「也可以直接发送：帮助 1」等提示行。纯文本版与 Markdown 版同步调整。

## v1.45.22

### 维护

- 移除 v1.45.21 为排查 qq_official @ 触发问题临时加入的诊断日志：`main.py` 初始化版本/源码路径日志、`处理用户激活` 解析失败时的 `用户激活入口诊断` 受控日志。qq_official 下 `@用户 激活` 等指令已确认可用。

## v1.45.21

### 修复

- 修复 QQ 官方机器人（qq_official 适配器）下 `@用户 激活` 等指令仍无反应的问题。`消息工具.获取命令文本` 是全局命令文本入口，但其 `清理命令文本` 没有清理 qq_official 适配器渲染的 `<@openid>` 格式，导致 `命令文本` 带前缀，影响 `是需激活文本命令` 等后续判断。现补 `<@!?openid>` 清理。

### 诊断

- 在 `main.py` 初始化时输出 `Plugin 馒头bot (vX.Y.Z) 已加载，源码路径=...` 日志，便于确认 AstrBot 是否加载最新版代码。
- `处理用户激活` 在 `解析激活命令` 返回 None 时新增 `用户激活入口诊断(未识别激活命令)` 受控日志，输出 `command`、`extracted`(提取激活命令文本结果)、`message_str`、`raw_message`、`segments`(消息段结构)，用于排查 qq_official 等适配器下激活命令未识别的真实原因。

## v1.45.20

### 修复

- 修复 QQ 官方机器人（qq_official 适配器）下 `@用户 激活` / `@用户 重置` / `@用户 查询` / `@用户 踢` 等指令仍失效的问题。qq_official 适配器在 `message_str`/`raw_message` 中把 At 渲染成 `<@openid>` 格式（如 `<@7C4E5E095BDF28F6BC1B64BD6818443E>`），而 `从文本提取At用户列表` 只匹配 `[At:xxx]` 和 `[CQ:at,qq=xxx]`，导致 @ 用户提取为空、命令文本清理后仍带 `<@openid>` 前缀不匹配命令规则。用户激活与群管功能两个模块的 `从文本提取At用户列表` 与命令文本清理函数现已新增 `<@!?openid>` 格式识别与清理。

## v1.45.19

### 修复

- 修复 QQ 官方机器人（qq_official 适配器）下 `@用户 踢` / `@用户 踢了` / `@用户 禁言` 等指令失效的问题。群管功能的 `规范化用户编号` 之前只接受 5-12 位纯数字 QQ 号，会拒绝 qq_official 适配器返回的字母数字 openid，导致 `提取被艾特用户QQ列表` 永远返回空列表、指令提示"请 @ 要踢出的用户"。现对齐用户激活模块的 `用户编号规则`（`^[A-Za-z0-9_-]{5,64}$`），同时接受数字 QQ 与 openid，并补 `Comp.At` 实例分支与 `qq_official` 占位符过滤。数字撤回规则、数字 ID 校验（跨群踢出/撤回踢出计数）仍保留纯数字，不受影响。

## v1.45.18

### 优化

- 继续降低正常运行流程日志为 debug：UC 网盘（上传前已删除远端同名旧文件）、群管功能（撤回检查跳过）。群管功能"撤回检查跳过"日志精简为只保留消息文本，去掉白名单/At/群名片/合并转发/闪传/数字/原始文本等字段详情；群管撤回操作的核心审计日志（撤回成功/失败/身份检查等）仍保持 info。

## v1.45.17

### 优化

- 降低正常运行流程的日志级别为 debug，避免 info 刷屏：番茄小说（开始下载/章节进度/章节完成/短链展开/目录获取/文件发送/缓存写入与删除/UC 上传成功/百度后台上传成功与跳过/API 切换）、百度网盘（完结已存在跳过/删除同名旧文件/开始上传/上传完成/后台上传成功与跳过）、用户激活（付费开关诊断/免费额度放行/获取群号诊断）。出问题时调高日志级别到 debug 即可查看；warning/error、用户激活诊断、授权链接诊断等 AGENTS.md 明确要求保留的受控日志仍保持 info 及以上。

## v1.45.16

### 修复

- 番茄小说不识别 `m.novelfm.com/s/短码` 分享链接：该短链会 302 跳到 `m.changdunovel.com/...?book_id=数字`，但短链正则只覆盖 `changdunovel.com/t/短码`。已扩展短链正则覆盖 `m.novelfm.com/s/短码`，命中后走原短链展开流程请求一次并从最终地址或页面文本提取 `book_id`；同时修复链接末尾反引号未清理导致短链末尾多一个 `` ` `` 的问题；同步更新析API 同名正则和 AGENTS.md 规则。

## v1.45.15

### 优化

- 帮助菜单支持发送文字返回上一步：在帮助流程中发送"返回上一步"/"返回"/"上一步"/"返回上一级"/"上一级"都等同于发送 0，逐层返回；纯文本版和 MD 键盘版均生效；不在帮助流程中发送这些词不触发。

## v1.45.14

### 优化

- 帮助菜单名称触发不再需要先发"帮助"：管理员随时发送帮助里列出的任意名称（大类/小类/触发项，如"主动触发"、"API功能"、"踢人"）都会直接跳转到对应层级；非管理员在无帮助状态下发名称静默忽略，不打断群聊；数字选择仍需先进入帮助流程。

## v1.45.13

### 优化

- 帮助菜单任意层级都可用名称触发：不管当前在大类、小类还是触发项层，发送任意层级里列出的名称（如"主动触发"、"API功能"、"随机英文单词"）都会直接跳转到对应层级；纯文本版和 MD 键盘版均生效；按钮点击发送名称同样适用；删除旧的 `名字转编号` 和 `匹配大类名称`，统一改用 `匹配任意层级名称` + `跳转到目标`/`跳转到目标MD`。

## v1.45.12

### 修复

- 帮助菜单大类切换失效：进入"主动触发"小类后再发送"被动触发"无反应。现在在任何层级发送大类名称都会先重置到大类层再进入对应大类，纯文本版和 MD 键盘版均生效。

## v1.45.11

### 优化

- 帮助菜单支持发送名称触发：发送"帮助"列出大类后，可直接发送名称（如"主动触发"）进入小类，不再只支持数字；纯文本版和 Markdown 键盘版均生效；提示文字同步更新说明可用名称；删除未被调用的旧版 `处理帮助指令MD`。

## v1.45.10

### 优化

- 帮助键盘按钮显示和发送内容改为名称而非数字：按钮 data 和 id 改为对应名称（如"主动触发"），点击按钮后发送名称；返回按钮 data 保持"0"；新增名字转编号函数支持名称匹配，同时保留数字匹配兼容手动输入。

## v1.45.9

### 优化

- 帮助键盘每行最多显示 2 个按钮，返回按钮单独放最后一行。

## v1.45.8

### 优化

- 帮助键盘返回按钮单独放最后一行，其他按钮在上面排列；恢复按钮文字为"返回上一步"。

## v1.45.7

### 优化

- 缩短帮助键盘返回按钮文字：`返回上一步` → `返回`，减少 QQ 客户端因按钮文字过长自动换行导致一行显示按钮过少的问题。

## v1.45.6

### 修复

- 修复 qq_official 适配器下 group_openid 获取失败：经查 AstrBot 源码确认 `group_openid` 被存入 `group_id` 字段（`get_group_id()` 同步返回），`get_group` 是基类空实现且 qq_official 未重写；用户 openid 通过 `get_sender_id()` 获取。改用正确字段路径，移除异步获取和临时诊断日志。

## v1.45.5

### 修复

- 修复 qq_official 适配器 `get_group` 返回协程对象导致 `group_openid` 为 `<coroutine object>` 的问题：`获取群号` 和 `获取用户openid` 跳过协程对象；新增 `获取群openid异步` 和 `获取用户openid异步` 正确 await 协程；移除临时诊断日志。

## v1.45.4

### 修复

- 修复帮助功能日志不可见：原使用 `logging.getLogger` 不被 AstrBot 日志系统显示，改为 `from astrbot.api import logger`，现在能看到 `[帮助MD键盘]` 发送成功/失败的真实原因。

## v1.45.3

### 修复

- 修复 `是QQ官方机器人` 判断失败导致帮助按钮不显示：`获取适配器名称` 新增读取 `platform_meta` 的 `name`/`id`/`platform`/`adapter_name`/`platform_name` 字段，兼容 qq_official 适配器；新增临时 `诊断适配器信息` 日志用于排查。

## v1.45.2

### 修复

- 修复帮助功能 Markdown + Keyboard 在 QQ 官方机器人下按钮不显示的问题：获取群号改为读取 `group_openid`，私聊新增 `获取用户openid` 读取 `user_openid`；Route 改用占位符方式；补充 `msg_seq` 参数。

## v1.45.1

### 修复

- 修复帮助功能 Markdown + Keyboard 按钮不显示的问题：keyboard 字段缺少官方文档要求的 `content` 包装层，导致按钮无法渲染。

## v1.45.0

### 新增

- 按 QQ 官方机器人开发文档重构帮助功能 Markdown + Keyboard 按钮交互：按钮 `permission.type` 改为 2（所有人可操作），补齐必填字段 `unsupport_tips`，每行最多 5 个按钮紧凑排列；私聊场景按钮设 `enter: True` 点击即自动发送，群聊场景点击后插入输入框。

## v1.44.2

### 修复

- 修复 `关闭收费` 后 `开启群聊收费` 仍被全局免费覆盖的问题：全局收费关闭时，已单独设置群聊或私聊付费开关的场景按独立开关判断，未设置才免费；其他群聊不受影响。

## v1.44.1

### 修复

- 修正 `开启收费` / `开启付费` 语义：开启后强制群聊和私聊全部进入收费判断，不再受此前单群或私聊关闭收费状态影响。
- 保留 `关闭私聊收费` 的独立免费逻辑：未设置全局强制收费时，私聊关闭收费后不消耗每日免费额度，私聊用户可无限制使用。
- 状态命令细化全局收费显示，区分强制全部收费、全部免费和按群聊/私聊独立开关判断。

## v1.44.0

### 新增

- 调整收费总开关：`关闭收费` / `关闭付费` 改为全局关闭群聊和私聊收费，`开启收费` / `开启付费` 仅恢复全局总开关，并继续沿用此前保存的群聊和私聊独立开关状态。
- 新增 `开启群聊收费` / `关闭群聊收费` 与 `开启群聊付费` / `关闭群聊付费`，用于单独控制当前群收费模式。
- 状态命令新增全局收费状态展示，便于区分全局总开关、当前群收费和私聊收费。

## v1.43.0

### 新增

- 帮助功能适配 QQ 官方机器人 Markdown + Keyboard 按钮消息：官方机器人发送帮助时自动使用 Markdown 格式渲染，并附带可点击的按钮（大类选择、小类选择、触发项选择、返回上一步）；普通机器人保持原有纯文本格式不变。发送失败时自动降级为纯文本。

## v1.42.5

### 改进

- 添加群管理身份检查详细诊断日志，定位非管理员群撤回不触发的原因。
- 添加撤回尝试详细诊断日志，记录 message_id、group_id、bot类型等关键信息。

## v1.42.4

### 修复

- 官方机器人在非管理员群撤回失败时，发送提示消息告知群成员检测到广告但无法撤回。
- 广告消息优先撤回处理，避免URL编码内容被卡密提取器误匹配导致撤回流程被跳过。

## v1.42.3

### 修复

- 闪传/群名片/合并转发等广告消息优先撤回处理，避免URL编码内容被卡密提取器误匹配导致撤回流程被跳过。

## v1.42.2

### 修复

- 闪传消息规则添加 `\[闪传\]` 匹配，适配QQ官方机器人AstrBot解析格式。
- 添加撤回检查诊断日志，方便定位消息未被撤回的原因。

## v1.42.1

### 修复

- QQ官方机器人广告撤回：添加 `[卡片消息]`、`[合并转发消息]`、`[闪传消息]` 正则匹配规则，适配AstrBot解析后的消息格式。
- QQ官方机器人撤回跳过问题：`是否At消息` 检查时去除 `[At:qq_official]`，避免用户@机器人触发的消息被误判为@消息而跳过撤回。

## v1.42.0

### 修复

- QQ官方机器人撤回消息：使用 `botpy.http.Route("DELETE", path)` 构造正确的 Route 对象调用 `_http.request`，完成群消息撤回。

## v1.41.9

### 修复

- QQ官方机器人撤回消息：添加 `_http.request` 方法签名诊断，定位参数类型错误。

## v1.41.8

### 修复

- QQ官方机器人撤回消息：修复 `api.recall_message` 失败后直接 raise 导致 `_http` fallback 未执行的问题，现在会正确 fallback 到底层 HTTP 客户端调用群消息撤回接口。

## v1.41.7

### 修复

- QQ官方机器人撤回消息：通过 `api._http` 底层 HTTP 客户端直接调用 `/v2/groups/{group_openid}/messages/{message_id}` 接口完成群消息撤回。

## v1.41.6

### 修复

- QQ官方机器人撤回消息：新增 api 和 bot 私有方法诊断日志，查找底层 HTTP 请求方法。

## v1.41.5

### 修复

- QQ官方机器人撤回消息：定位到 `api.recall_message` 是频道接口（`/channels/...`），群消息撤回需用 `/v2/groups/...` 接口。新增 api 全部方法诊断日志。

## v1.41.4

### 修复

- QQ官方机器人撤回消息：增加详细异常日志，定位 `api.recall_message` 失败的具体原因。

## v1.41.3

### 修复

- QQ官方机器人撤回消息：通过诊断日志定位到 `api.recall_message` 方法，修复参数顺序为 `channel_id` + `message_id`。

## v1.41.2

### 修复

- QQ官方机器人撤回消息增加诊断日志，输出 bot/api 关键方法列表，用于定位正确的撤回接口。

## v1.41.1

### 修复

- 修复 `获取群号` 函数对 QQ 官方机器人未读取 `group_openid` 导致付费开关 key 不一致的问题。
- 撤回消息（delete_msg）兼容 QQ 官方机器人 API，自动尝试 `channel_id`/`group_openid`/`openid` 参数，并保留 OneBot 原有 `message_id` 撤回接口。
- `get_group_msg_history` 和 `get_group_member_info` 自动适配 QQ 官方机器人的 `group_openid`/`user_openid` 参数。
- 付费开关增加诊断日志，方便排查问题。

## v1.41.0

### 修复

- 私聊收费/付费开关关闭后未激活用户仍被拦截的问题，现在付费开关指令独立处理，不受用户激活流程影响。

### 新增

- 插件全部功能适配 QQ 官方机器人（qq_official），包括群管功能（踢人、禁言、撤回）、群文件清理、付费开关、用户激活、小说下载等所有功能。
- 新增适配器检测工具函数 `获取适配器名称`、`是QQ官方机器人`、`是OneBot适配器`，自动识别运行环境。
- 群管功能踢人和禁言接口自动适配 `group_openid`/`user_openid` 参数，兼容 QQ 官方机器人环境。

## v1.40.4

### 修复

- 崩溃API 下载失败时对用户统一提示 `番茄小说下载失败请重新发送链接或者换一本书`，具体失败原因只写入日志，避免暴露接口内部错误。

### 调整

- 群文件清理删除阶段每轮并发数从 20 调整为 200，并同步 README 和 AGENTS 说明。

## v1.40.3

### 调整

- 群文件清理删除阶段每轮并发数从 2 调整为 20。

## v1.40.2

### 修复

- 修复 aiocqhttp 场景下 `bot.self_id` 为空导致群文件清理无法确认机器人 QQ 的问题；现在会从事件、消息对象、bot 对象和 `get_login_info` 提取机器人 QQ 后再判断 QQ 群管理身份。

## v1.40.1

### 修复

- 修复机器人 QQ 或群成员身份查询失败时仍会放行群文件清理的问题；现在只有明确查到机器人是 QQ 群主或管理员才会清理，否则静默跳过。

## v1.40.0

### 新增

- 群文件清理前会检查机器人在目标群是否为 QQ 群主或管理员；非管理群静默跳过，不清理也不提示。
- `清理群文件` / `群文件清理` 开始清理前会提示 `正在清理群文件`，`清理全部群文件` 会提示 `正在清理群文件（全部）`。

## v1.39.1

### 调整

- 群文件清理删除阶段改为每轮最多 2 个文件并发删除，当前批次结束后重新扫描继续清理。
- 群文件清理遇到 `Invalid file_id` 时保留不带 `busid` 删除尝试，重新扫描同名文件后的重试次数改为 1 次，并扩大诊断日志截断长度。

## v1.39.0

### 新增

- `状态` 指令新增运行内存、运行 CPU、系统内存、系统 CPU、系统运行时间和插件运行时间展示。
- 运行资源信息使用标准库按平台读取，仍只允许群文件清理管理员白名单 QQ 查看。

## v1.38.0

### 新增

- 新增管理员指令 `状态`，可查看系统信息、插件版本、当前番茄 API、番茄/七猫开关、群聊/私聊收费开关、每日免费额度、数据库和 UC/百度网盘启用状态。
- 状态命令只读取现有配置和 MySQL 运行状态，不展示 OIAPI key、网盘 Cookie 等敏感内容。

## v1.37.0

### 新增

- 新增管理员指令 `开启私聊收费` / `关闭私聊收费`，并支持 `开启私聊付费` / `关闭私聊付费` 别名。
- 私聊收费状态默认开启，写入 MySQL `mantou_runtime_state`；群聊仍按当前群独立保存，私聊和群聊互不影响。
- 番茄、七猫和 OIAPI 文本接口共用同一套激活拦截判断，私聊下载会同步读取私聊收费状态。

## v1.36.4

### 调整

- 群文件清理删除阶段改为每轮最多 8 个文件并发删除，当前批次结束后重新扫描继续清理。

## v1.36.3

### 修复

- 重做群文件清理删除流程：删除阶段改为每次只处理 1 个文件，删除后重新扫描，避免适配器临时文件 ID 在并发删除时持续失效。
- `delete_group_file` 携带 `busid` 返回 `Invalid file_id` 时，会额外尝试不带 `busid` 删除；失败去重改用文件名、大小、上传者和 `busid`，避免同一文件因 `file_id` 每轮变化而重复失败。

## v1.36.2

### 修复

- 群文件清理遇到 `Invalid file_id` 时改为分轮重新扫描群文件并最多重试 3 次，避免适配器首次重扫仍返回缓存文件 ID 时继续失败。

## v1.36.1

### 修复

- 群文件清理遇到 `Invalid file_id` 时会重新扫描当前群文件，找到同名文件后用新的 `file_id` 重试一次；重新扫描后文件不存在则按失效记录跳过，避免适配器缓存旧文件 ID 导致清理失败。
- 同步当时的群文件清理说明，删除阶段使用 20 并发。

## v1.36.0

### 新增

- 新增 `功能文件/API功能/崩溃API/番茄小说.py`，接入 `http://111.170.14.45:2000` 的下载任务接口。
- `查看API` 新增 `3. 崩溃API`，该接口会创建下载任务、轮询 `/download/progress` 获取进度，再下载 `/download/file` 的完整 TXT。
- 崩溃API 下载完成后会重新整理为现有番茄 TXT 格式，保留当前的书名、作者、简介、免责声明、文件名、UC/百度网盘和 QQ 文件发送链路。

## v1.35.3

### 修复

- 番茄 OIAPI 返回 `Key注册失败` 时，保留接口原始提示并在下一行追加 `请等待10分钟再下载`。

## v1.35.2

### 调整

- 分类整理 `功能文件/管理功能/` 下的 Python 文件，新增 `基础功能`、`小说功能`、`网盘功能` 和 `群聊功能` 子目录，并同步更新内部导入路径。

## v1.35.1

### 修复

- 修复 AstrBot 事件角色显示为 `member` 时没有继续查询真实 QQ 群身份的问题；现在会通过 OneBot `get_group_member_info` 识别 QQ 群主和管理员，避免误撤回。

## v1.35.0

### 新增

- 群管自动撤回新增 QQ 群身份保护：QQ群主和管理员发送数字、群名片/JSON 卡片、合并转发或 QQ 闪传时不会被自动撤回，也不会累计三次踢出计数。

## v1.34.2

### 修复

- 百度网盘后台上传完结小说时，如果目标目录已有同名普通文件，会跳过重复上传；连载小说仍按原逻辑删除旧同名文件后重传。

## v1.34.1

### 调整

- 百度网盘上传书籍状态配置改为可选择项，可直接选择 `完结`、`连载` 或 `全部`。

## v1.34.0

### 新增

- 新增百度网盘后台上传，插件配置可填写 `baidu_pan_cookie`、`baidu_pan_upload_dir` 和 `baidu_pan_upload_status`。
- 番茄和七猫小说生成完整原小说 txt 后可后台上传到百度网盘；上传前删除目标目录下同名普通文件，不删除同名文件夹。
- 百度网盘上传状态可选 `完结`、`连载` 或 `全部`，默认只上传完结；百度只做后台备份，不创建分享链接，也不影响 QQ 原有发送逻辑。
- 付费开关新增 `开启收费` / `关闭收费` 别名，和 `开启付费` / `关闭付费` 效果一致。

## v1.33.8

### 调整

- UC 网盘上传前恢复删除目标目录下远端同名普通文件；只删除同名文件，不删除同名文件夹，避免重复生成旧小说文件。

## v1.33.7

### 修复

- 修复 UC 网盘创建上传目录 `/file` 返回 `file is doloading[同名冲突]` 时直接回退发送源文件的问题；现在会兼容布尔目录标记并重新查找已有目录继续上传。

## v1.33.6

### 修复

- UC 网盘上传完成接口返回 `23008` / `file is doloading[同名冲突]` 时，改为复用预上传阶段返回的文件 ID 继续创建分享，对齐旧项目 UC 客户端的容错方式。
- UC 网盘 HTTP 错误日志现在会带接口路径，后续能直接看出失败发生在预上传、完成上传、创建分享还是获取分享链接。

## v1.33.5

### 修复

- 修复 UC 网盘上传完成接口长时间返回 `23008` / `file is doloading[同名冲突]` 时仍回退发送源文件的问题；现在会在重试后查询目录，必要时使用当前预上传文件 ID 继续创建分享，并在分享创建阶段继续等待处理中状态。

## v1.33.4

### 修复

- 修复 UC 网盘上传完成接口返回 `23008` / `file is doloading[同名冲突]` 时仍会回退发送源文件的问题；现在会等待并重试同一个上传任务。

## v1.33.3

### 修复

- UC 网盘上传前不再删除远端同名旧文件；如果 UC 返回同名冲突，会改用备用远端文件名上传，QQ 发送的链接 txt 仍保持原小说文件名。
- OIAPI 返回业务失败时，用户回复只显示接口 `message` 内容，例如 `付费内容不解析`，不再显示 `code` 和 `message=` 前缀。

## v1.33.2

### 修复

- 修复 UC 网盘预上传返回 `23008` / `file is doloading[同名冲突]` 时直接回退发送源文件的问题；现在会重新删除远端同名文件并有限重试。

## v1.33.1

### 修复

- 修复部分 UC 网盘 OSS 上传响应没有返回 `ETag` 时会误判上传失败的问题；现在会使用本地文件 MD5 作为单分片 ETag 继续完成上传。

## v1.33.0

### 新增

- 新增 UC 网盘自动上传分享，插件配置可填写 `uc_pan_cookie` 和 `uc_pan_upload_dir`。
- 番茄和七猫小说下载完成后会先上传完整原小说 txt 到 UC 网盘；上传成功时 QQ 发送同名链接 txt，内容为 `请复制链接打开UC网盘` 和 `链接：xxx`。
- UC 网盘上传失败或未配置 Cookie 时继续发送原小说 txt，远端上传前会删除同名旧文件。

## v1.32.1

### 调整

- `开启付费` / `关闭付费` 改为只影响当前群，不再使用全局付费状态。
- 付费开关状态按群号写入 MySQL 运行状态表，其它群默认仍保持开启付费模式。

## v1.32.0

### 新增

- 新增管理员指令 `开启付费` / `关闭付费`，状态写入 MySQL 运行状态表。
- `关闭付费` 后未激活用户可直接使用受激活门禁保护的功能，小说下载不消耗每日免费额度，也不显示剩余免费本数。

## v1.31.0

### 新增

- `查询卡密` 分页结果中新增 `复制` 命令，可按当前查询条件输出全部卡密，格式和 `生成卡密` 一样一行一个。
- `复制 数字`、`复制数字` 和 `复制 数字天` 会在当前卡密查询结果中只输出对应天数的卡密。

## v1.30.1

### 调整

- `@用户 踢` 和 `@用户 踢了` 都会在踢出后撤回目标用户最近消息。
- 踢人撤回数量改为每个目标用户最多 50 条，多个 @ 用户逐个处理，不共享同一个 50 条上限。

## v1.30.0

### 新增

- 群管广告自动撤回成功后，改为拉取最近 100 条群历史，筛选触发用户消息并最多撤回 8 条。
- 管理员发送 `@用户 踢了` 时，当前群踢出并跨群检查后，会自动撤回目标用户最近最多 200 条消息，支持一次 @ 多个用户逐个处理。
- 群管撤回逻辑复用 `get_group_msg_history` 拉历史、按用户筛选、按时间倒序、循环 `delete_msg` 的标准做法，不引入非标准批量撤回接口。

## v1.29.0

### 新增

- 卡密同步列表改为和 MySQL 双向同步：配置列表删除卡密会删除数据库记录，数据库删除卡密会刷新删除配置列表，数据库新增卡密会刷新新增到配置列表，配置列表按格式新增卡密会写入数据库。
- 卡密同步使用 MySQL `mantou_runtime_state` 保存上次同步快照，用于区分配置变更和数据库变更。

## v1.28.2

### 修复

- 卡密同步查看改为只从 MySQL 单向同步到插件配置显示；数据库变化后会在插件同步配置时刷新显示，删除配置列表内容不会删除数据库卡密记录。
- `_conf_schema.json` 移除卡密同步初始化状态开关，避免误以为配置列表可以控制数据库删除。

## v1.28.1

### 修复

- 修复普通文本里出现类似卡密的 12 位片段时会误触发卡密回复的问题；现在只有 MySQL 中真实存在的卡密才会进入卡密激活流程，不存在的卡密候选会被忽略。

## v1.28.0

### 新增

- 番茄小说 API 当前选择改为写入 MySQL 内置表 `mantou_runtime_state`，不再生成 `功能文件/下载缓存/番茄小说API.json`。
- 番茄/七猫小说功能开关状态改为写入 MySQL 内置表 `mantou_runtime_state`，不再生成 `功能文件/下载缓存/小说功能开关.json`。

## v1.27.0

### 新增

- 小说下载开始提示新增免费额度剩余本数；当天最后一本会提示可购买 VIP 无限下载，免费次数用完后回复新的购买 VIP / 次日免费下载提示。
- 未激活用户每日免费额度改为按用户 QQ 全局计数，不再按群区分，避免同一用户换群重复获得免费次数。
- `_conf_schema.json` 新增基础配置、卡密同步查看和数据库配置分类；数据库配置归入 `database_settings`。
- 插件配置新增已使用/未使用卡密同步查看列表；同步初始化后，从配置列表移除卡密并重载插件会同步删除数据库记录。
- 每日免费额度使用次数改为写入 MySQL 内置表 `mantou_user_free_quota`，不再写入本地 JSON，避免插件重载或同步代码后恢复免费次数。

## v1.26.1

### 调整

- 调整用户激活 MySQL 配置：插件配置不再单独显示数据库名，默认使用 `user_activation_database_user` 作为数据库名；数据库相关配置移动到配置列表底部。

## v1.26.0

### 新增

- 新增 `user_activation_daily_free_quota` 插件配置，可设置未激活用户每天免费使用受激活门禁保护功能的次数；次数按群和用户分别计数，写入 `功能文件/下载缓存/用户免费额度.json`，跨天自动重置。

## v1.25.0

### 新增

- 管理员使用 `@用户 踢` / `@用户 踢了` 手动踢出成员时，当前群踢出成功后会通过 `get_group_list` 获取机器人所在群，并用 `get_group_member_info` 检查该用户是否在其它群，存在则一并踢出。

## v1.24.1

### 修复

- 修复群名片、卡片消息中的隐藏字段或标题被用户激活模块误识别为卡密，导致回复“卡密无效或已使用”的问题；卡密激活现在只从可见文本或 text/plain 消息段识别连续 12 位卡密。

## v1.24.0

### 新增

- 群管撤回成功后，会通过 `get_group_msg_history` 获取群历史，筛选触发用户最近消息并逐条调用 `delete_msg` 撤回，默认最多撤回 5 条。
- 数字撤回模块累计 3 次自动踢出当前群成员后，会通过 `get_group_list` 获取机器人所在群，并用 `get_group_member_info` 检查该用户是否在其它群，存在则一并踢出。

## v1.23.0

### 新增

- 群文件清理管理员白名单 QQ 可发送 `开启番茄` / `关闭番茄` 开关番茄小说下载功能。
- 群文件清理管理员白名单 QQ 可发送 `开启七猫` / `关闭七猫` 开关七猫小说下载功能。
- 小说功能开关状态写入 `功能文件/下载缓存/小说功能开关.json`，关闭后对应小说链接会直接回复功能已关闭，不进入下载流程。
- 帮助菜单新增小说开关触发项。

## v1.22.0

### 新增

- 群文件清理管理员白名单 QQ 可发送 `关闭禁言`，调用 OneBot `set_group_whole_ban enable=False` 关闭当前群全员禁言。
- 群文件清理管理员白名单 QQ 可发送 `关闭全部禁言`，通过 `get_group_list` 获取机器人所在群后逐群关闭全员禁言。
- 帮助菜单将全员禁言触发项同步为 `开启禁言` / `关闭禁言` / `开启全部禁言` / `关闭全部禁言`。

## v1.21.0

### 新增

- 群文件清理管理员白名单 QQ 可发送 `开启禁言`，调用 OneBot `set_group_whole_ban` 开启当前群全员禁言。
- 群文件清理管理员白名单 QQ 可发送 `开启全部禁言`，通过 `get_group_list` 获取机器人所在群后逐群开启全员禁言。
- 群文件清理管理员白名单 QQ 可发送 `清理全部群文件`，通过 `get_group_list` 获取机器人所在群后逐群执行群文件清理。
- 帮助菜单新增 `开启禁言`、`开启全部禁言` 和 `清理全部群文件` 触发项。

## v1.20.0

### 新增

- `查询卡密` 新增二级选择菜单，管理员可发送 `1` 查看已使用卡密、`2` 查看未使用卡密、`0` 返回上一步。

### 调整

- 保留 `查询卡密 使用`、`查询卡密 没使用`、按 QQ/@用户/卡密筛选和分页翻页等直接查询方式。

## v1.19.0

### 新增

- `查询用户` 和 `查询卡密` 翻页新增 `上`、`下`、`上一页`、`下一页` 指令。
- 卡密激活支持从前后带普通文字的消息中识别连续 12 位卡密，中间断开或插入字符的内容不会拼接识别。

### 调整

- `查询卡密` 列表将卡密天数移动到使用状态同一行显示。
- `查询用户` 列表改为按剩余时间从少到多排序，并显示 QQ 号、卡密号和剩余时间。
- 调整用户激活 `查询` 回复字段顺序，将激活时间和结束时间移动到剩余时间下方。

## v1.18.0

### 新增

- 用户激活查询新增 `查询` 指令，替代 `查询时间`，可显示 QQ 号、卡密号、激活时间、结束时间、激活天数和剩余时间。
- `查询卡密` 列表新增卡密天数显示，格式为 `----xx天`。

### 调整

- `查询卡密` 默认只查看未使用卡密，发送 `查询卡密 使用` 查看已使用，发送 `查询卡密 没使用` 查看未使用。
- `查询用户` 列表改为上下排列，隐藏具体时分秒，并使用长分隔线分隔用户。

## v1.17.0

### 新增

- 新增管理员帮助功能，群文件清理管理员白名单 QQ 可发送 `帮助` 查看 `主动触发` / `被动触发` 大类。
- 帮助菜单支持继续按数字查看小类、触发指令和详情，发送 `0` 返回上一层；也支持直接发送 `帮助 数字` 查看指定大类。

## v1.16.0

### 新增

- 用户激活支持直接填写 QQ 号进行激活、重置和查询时间，多个 QQ 号可批量处理。
- 新增 `查询用户` / `下一页`，管理员可分页查看当前群已激活用户，每页 10 条。
- 新增 `增加` / `减少` 激活时间，可在原有到期时间基础上增减天数，例如 `@用户 增加 5`、`减少 用户QQ 3`。
- 新增 `生成卡密`，管理员可生成 12 位一次性卡密，用户发送卡密后默认激活 30 天。
- 新增 `查询卡密`，管理员可按已使用、未使用、QQ/@用户或卡密查询，每页 5 条并支持 `下一页`。

### 调整

- 已激活用户再次普通激活时不重复写入，直接提示该用户已激活。
- 用户激活和卡密存储改为只使用 MySQL，插件内置创建 `mantou_user_activation` 和 `mantou_user_activation_cards` 两张表。

## v1.15.0

### 新增

- 群管踢人支持一次 @ 多个用户，发送 `@用户1 @用户2 踢` 或 `@用户1 @用户2 踢了` 会逐个踢出并返回批量结果。
- 用户激活、用户重置和查询时间支持一次 @ 多个用户，管理员可批量激活、重置或查询多个成员。

## v1.14.0

### 新增

- `数字撤回.py` 改名为 `群管功能.py`，原数字撤回、卡片撤回和三次自动踢出逻辑保留。
- 群文件清理管理员白名单 QQ 可发送 `@用户 踢` 或 `@用户 踢了` 直接踢出群成员。

## v1.13.0

### 新增

- `查询时间` 回复新增 `激活时间` 字段，会从本地 JSON 或 MySQL 激活记录的 `updated_at` 读取。

## v1.12.0

### 新增

- 新增 `查询时间` 指令，普通用户可查询自己的激活状态、到期时间和剩余时间。
- 群文件清理管理员白名单 QQ 可通过 `@用户 查询时间`、`查询时间 @用户` 或 `@机器人 查询时间 用户QQ` 查询其他用户激活时间。

## v1.11.0

### 新增

- 用户激活新增重置命令，群文件清理管理员白名单 QQ 可发送 `@用户 重置` 或 `重置 @用户` 取消该用户激活。
- 支持 `@机器人 重置 用户QQ` 手写目标；重置命令会同时支持本地 JSON 和 MySQL 激活记录删除。

## v1.10.4

### 修复

- 修复 `@用户 激活` 在唤醒事件中被误判为激活发送者本人的问题，现在优先激活消息里被 @ 的用户。
- 用户激活继续只允许群文件清理管理员白名单 QQ 使用；需要唤醒机器人并手写目标时可使用 `@机器人 激活 用户QQ`。

## v1.10.3

### 修复

- 用户激活命令解析会从事件消息段中重新提取非 @ 文本，并清理可见 `@昵称` 前缀，兼容 `@馒头 激活` 这类适配器展示文本。

## v1.10.2

### 修复

- 用户激活 @ 解析对齐旧项目实现，兼容 `message`、`components`、`content` 组件字段和 `message_obj.raw_message` 中的 `[CQ:at]` / `[At:QQ]`。
- `@机器人 激活` 兼容 `is_atbot`、`is_wake`、`is_at_or_wake_command`、`context.robot_id` 和 `[At:机器人]` 标记，按发送者本人激活。
- 激活命令触发时输出受控 `用户激活诊断` 日志，便于继续按真实消息段补规则。

## v1.10.1

### 修复

- 修复 aiocqhttp 事件只在 `message_str` / `raw_message` 中提供 `[At:QQ]` 时，用户激活命令取不到被 @ 用户的问题。
- `@机器人 激活` 作为唤醒命令时改为激活发送者本人，避免把机器人自己的 At 当成目标用户。

## v1.10.0

### 新增

- 新增用户激活功能，群文件清理管理员可发送 `@用户 激活`、`@用户 激活 天数` 或 `激活 @用户` 激活成员，未填写天数时默认 30 天。
- 随机英文单词、随机一言、疯狂星期四、古诗词名句、七猫小说下载和番茄小说下载增加激活门禁，未激活用户使用时回复 `请查看群公告查看激活方法`。
- 新增用户激活存储配置，默认写入本地 JSON；配置启用 MySQL 后可连接数据库并自动创建激活记录表。

## v1.9.2

### 修复

- 调整消息处理顺序，群名片、合并转发和 QQ 闪传等撤回判断先于七猫/番茄小说下载识别，避免转发消息被误识别成小说链接。

## v1.9.1

### 修复

- 数字撤回三次踢出计数改为统计数字撤回模块内所有成功撤回类型，包括数字、群名片/JSON 卡片、合并转发和 QQ 闪传。

## v1.9.0

### 新增

- 数字撤回新增同群同成员触发计数，成功触发数字撤回 3 次后调用 `set_group_kick` 自动踢出该成员。
- 踢出计数用于数字撤回模块处理的撤回场景。

## v1.8.3

### 修复

- 选择析API下载时，书籍详情改为优先使用番茄官方落地页和官网页面接口，避免析API详情返回错误作者或书籍信息。

## v1.8.2

### 修复

- `main.py` 显式重载析API番茄小说子模块，避免 AstrBot 重载插件后仍使用旧的析API正文清理逻辑。

## v1.8.1

### 修复

- 限制 `查看API`、`1` / `2` API 切换和 `授权` 指令仅群文件清理管理员白名单 QQ 可用。
- 修复析API正文提取时提前删除 HTML 段落标签导致 txt 内容挤在一起的问题。
- 番茄小说 txt 输出统一转换为 CRLF 换行，提升 Windows 文本查看兼容性。
- `_conf_schema.json` 明确 `group_file_cleanup_admin_qq` 同时用于群文件清理、授权链接和番茄小说API切换。

## v1.8.0

### 新增

- 将原 `功能文件/oiapi/` 调整为 `功能文件/API功能/OIAPI/`，并新增 `功能文件/API功能/析API/` 用于析API接口。
- 番茄小说新增 `查看API` 指令，列出 API 站点后可发送 `1` 切换 OIAPI、发送 `2` 切换析API，当前选择会写入运行状态文件。
- 番茄小说新增析API下载实现，使用 `https://biek.top//api/fq.php` 的 `search`、`detail`、`catalog`、`content` 流程下载；OIAPI 和析API不会自动互相切换。
- 群文件清理删除阶段默认并发数调整为 200。

## v1.7.0

### 新增

- 群文件清理删除阶段改为有限并发执行，默认同时调用 20 个 `delete_group_file`，提升大量群文件清理速度。
- 群文件清理仍保留递归扫描、去重和删除后重新扫描机制，避免适配器单次只返回部分文件时漏删。

## v1.6.12

### 修复

- 移除 v1.6.11 中误加的 NapCat OneBot HTTP 和 NapCat WebUI 实时调试连接配置，群文件清理不再连接 NapCat。
- 群文件清理在当前适配器没有 `api.call_action` 或返回非数字群号时输出 `群文件清理事件诊断`，用于查看当前事件真实可用参数。

## v1.6.11

### 修复

- 群文件清理在当前事件没有 `api.call_action` 时支持配置 NapCat OneBot HTTP 或 NapCat WebUI 实时调试后备调用，复用 `get_group_root_files`、`get_group_files_by_folder` 和 `delete_group_file` 清理群文件。
- `qq_official` 只返回 `group_openid` 时不再把它当作数字 QQ 群号，可通过 `清理群文件 数字群号` 或 `group_file_cleanup_group_map` 指定真实群号。

## v1.6.10

### 修复

- 恢复 QQ 官方机器人群聊下七猫和番茄的 AstrBot `File` 组件发送路径，不再提前拦截为发送失败。

## v1.6.9

### 修复

- 番茄小说优先提取真实书名字段，避免文件名出现“完整版在线免费阅读”等网页标题文案。
- 番茄小说外部下载提示继续不显示简介，txt 文件头部重新保留简介。

## v1.6.8

### 修复

- 七猫和番茄在 QQ 官方机器人群聊中不再尝试发送普通 txt 文件，避免官方富媒体上传返回 `call inner proxy error` 后反复重试。

## v1.6.7

### 修复

- 番茄小说支持识别 `changdunovel.com/t/短码` 分享短链，会展开短链后从最终地址或页面文本提取 `book_id`。

## v1.6.6

### 修复

- 授权链接命令支持 `授权 数字群号 机器人QQ`，当 qq_official 事件和 AstrBot 上下文都取不到数字 `botUin` 时可手动指定。

## v1.6.5

### 修复

- 授权链接获取机器人 QQ 号时优先读取 AstrBot `context.robot_id`，兼容 qq_official 事件和 `get_login_info` 不返回数字 `botUin` 的情况。

## v1.6.4

### 修复

- 授权链接命令支持 `授权 数字群号`，当 qq_official 等适配器事件中没有数字群号时可手动指定 `groupCode`。

## v1.6.3

### 修复

- 授权链接 `botUid` 提取兼容纯数字 UID、列表参数和 QQ 到 UID 映射响应，避免 `getUidFromUin` 返回了 UID 但被误判为不可用。
- `getUidFromUin` 等接口有返回但无法识别时输出 `授权链接UID转换响应未识别`，便于继续按真实响应补规则。

## v1.6.2

### 修复

- 授权链接生成支持从事件 JSON、消息段 JSON 和 URL 编码 JSON 中递归提取 `groupCode`、`botUin`、`botUid` 等参数。
- 授权命令触发时新增受控诊断日志 `授权链接事件诊断`，用于查看真实消息 JSON 并继续补充适配器字段规则。

## v1.6.1

### 调整

- 番茄小说下载前提示和 txt 文件头部不再输出简介，只保留书名、作者、状态、章节、字数和下载提示。

## v1.6.0

### 调整

- 将新增授权链接功能对应版本调整为 `v1.6.0`，后续新增功能使用中版本递增，修复和小调整使用小版本递增。
- `功能文件/oiapi/番茄小说.py` 改为中文函数和变量命名，保留 OIAPI、OneBot、AstrBot 等外部协议字段不变。
- `功能文件/管理功能/授权链接.py` 改为中文函数和变量命名，不再通过英文函数挂中文别名。

## v1.5.52

### 新增

- 新增 `授权` 指令，自动生成 QQ 群服务授权链接 `https://club.vip.qq.com/transfer?open_kuikly_info=...`。
- 授权链接会动态读取当前群号和机器人 QQ 号，并尝试调用 `getUidFromUin` 等适配器扩展接口获取机器人 UID。

### 说明

- 如果当前适配器无法把机器人 QQ 转换为 UID，插件会直接提示缺少机器人 UID，不会生成参数不完整的授权链接。

## v1.5.51

### 修复

- 七猫小说文件发送增加 AstrBot `File` 组件路径，兼容 QQ 官方机器人 `qq_official` 富媒体发送。
- 七猫和番茄通过 `File` 组件发送时改为延迟清理下载缓存，避免适配器读取本地文件前缓存被删除。

### 说明

- QQ 官方机器人发送文件应走官方富媒体接口 `/v2/users/{openid}/files` 或 `/v2/groups/{group_openid}/files`，AstrBot `File` 组件会封装为 `msg_type=7` 的 `media` 消息；OneBot 的 `upload_group_file`/`upload_private_file` 只作为 OneBot 适配器回退。
- 官方文档标注群聊 `file_type=4 文件` 为“群场景暂不开放”，如果 QQ 官方群聊仍拒绝普通 txt 文件，这是平台能力限制，不是 OneBot 接口能修复的问题。

## v1.5.50

### 修复

- 番茄小说发送文件时优先使用 AstrBot `File` 组件，修复部分事件中 bot 没有 `api.call_action` 接口导致下载成功但文件发送失败的问题。

## v1.5.49

### 新增

- 新增 `功能文件/oiapi/番茄小说.py`，接入 OIAPI `FqRead` 下载番茄小说 txt，支持番茄明文链接和 JSON 分享卡片触发。
- 新增插件配置 `番茄小说key`，用于调用 `https://oiapi.net/api/FqRead`。

### 调整

- 番茄小说文件发送与七猫一致：写入 `功能文件/下载缓存/`，裸本地路径优先，`file://` URI 回退，上传尝试结束后删除 txt，成功时不额外发送完成提示。
- 番茄正文请求按约 500 万字分段，范围请求失败时逐步拆分到小范围或单章。
- 数字/卡片撤回白名单新增番茄相关域名，避免小说分享卡片被撤回。

## v1.5.48

### 修复

- 去掉群文件清理权限、群聊和 bot 缺失提示末尾的句号。

## v1.5.47

### 新增

- 七猫小说生成的 txt 文件顶部新增免责声明，放在书名信息前。

## v1.5.46

### 调整

- 七猫小说文件发送成功后不再额外发送下载完成/文件已发送提示，只在发送失败时回复错误原因。

## v1.5.45

### 调整

- 七猫小说发送文件名改为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`。
- 七猫小说下载并发数调整为 20，并明确下载流程不发送封面图片。

## v1.5.44

### 调整

- 七猫小说文件发送改为全部走本地缓存文件上传，不再使用 `base64://` 或 `dataurl`。
- 新增 `功能文件/下载缓存/` 目录，运行时自动创建并写入待上传 txt，上传尝试结束后立即删除缓存文件。

## v1.5.43

### 修复

- 七猫小说大文件发送直接跳过 `base64://` 和 `dataurl`，避免 30MB+ 小说通过 WebSocket 传参导致 API 超时。
- 大文件发送改为直接使用临时文件 `file://` URI 回退流程。

## v1.5.42

### 修复

- 七猫小说文件发送增加多格式重试，兼容适配器不识别裸 `/tmp/...` 路径的问题。
- 发送顺序调整为 `base64://`、`data:text/plain;base64,`、临时文件 `file://` URI、裸路径，并记录每个失败候选。

## v1.5.41

### 新增

- 七猫小说下载新增受控日志：开始下载、章节进度、完成汇总、文件发送方式和临时文件删除结果。
- 章节进度日志按约 10% 分段输出，避免逐章刷屏。

## v1.5.40

### 修复

- 七猫小说新增识别 `app-share.wtzw.com/.../short-story-detail/数字` 短篇分享链接。

## v1.5.39

### 调整

- 移除七猫文本命令集合，只保留直接识别七猫链接和七猫分享卡片。

## v1.5.38

### 修复

- 七猫小说识别 `app-share.wtzw.com/.../article-detail/数字` 分享链接和 JSON 分享卡片中的链接。
- 数字/卡片撤回白名单新增 `qimao.com` 和 `app-share.wtzw.com`，避免七猫分享卡片被撤回。

## v1.5.37

### 新增

- 七猫小说支持直接识别 `https://www.qimao.com/shuku/数字/` 链接触发下载。
- 七猫下载前先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，再继续下载并发送文件。

## v1.5.36

### 调整

- 七猫小说下载完成后直接发送到 QQ，不再保存到仓库 `downloads` 目录。
- 文件发送优先尝试 base64 直传；适配器不支持时使用临时 txt 上传，上传结束后立即删除临时文件。

## v1.5.35

### 新增

- 新增 `功能文件/管理功能/七猫小说.py`，支持 `七猫搜索 关键词` 和 `七猫小说 书名/ID/链接`。
- 七猫小说下载会生成 txt 文件，并优先通过 `upload_group_file` 或 `upload_private_file` 发送。
- 新增 `pycryptodome` 依赖用于解密七猫章节正文。

## v1.5.34

### 修复

- 群文件清理改为循环扫描删除，避免适配器单次只返回前 50 个文件时清理提前结束。

## v1.5.33

### 调整

- 群文件清理递归扫描所有文件夹并去重，不设置清理数量上限。

## v1.5.32

### 调整

- 移除群文件批量删除间隔配置，群文件清理固定最高频率连续删除。

## v1.5.31

### 调整

- 群文件清理结果文案去掉句末 `。`。

## v1.5.30

### 新增

- 新增群文件清理命令 `清理群文件` / `群文件清理`。
- 新增 `_conf_schema.json` 插件配置，使用 `group_file_cleanup_admin_qq` 配置允许使用群文件清理的 QQ 白名单。
- 群文件批量清理通过枚举群文件后逐个调用 `delete_group_file` 完成；未发现 go-cqhttp/NapCat 支持单接口批量删除多个群文件。

## v1.5.29

### 新增

- 新增卡片白名单域名 `changdunovel.com`，命中该域名的 JSON/外部分享不撤回。

### 调整

- 卡片诊断日志单段截断长度从 300 提升到 2000，尽量保留完整 JSON 参数。

## v1.5.28

### 修复

- 将闪传诊断扩展为卡片诊断，群名片/JSON 卡片/合并转发/闪传撤回前都会输出真实消息段参数。

## v1.5.27

### 新增

- 闪传疑似消息触发时输出诊断日志，记录 `message_str`、`raw_message`、消息段类型、data、对象字符串和 message_id，便于按真实参数修规则。

## v1.5.26

### 修复

- QQ 闪传/文件卡片撤回兼容 aiocqhttp 显示为 `该消息类型暂不支持查看` 的情况。
- 收窄 QQ 闪传判断，不再按普通 OneBot `file`、`[CQ:file,...]` 或 AstrBot `ComponentType.File` 撤回普通文件。

## v1.5.25

### 新增

- 新增 QQ 闪传/文件卡片撤回，识别 AstrBot `ComponentType.File`、OneBot `file` 消息段、`[CQ:file,...]` 和展示文本 `QQ闪传`。

## v1.5.24

### 新增

- 数字撤回模块新增合并转发/聊天记录消息撤回，识别 OneBot `forward`/`node` 和 AstrBot `ComponentType.Forward/Node/Nodes`。

### 调整

- 撤回实现只保留 `bot.delete_msg(message_id=...)` 一个接口；OneBot 11 标准没有批量撤回接口，当前仍只撤回触发消息本身。

## v1.5.23

### 调整

- 将 `消息工具.py` 移动到 `功能文件/管理功能/`，保持管理相关消息解析集中存放。

## v1.5.22

### 调整

- 精简 `main.py`，将消息命令解析移动到 `功能文件/消息工具.py`，数字撤回入口移动到数字撤回模块。

## v1.5.21

### 调整

- 移除排查阶段加入的源码路径和版本号详细日志，保留子模块刷新和 `At` 组件对象识别修复。

## v1.5.20

### 修复

- 普通 `@人` 撤回判断兼容 AstrBot 组件对象，识别 `ComponentType.At` 和对象字符串中的 `[At:...]`。

## v1.5.19

### 修复

- 插件重载时强制刷新 `功能文件` 子模块，避免 AstrBot 只重载主插件后继续使用内存中的旧数字撤回代码。

## v1.5.18

### 调整

- 插件启动和数字撤回日志输出当前版本号与源码路径，用于确认 AstrBot 实际加载的代码版本。

## v1.5.17

### 修复

- 在实际执行撤回前再次拦截普通 `@人` 消息，防止其他调用路径绕过入口判断造成误撤回。

## v1.5.16

### 修复

- 普通 `@人` 消息在撤回判断入口直接放行，避免被群名片/卡片兼容逻辑或数字规则误撤回。

## v1.5.15

### 修复

- 数字撤回忽略普通 `@人` 消息，识别 `[CQ:at,...]`、`[At:...]` 和 OneBot `at` 消息段，避免误撤回。

## v1.5.14

### 修复

- 群名片撤回兼容 AstrBot 将 JSON 卡片放在组件对象中的情况，识别 `ComponentType.Json`、`ComponentType.Share` 和 `ComponentType.Contact`。

## v1.5.13

### 修复

- 群名片撤回兼容 AstrBot 显示为 `[ComponentType.Json]` 的 JSON 卡片消息。

## v1.5.12

### 修复

- `main.py` 兼容旧版数字撤回模块，避免插件半更新时因缺少 `是否群名片消息` 报错。

## v1.5.11

### 新增

- 新增群名片撤回，识别 `[CQ:contact,type=group,...]` 和 OneBot `contact` 群消息段。

## v1.5.10

### 修复

- 主流程在数字撤回前先拦截链接消息，兼容明文链接、URL 编码链接和常见域名路径，避免分享链接仍被撤回。

## v1.5.9

### 修复

- 数字撤回忽略包含 `http://` 或 `https://` 的链接消息，避免小说分享链接中的时间戳、书籍编号等数字误触发。

## v1.5.8

### 修复

- oiapi 命令文本独立读取 `message_str`、`raw_message` 和 text 消息段，修复 aiocqhttp 下 `随机一言` 等命令不命中的问题。

## v1.5.7

### 修复

- oiapi 指令优先于数字撤回判断，避免 `随机英文单词` 等命令被撤回逻辑拦截。
- 数字撤回不再读取 `raw_message` 字符串，避免 QQ 官方机器人 ID 中的数字误触发。

## v1.5.6

### 修复

- 数字撤回优先并仅使用 `event.message_str` 可见文本，避免继续扫描 QQ 官方机器人对象 ID 造成 oiapi 误触发。

## v1.5.5

### 修复

- 数字撤回只使用可见文本和 text 消息段做判断，避免 QQ 官方机器人对象 ID 中的数字误触发。
- 文本提取会清理 `[At:...]` 和 CQ at/reply 片段，恢复 `@机器人 随机英文单词` 等 oiapi 回复。

## v1.5.4

### 修复

- `main.py` 兼容旧版数字撤回模块，避免插件半更新时因缺少 `获取消息文本` 导入失败。

## v1.5.3

### 修复

- 数字撤回改为从 `raw_message`、`message` 消息段和 `message_str` 中提取文本，修复 `你好8086924771` 这类混合文本不触发的问题。

## v1.5.2

### 修复

- 数字撤回补充 `bot.api.call_action("delete_msg")` 撤回方式。
- 当前消息编号支持从 event 顶层兜底读取，避免适配器字段位置不同导致撤回失败。

## v1.5.1

### 调整

- 数字撤回现在会匹配消息中任意连续 9 到 12 位数字，例如 `你好1078887813`。
- 数字中间被其他字符打断时不会触发，例如 `107888A7813`。

## v1.5.0

### 新增

- 新增 `管理功能/数字撤回.py`。
- 当用户发送整条消息为 9 到 12 位连续数字时，插件会尝试撤回该消息。
- 数字撤回命中后不发送回复，避免刷屏。

### 调整

- `main.py` 在 oiapi 功能匹配前先执行数字撤回检查。

## v1.4.0

### 新增

- 新增 `古诗词名句` 功能，接入 `https://oiapi.net/api/Sentences`。
- 新增 `随机英文单词` 功能，接入 `https://oiapi.net/api/RandEnglishDict`。
- 新增 `随机一言` 功能，接入 `https://oiapi.net/api/AWord`。
- 新增 `疯狂星期四` 功能，接入 `https://oiapi.net/api/KFC`。

### 调整

- 将功能代码分类存放到 `功能文件/oiapi/` 目录。
- `main.py` 只保留插件注册、消息监听和功能调用。
- README 改为功能导航和折叠详情结构。

### 依赖

- 添加 `aiohttp` 作为异步 HTTP 请求依赖。
