# 馒头bot

适用于 AstrBot 的小说下载与群聊管理插件。

## 项目信息

| 项目 | 内容 |
| --- | --- |
| 插件名 | 馒头bot |
| 作者 | 馒头 |
| 版本 | v5.31.1 |
| 仓库 | https://github.com/TimShitPig/mantou_Bot |

## 功能导航

| 分类 | 功能 | 触发文本 | 说明 |
| --- | --- | --- | --- |
| 管理功能 | 群管功能 | `连续 9-12 位数字` / 群名片 / 空间相册分享 / 合并转发 / QQ 闪传 / `禁言 @成员` / `禁 @成员 1` / `解除禁言 @成员` / `解 @成员` | QQ 群主和管理员发送的消息不会被自动撤回；普通成员触发撤回后拉取 100 条群历史并最多撤回该用户最近 8 条消息；广告消息撤回并成功禁言后只发送一条“@发送者 / 请勿发送此类消息 / 如果是小说请联系群主”提醒；单成员禁言成功后会 @被禁言成员并提示联系群主；支持 OneBot 与 QQ 官方群；暂不提供踢人和全体禁言功能 |
| 管理功能 | 帮助功能 | `帮助` 后发送数字 / `帮助 数字` / `0` | 仅群文件清理管理员白名单 QQ 可用，按主动触发和被动触发查看小类、触发指令和详情；QQ 官方帮助按钮全部使用原生回调，点击后静默切换菜单或执行快捷命令 |
| 管理功能 | 卡片撤回 | 群名片/群分享/JSON 卡片/空间相册分享/合并转发/QQ 闪传 | 自动撤回群名片、JSON 卡片、QQ 官方显示为“暂不能查看该消息内容”的空间相册分享、合并转发和 QQ 闪传消息 |
| 管理功能 | 小说开关 | `小说` / `小说列表` / `开小说` / `关小说` / `开番茄` / `关番茄` / `开七猫` / `关七猫` / `开书旗` / `关书旗` / `开QQ阅读` / `关QQ阅读` / `开QQ浏览器` / `关QQ浏览器` / `开得间` / `关得间` / `开点众` / `关点众` / `开知乎` / `关知乎` / `开塔读` / `关塔读` / `开百度` / `关百度` / `开小米` / `关小米` / `开宜搜` / `关宜搜` / `开米读` / `关米读` / `开猫眼` / `关猫眼` / `开酷我` / `关酷我` / `开酷匠` / `关酷匠` / `开连城` / `关连城` / `开菠萝包` / `关菠萝包` / `开晋江` / `关晋江` / `开启/关闭对应功能` / `开测试` / `关测试` | 仅群文件清理管理员白名单 QQ 可用；`开小说`、`关小说`是全部群聊共用的总开关，关闭时小说下载和找书均不可用；重新开启后仍保留各平台原有的单独开关状态 |
| 管理功能 | 小说下载任务 | 小说链接/分享卡片 | 每本小说都创建独立后台任务，不设置跨平台或跨书全局队列；下载 QQ 阅读时，其他平台、其他书籍和同平台后续书籍仍可同时开始处理 |
| 管理功能 | 小说网盘 | `网盘` / `网盘状态` / `当前网盘` / `换UC` / `换夸克` / `换百度` / `夸克登录` / 直接粘贴三种网盘 Cookie | 仅群文件清理管理员白名单 QQ 可用；查看或切换主分享网盘；私聊发送 `夸克登录` 可按夸克网页当前扫码协议获取并保存网页 Cookie，也可直接粘贴 UC、夸克或百度 Cookie 覆盖 MySQL 登录态；插件重载时保留待上传 TXT，自动恢复未完成的主网盘上传，完成后再清理缓存 |
| 管理功能 | 状态 | `状态` | 仅群文件清理管理员白名单 QQ 可用，查看系统位数、CPU、物理内存、磁盘、进程数、操作系统、框架版本、数据库状态、系统运行时间和当前时间；每次输入时检查一次数据库 |
| 管理功能 | 找书 | `找关键词` / `找书 关键词` / `找书名 关键词` / `找作者 关键词` / `上一页` / `下一页` | 聚合搜索番茄、七猫、QQ阅读、QQ浏览器、书旗、得间、点众、塔读、百度小说、小米小说、宜搜、米读、猫眼、酷我、酷匠、连城、菠萝包和晋江，每页 5 条；自动识别书名或作者，也可显式指定；按匹配方式、实际匹配字数、覆盖率和跨平台共识排序，无关结果不展示；搜索时跳过已明确没有目录的番茄记录，以及目录预检为章节单独付费的 QQ 阅读记录；不显示链接和来源；官方机器人正文中的书名和作者可点，点击后填入下载指令并发送即可下载 |
| 管理功能 | 番茄小说 | 番茄链接/番茄 JSON 分享卡片 | 默认使用本地番茄畅听下载；详情、目录和正文均请求畅听接口，正文按每批 1500 章通过批量接口一次请求并复用 HTTP/2 异步会话，最多 5 路动态并发，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 七猫小说 | 七猫链接/七猫分享卡片 | 识别七猫长篇/短篇链接，正文优先批量下载，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 书旗小说 | 书旗链接/书旗分享卡片 | 识别书旗长篇/短篇/短链链接；使用整本下载接口获取 SQB 压缩包，按目录顺序解密 `.sqc` 正文并合成 TXT；完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 得间小说 | 得间链接/得间分享卡片 | 先通过 `batchDownloadChapteres` 批量获取整本章节正文地址，再复用同一个异步 HTTP 会话和连接池并发下载；每章只申请必要的授权令牌并使用 `PyCryptodome`/`gmpy2` 库解密，按章节数动态调度最高 500 路并发，批量地址缺章时不生成部分 TXT；完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | QQ阅读 | QQ阅读详情/目录/分享链接、`book.qq.com/book-detail/数字`、直接粘贴含 `ywguid` / `ywkey` 的 Cookie | 使用参考 App 核心、动态网关签名、动态密钥池和 libfock 解密；普通正文每批 200 章、最多 100 路并发，复用同一个异步 HTTP 会话，批量只请求一次；正文包解密使用 PyCryptodome AES/DES/CTR，CPU 解密单独限流；空正文、解密失败或响应缺失章节按连续最多 31 章窗口、最多 100 路并发重试，最多 3 轮，并记录受控 DEBUG 汇总，某轮没有恢复章节时立即停止；管理员直接发送普通 Cookie、`ywkey: 值` / `ywguid: 值` 两行文本、Cookie 头、curl、Cookie Editor JSON 或 cookies.txt 即可保存登录态，新 Cookie 覆盖旧值；登录态只写入数据库，不创建本地账号池、HAR 或 Cookie 文件；下载时优先加载数据库登录态并覆盖固定 `uid/usid`；普通目录 HTTP 400 时自动切换出版资源目录，`site=4` 出版书按 EPUB spine 顺序合并正文；完整合成 TXT 后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | QQ浏览器小说 | QQ 浏览器小说链接、`qb://ext/novelreader?...bookId=数字` | 仅使用 QQ 浏览器公开搜索、详情、目录和正文接口；按番茄格式生成带免责声明的 UTF-8 CRLF TXT，并交给现有小说完成出口；不包含 Cookie、EPUB、封面或独立上传功能 |
| 管理功能 | 点众小说 | 点众链接/点众分享卡片 | 全程使用点众 App 详情、目录和单章正文接口；目录按窗口最多 500 路并发读取，正文按章节数动态分配独立 App 会话，最高 60 路并发并复用各自 HTTP 连接，避免同一身份的广告解锁状态互相覆盖；会员锁定章节不重复重试，仅失败章节使用新会话重试，缺少任意正文时停止合成；完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 知乎 | `story.zhihu.com/manuscript/paid_column/专栏ID/章节ID` 分享链接/分享卡片 | 一条分享链接只下载其中的当前分享内容；用 `header` 补充标题和作者，正文只请求一次对应的 `q=专栏ID&a=章节ID`，失败时最多重试 3 次并回退同一章节恢复流程，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 塔读小说 | `reader.tadu.com` 书籍链接/找书结果 | 使用塔读 App 详情、目录和正文接口；目录缺少地址时回退单章地址接口，HTTP 使用 aiohttp 连接复用，最高 400 路动态并发；TDZ 解密、AES/DES 算法和正文解析与下载逻辑合并在同一个 `塔读小说.py` 文件内，缺章不生成 TXT，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 百度小说 | 百度小说链接/百度小说分享卡片 | 识别 `mr.baidu.com`、`boxnovel.baidu.com`、`novel.baidu.com` 及 `boxnovel.baidu.com/boxnovel/yuedu/wapdetail?...doc_id=...` 分享页；出版源使用百度阅读 `doc_id` 详情、目录和正文接口，普通源使用 AES 正文接口，按番茄格式生成完整 TXT；缺章或出版源正文不可用时不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 小米小说 | 小米浏览器小说链接/小米小说分享卡片 | 使用小米当前 `dushu.xiaomi.com` 搜索、详情、目录和正文接口，兼容 `reader.browser.miui.com` 旧链接及分享卡片，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 宜搜小说 | 宜搜小说链接/分享卡片 | 使用宜搜 App 详情和分页目录接口，复用异步 HTTP 会话并发请求单章正文，正文用 PyCryptodome DES-CBC 解密，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 米读小说 | 米读小说链接/分享卡片 | 使用米读详情、目录和正文接口，复用异步 HTTP 会话并发下载，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 猫眼小说 | 猫眼/掌阅小说链接 | 使用猫眼公开详情、XML 目录和正文接口，复用异步 HTTP 会话并发下载并清理 HTML 正文，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 酷我小说 | 酷我小说链接/分享卡片 | 使用酷我详情、目录和正文接口，复用异步 HTTP 会话并发读取明文正文，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 酷匠小说 | 酷匠小说链接/分享卡片 | 使用酷匠详情、目录和正文接口，复用异步 HTTP 会话并发请求，正文用 PyCryptodome AES-CBC 解密并 gzip 解压，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 连城小说 | 连城小说链接/分享卡片 | 使用连城详情、目录和 HTML 正文接口，复用异步 HTTP 会话并发下载并清理 HTML，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 菠萝包小说 | 菠萝包小说链接/分享卡片 | 使用菠萝包详情、目录和正文接口，复用异步 HTTP 会话并发读取明文正文，按番茄格式生成完整 TXT；缺章不生成部分文件，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |
| 管理功能 | 晋江小说 | 晋江小说链接/分享卡片 | 使用晋江公开详情、目录和正文接口，复用异步 HTTP 会话并发读取可公开章节；付费或明确锁定章节不请求，缺少任意公开章节时不生成部分 TXT，完成后上传当前选择的小说网盘并发送“点击打开”按钮 |

<details>
<summary>查看群管功能</summary>

用户发送的消息中包含连续 9 到 12 位数字时，插件会尝试撤回该消息。群名片、JSON 卡片、QQ 官方显示为“暂不能查看该消息内容”的空间相册分享、合并转发和 QQ 闪传也会走同一个撤回模块。QQ 群主和管理员发送的消息不会被自动撤回；插件会优先用 OneBot `get_group_member_info` 查询真实 QQ 群成员角色，只有查不到时才使用事件里的 `role` 兜底。普通成员当前消息撤回成功后，会通过 `get_group_msg_history` 拉取最近 100 条群历史，筛选该用户最近消息并逐条调用 `delete_msg` 撤回，默认最多撤回 8 条；广告消息撤回并成功禁言后只发送一条分三行的提醒：@发送者、请勿发送此类消息、如果是小说请联系群主。

本模块不提供踢人和全体禁言功能。发送 `禁言 @成员` 或 `禁 @成员` 默认禁言 7 天；`禁 @成员 1` 表示 1 天，带 `秒`、`分`、`时`、`天` 时按对应单位计算；成功后会 @被禁言成员并提示“你已经被禁言，请联系群主说明情况”；`解除禁言 @成员` 或 `解 @成员` 立即解除。OneBot 使用 `set_group_ban`，QQ 官方群使用官方成员禁言接口。

会触发：`123456789`、`你好1078887813`、`你好A1078887813 你好`

不会触发：`12345678`、`1234567890123`、`107888A7813`、`123 456 789`、包含 `http://` 或 `https://` 的链接消息

</details>

## 项目结构

```text
.
├── main.py
├── metadata.yaml
├── requirements.txt
├── README.md
└── 功能文件/
    ├── 下载缓存/
    └── 管理功能/
        ├── 基础功能/
        │   ├── 帮助功能.py
        │   ├── QQ官方交互桥.py
        │   ├── 权限工具.py
        │   ├── 状态功能.py
        │   ├── 消息工具.py
        │   └── 运行状态数据库.py
        ├── 小说功能/
        │   ├── 小说/
        │   │   ├── 番茄小说.py
        │   │   ├── 七猫小说.py
        │   │   ├── 书旗小说.py
        │   │   ├── QQ阅读.py
        │   │   ├── QQ浏览器小说.py
        │   │   ├── 得间小说.py
        │   │   ├── 点众小说.py
        │   │   ├── 知乎小说.py
        │   │   ├── 知乎分享恢复客户端.py
        │   │   ├── 塔读小说.py
        │   │   ├── 百度小说.py
        │   │   ├── 小米小说.py
        │   │   ├── 宜搜小说.py
        │   │   ├── 米读小说.py
        │   │   ├── 猫眼小说.py
        │   │   ├── 酷我小说.py
        │   │   ├── 酷匠小说.py
        │   │   ├── 连城小说.py
        │   │   ├── 菠萝包小说.py
        │   │   └── 晋江小说.py
        │   └── 功能/
        │       ├── 找书.py
        │       ├── 小说功能开关.py
        │       └── 下载缓存清理.py
        ├── 网盘功能/
        │   ├── UC网盘.py
        │   ├── 夸克网盘.py
        │   ├── 百度网盘.py
        │   ├── 网盘Cookie.py
        │   └── 小说网盘.py
        └── 群聊功能/
            ├── 群列表工具.py
            └── 群管功能.py
```

`main.py` 只负责 AstrBot 插件注册、消息监听和功能调用，具体功能代码放在 `功能文件/` 目录中。

## 安装方式

将插件目录放入 AstrBot 的 `data/plugins` 目录，然后在 AstrBot WebUI 的插件管理中重载插件。

## 依赖说明

依赖已写入 `requirements.txt`：

```text
aiohttp
pycryptodome
pymysql
qrcode[pil]
```

## 插件配置

| 配置项 | 说明 |
| --- | --- |
| `group_file_cleanup_admin_qq` | 管理员 QQ 白名单，可使用帮助、状态、小说网盘切换、保存三种网盘与 QQ阅读 Cookie、小说开关和群管指令 |
| `uc_pan_settings` | UC 网盘配置分类，包含 `uc_pan_cookie` 和 `uc_pan_upload_dir`；发送 `换UC` 后作为小说主分享网盘 |
| `quark_pan_settings` | 夸克网盘配置分类，包含 `quark_pan_cookie` 和 `quark_pan_upload_dir`；发送 `换夸克` 后作为小说主分享网盘 |
| `baidu_pan_settings` | 百度网盘配置分类，包含 `baidu_pan_cookie`、`baidu_pan_upload_dir` 和 `baidu_pan_upload_status`；发送 `换百度` 后作为主分享网盘，选择其他主网盘时仍可按状态后台备份 |
| `database_settings` | 数据库配置分类，包含 `database_host`、`database_port`、`database_user`、`database_password`；数据库名默认与用户名相同 |

小说下载功能全部免费可用，不保留用户激活、卡密、收费、付费或每日免费额度。运行状态仅使用 MySQL 的 `mantou_runtime_state` 表保存三种网盘 Cookie、QQ阅读登录态、小说功能开关和当前主网盘；数据库配置在插件配置最下面的 `database_settings` 分类内，数据库名默认与用户名相同。未配置数据库时不会尝试连接 MySQL，小说功能默认全部开启，主网盘默认 UC，相关运行状态无法持久化。管理员白名单内的 QQ 可直接粘贴 UC、夸克、百度或 QQ阅读 Cookie，无需命令前缀；三种网盘登录态保存在 `novel_pan_auth` 命名空间，QQ阅读只保存 `ywguid/ywkey`。新 Cookie 覆盖旧值，数据库登录态优先于插件配置。动态密钥池仍只保存在当前进程内，不写入本地 `keypool.cache`。

群文件清理管理员白名单内的 QQ 可发送 `状态` 查看系统位数、CPU占用、物理内存、磁盘空间、系统进程、操作系统、框架版本、数据库状态、运行时间和当前时间。每次输入 `状态` 时只执行一次数据库连通性检查，仅显示 `正常`、`未配置` 或 `异常`，不显示 Cookie、账号、IP、密钥、数据库地址、功能开关或网盘信息。

群文件清理管理员白名单内的 QQ 可发送 `帮助` 查看 `主动触发` / `被动触发` 大类，随后在 120 秒内发送数字进入小类，再发送数字查看触发指令列表，再发送数字查看详情；发送 `0` 返回上一层。也可直接发送 `帮助 1` 查看指定大类。QQ 官方 Markdown Keyboard 的帮助菜单、返回和详情快捷命令按钮均使用原生 `action.type=1` 回调：点击后不会向聊天发送指令，互动桥会投递到后端完成菜单切换或执行快捷命令；群聊仍走 `/v2/groups/{group_openid}/messages` 群消息接口。普通成员无法触发帮助功能。

群文件清理管理员白名单内的 QQ 可发送 `开启平台名` / `关闭平台名` 或 `开平台名` / `关平台名`，独立开关番茄、七猫、书旗、QQ阅读、QQ浏览器、得间、点众、知乎、塔读、百度、小米、宜搜、米读、猫眼、酷我、酷匠、连城、菠萝包和晋江小说下载功能；状态写入 MySQL 的 `mantou_runtime_state` 表，重载后继续生效。关闭后用户发送对应小说链接会直接回复该功能已关闭，不进入下载流程。

知乎小说识别 `story.zhihu.com/manuscript/paid_column/专栏ID/章节ID` 分享链接及卡片。一条分享链接只处理其中的当前章节，不再请求完整专栏目录或相邻章节。`https://api.zhihu.com/km-indep-home/manuscript/{专栏ID}/{章节ID}/header` 用于补充当前章节标题和作者；正文只请求一次对应的提取服务参数（`q=专栏ID&a=章节ID`）。请求失败时只重试和恢复同一章节；正文标题必须与详情标题一致，成功后单章合成为一个 TXT 并上传当前选择的小说网盘。

群文件清理管理员白名单内的 QQ 可发送 `网盘` / `网盘状态` / `当前网盘` 查看 UC、夸克和百度网盘配置状态；发送 `换UC`、`换夸克` 或 `换百度` 切换所有小说下载共用的主分享网盘。选择结果写入 MySQL `mantou_runtime_state`（namespace=`novel_share_pan`，state_key=`active`），重载后继续生效；只调用当前选择的网盘，不自动回退其他网盘。管理员可在私聊发送 `夸克登录`，使用夸克网盘 App 扫描 5 分钟有效二维码，成功后自动把网页 Cookie 保存到 MySQL；重复发送会取消上一轮等待。管理员也可直接粘贴普通 Cookie、`Cookie:` 请求头、curl、Cookie Editor JSON 或 Netscape cookies.txt；插件按字段自动识别平台，识别不明确时可加 `UC Cookie:`、`夸克 Cookie:` 或 `百度 Cookie:` 前缀。

UC Cookie 可继续填写到 `uc_pan_cookie`，也可由管理员直接粘贴包含 `UDRIVE_TRANSFER_SESS` 的完整 Cookie；聊天保存值写入 `novel_pan_auth/uc` 并优先于配置。上传目录读取 `uc_pan_upload_dir`，默认 `/小说机器人`。上传前会分页扫描并删除目标目录下与原小说文件名完全相同的远端普通文件，不删除同名文件夹；如果创建上传目录时返回同名冲突，会重新查找已有目录继续上传；如果 UC 上传仍返回同名冲突，插件会重新扫描并删除同名旧文件，再继续使用原小说文件名重试，不再改用备用远端文件名；如果上传完成接口返回 `23008` / `file is doloading`，插件会复用预上传阶段返回的文件 ID 继续创建分享，创建分享时文件仍在处理中也会等待重试。

夸克 Cookie 可继续填写到 `quark_pan_cookie`，也可由管理员私聊发送 `夸克登录` 扫码自动获取，或直接粘贴包含 `b-user-id` / `__uid` / `__kps` 之一和 `__puus` 的完整 Cookie；聊天保存值写入 `novel_pan_auth/quark` 并优先于配置。每次发送 `夸克登录` / `登录夸克` / `刷新夸克Cookie` 都会先彻底取消上一轮等待，再用唯一防缓存请求获取全新二维码。扫码流程跟随当前网页二维码参数，非成功轮询状态继续等待，登录态换取固定携带 `fr=pc` 与 `platform=pc`。夸克请求完全按当前网页端 `drive-pc.quark.cn` 参数执行，短时 `ctoken` 不再加入请求参数；上传目录读取 `quark_pan_upload_dir`，默认 `/小说机器人`。插件逐级创建目录、删除同名旧文件并等待删除任务完成，再按网页端预上传、哈希校验、OSS 单分片和完成上传签名创建文件，最后通过分享接口取得短链接；请求期间刷新 `__puus` 时会受控回写最新值。

百度 Cookie 可继续填写到 `baidu_pan_cookie`，也可由管理员直接粘贴包含 `BDUSS` 和 `STOKEN` 的完整 Cookie；聊天保存值写入 `novel_pan_auth/baidu` 并优先于配置。上传完成后创建带提取码的永久分享链接，提取码写入链接查询参数供按钮直接打开。选择 UC 或夸克为主网盘时，百度仍按 `baidu_pan_upload_status` 做后台备份；完结同名文件已存在时跳过，连载删除同名普通文件后重传。百度作为当前主分享网盘时跳过重复后台备份，且不受后台状态筛选限制。

任一主网盘上传成功后，QQ 官方机器人都在同一条消息中发送“宝宝你的 / 书名 / 作者 / 已经下载完成”、备用“点击此文字打开”Markdown 链接和“点击打开”链接按钮；当前主网盘未配置或上传失败时仅回复 `文件发送失败，请稍后再试`，不会改用其他网盘或发送原小说 txt。

七猫小说支持长篇链接和短篇分享链接，正文优先使用 App 批量接口下载（默认每批 50 章、最多 200 个批次异步并发），短篇会走短篇详情/目录和 `reader_agent=1` 正文接口。书旗小说支持 `shuqi.com/book/数字.html`、`t.shuqi.com/book/数字`、`t.shuqi.com/catalog/数字/`、`t.shuqi.com/cover/数字`、`t.shuqi.com/shortNovel/reader/数字` 和 `d.shuqi.com/短码`；书旗搜索、联想、链接识别和下载都保留在 `书旗小说.py`，下载部分以 `download_shuqi.php` 的固定 UID、盐值、M9 加密和整本 SQB/SQC 解密为核心，目录和整本下载地址并行请求，按目录章节数修正 `try_N` 后只下载一次压缩包并按目录顺序合成；缺少任意目录或正文时停止，不生成部分 TXT。下载完成后会临时写入 `功能文件/下载缓存/` 供当前主网盘分享和百度后台备份；QQ 不直接接收 txt 文件。插件每次启动或重载都会删除该目录遗留的小说 txt。发送文件名格式为 `[完结]书名：xxx 作者：xxx.txt` 或 `[连载]书名：xxx 作者：xxx.txt`，txt 文件顶部会写入免责声明。

点众小说只使用 App 详情、目录、单章正文和广告解锁接口。目录按窗口最多 500 路并发获取；正文按章节数动态创建独立 App 会话，首轮从 16 路开始、成功率稳定时最高提升至 60 路，每路复用自己的 HTTP Session 并串行解锁，避免共享身份导致正文解锁互相覆盖。遇到没有广告解锁入口的会员章节会直接标记为会员锁定，不重复请求；首轮后仅其他失败章节使用新会话低并发重试，缺少任意章节时停止合成。

塔读小说使用单文件实现：详情、批量目录和缺失章节地址补全均走 App 接口，目录和正文下载复用 aiohttp 会话，正文动态并发最高 400；TDZ 容器解密、AES/DES 算法和 UTF-16LE 正文解析直接内嵌在 `塔读小说.py`，不拆分独立加密模块。目录或正文缺少任意章节时停止合成。

塔读游客会话启动时自动注册；服务端返回有效期后，在到期前按 `earlyTime` 自动调用 `/user/api/token/get` 续期，续期失败再重新注册游客会话。登录态只保存在当前进程内，不写入插件配置或本地会话文件。

QQ 浏览器小说只识别 `bookshelf.html5.qq.com`、`novel.html5.qq.com`、`qbnovel.qq.com` 和 `qb://ext/novelreader` 书籍链接；搜索使用 QQ 浏览器小说聚合接口，详情、目录和正文使用公开阅读接口，正文按批次并发读取，缺少任意章节时停止合成。下载前会回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`，TXT 格式与番茄一致，正文完成后复用现有小说网盘完成出口。

番茄小说识别 `fanqienovel.com`、`changdunovel.com`、`fqnovel.com` 和 `novelfm.com` 链接，支持 `changdunovel.com/t/短码` 分享短链和 JSON 卡片中的链接。默认走小说功能内置的番茄畅听下载逻辑：先获取书籍详情和公开目录，再使用 App 正文批量接口并发拉取小说正文，只生成 TXT，不接入音频、媒体下载、探测或命令行调试功能。下载前会先回复书名、作者、状态、章节、字数和 `正在下载中请稍等.....`；只显示接口提供的原始精确总字数，畅听副本缺少该字段时必须经书名、作者和章节数严格核验同书规范记录后补查，无法核验则显示“未知”。外部提示不显示简介，txt 文件头部保留简介，文件名格式与七猫一致。

番茄小说写入 `功能文件/下载缓存/` 后仅用于当前主网盘分享和百度后台备份；不再通过 AstrBot `File` 组件或 OneBot 文件接口发送 txt。主网盘上传成功后发送下载完成提示与“点击打开”按钮，源缓存由后台任务结束后清理；当前主网盘未配置或上传失败时只回复固定的文件发送失败提示。

QQ 官方机器人 `qq_official` 发送文件应优先走 AstrBot `File` 组件，它会封装官方富媒体接口并发送 `media` 消息；如果 QQ 官方群聊返回 `call inner proxy error`，这是平台富媒体上传限制或临时错误。OneBot 的 `upload_group_file`/`upload_private_file` 只适用于 OneBot 适配器回退。

## 参考链接

- [AstrBot 官方仓库](https://github.com/AstrBotDevs/AstrBot)
- [AstrBot 插件开发文档](https://docs.astrbot.app/dev/star/plugin-new.html)
